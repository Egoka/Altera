/**
 * Ритм лент публичных страниц: явные списки раскладок из реестра `articleLayouts`.
 *
 * Явное перечисление честнее генератора — ритм ленты это редакционное решение,
 * оно должно читаться в одном месте и видеться в диффе. Круг ритма обязан быть
 * непротиворечивым сам по себе, иначе `validateRhythm` скажет об этом вслух
 * (раньше хвост молча не рендерился). Ритмы проверяются тестами
 * `tests/articleLayouts.test.ts`, `tests/feedGroups.test.ts` и `tests/authorFeed.test.ts`.
 */

/**
 * «Новое» на главной. Число материалов подборки заранее не известно (окно новизны, до 12
 * карточек), поэтому круг ритма проходит `buildFeedGroups`, а остаток ложится в запасную
 * раскладку под него.
 */
export const LATEST_RHYTHM = ["hero-left", "quad-square", "trio-uneven", "stripe-wide-narrow", "trio-tall"]

/**
 * Лента рубрики: один круг ритма — страница в 24 материала (`section-feed.md` §5).
 * Открывается `lead-pair-right` (решение владельца 2026-09-13), дальше масса группы
 * ходит справа налево; порядок не повторяет ленты главной. Хвост неполной страницы
 * собирает `buildFeedGroups` запасной раскладкой под остаток.
 */
export const SECTION_RHYTHM = [
  "lead-pair-right",
  "hero-right",
  "trio-uneven",
  "quad-square",
  "mirror-tower",
  "stripe-wide-narrow",
  "feature-stack"
]

/**
 * Лента автора читается как хроника (`author.md` §5): только однорядные раскладки,
 * в которых порядок чтения слева направо совпадает с порядком по дате. Башни и
 * слоты на несколько рядов сюда не ставятся — в них порядок неочевиден. Круг
 * открывается одиночной статьёй во всю строку (владелец, 2026-09-14) и вмещает
 * одиннадцать материалов; материалы делятся по месяцам, и каждый месяц
 * собирается своим кругом (`groupByMonth` + `buildFeedGroups`), то есть каждый
 * месяц тоже открывается одиночной статьёй.
 */
export const AUTHOR_RHYTHM = ["solo-wide", "stripe-wide-narrow", "pair-tall", "trio-uneven", "trio-tall"]
