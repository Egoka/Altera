/**
 * Ритм лент стартовой страницы: явные списки раскладок из реестра `articleLayouts`.
 *
 * Явное перечисление честнее генератора — ритм ленты это редакционное решение,
 * оно должно читаться в одном месте и видеться в диффе. Сумма вместимостей
 * обязана совпасть с числом материалов, иначе `validateRhythm` скажет об этом
 * вслух (раньше хвост молча не рендерился). Обе ленты проверяются тестом
 * `tests/articleLayouts.test.ts`.
 */

/** «Новое». */
export const LATEST_RHYTHM = ["hero-left", "quad-square", "trio-uneven", "stripe-wide-narrow", "trio-tall"]

/**
 * «Востребованное» намеренно не повторяет ритм «Нового»: раскладки другие и в
 * другом порядке, иначе две ленты подряд читаются как одна. Пара вертикалей
 * между двумя башнями — пауза между тяжёлыми группами.
 */
export const DEMANDED_RHYTHM = ["wide-trio-right", "pair-tall", "mirror-tower", "lead-pair-right", "trio-tall"]
