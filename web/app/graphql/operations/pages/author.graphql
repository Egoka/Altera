query GetAuthorPage($slug: String!, $page: Int = 1, $limit: Int = 20) {
  user(handle: $slug) {
    ...AuthorSummary
  }
  articlesByAuthor(authorHandle: $slug, page: $page, limit: $limit) {
    articles {
      ...ArticleCard
      sectionTags {
        name
        slug
      }
    }
    totalCount
    totalPages
    currentPage
  }
  authorStats(authorHandle: $slug) {
    totalArticles
    articlesThisMonth
    popularTags {
      name
      slug
    }
  }
}
