query GetSystemSettings($group: SystemSettingsGroup!) {
  systemSettings(group: $group) {
    group
    adapter
    canChange
    settings {
      key
      source
      secret
      configured
      value
      mask
    }
  }
}
