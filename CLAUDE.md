# Altera — Claude Code

Следуй [общим техническим инструкциям](AGENTS.md). Отвечай на текущий запрос
пользователя; прямые вопросы и задачи разработки не запускают процесс Multica.
Не вызывай её команды, не назначай себе задачу и не загружай её операционные
инструкции по наличию бэклога, старого плана или занятого parent.

Технические соглашения — [project-rules.md](docs/development/project-rules.md),
команды проверок — [testing.md](docs/development/testing.md); открывай по необходимости
для задачи. Чужие изменения сохраняй. Секреты не включай в файлы, вывод или отчёты.

Прямой запрос на код не требует паспорта, стадий, парного плана/отчёта или
разрешения от очереди. Полномочия определяет запрос пользователя.

Автоматизированный запуск получает свой процесс отдельно от этих общих инструкций.

## Что сломано

Сверено с кодом `app` 2026-09-26. Статусы задач — `docs/backlog/matrix.md`
(пересобирается `scripts/backlog/sync_status.py`). Срез `docs/vision/00-reality-check.md`
(§С2.7, 2026-09-20) отстаёт: вход, сессии, лимиты, sitemap и RSS там ещё значатся отсутствующими.

Реализовано и влито, но ещё не принято независимым ревью: вход `/login` и `/auth/verify`,
refresh/logout (T-022, T-023), лимиты частоты (T-024), sitemap и RSS (T-099).

Что ещё не работает или не готово:

- **Редактор и страница материала в кабинете**: `/me/articles/[slug]`, `/me/articles/[slug]/edit`,
  `/admin/articles/[slug]`, `/admin/articles/[slug]/edit`, `/admin/articles/new`, `/admin/me` и
  `/admin/sections` — заглушки. Пакет `content` и редактор не начаты; каталог блоков первого
  запуска решён (журнал §31), T-041 свободна от Q-02.
- **Хостинг, S3, почтовый провайдер и домен отложены (журнал §34)**: письма уходят только через
  `console`, файлы лежат в локальном адаптере, сборщик ошибок — `noop` (T-021, T-062, T-089
  утверждены владельцем). Прод-окружения в РФ нет (T-103). Выбраны GlitchTip/Sentry и YandexGPT.
- **Вход по паролю** решён владельцем для первого запуска (журнал §34), не реализован (T-115).
- **Пороги лимитов частоты** — `[ДОПУЩЕНИЕ]` до утверждения предложения архитектора (журнал §36,
  T-116).
- **SEO неполное**: `robots.txt`, sitemap, RSS и `noindex` есть; meta, canonical, hreflang, OG и
  JSON-LD есть лишь на отдельных страницах (`about`, `legal`). Сквозная задача T-100 не начата.
- **Проверка и публикация**: AI-проверка (T-048, `fake` разрешён журналом §32; критерии утверждены — §43),
  переходы статусов (T-049) и конвейер медиа (T-063, журнал §33) не начаты.
