# Качество, эксплуатация и данные

- **Статус**: принято владельцем 2026-09-05 (ADR-0011, ADR-0019, ADR-0024, ADR-0031, ADR-0032).
- **Основание**: тестов нет, CI маскирует падения, сид-скрипт отсутствует, прод-сборка веба
  не работает под Node 24, деплой-манифестов нет [ФАКТ: `00-reality-check.md` §5.6, §6].
- Этот документ владеет: источниками истины по данным, окружениями, сидами, миграциями и
  откатом, резервными копиями, тестами, CI, наблюдаемостью, обработкой ошибок, бюджетом
  инфраструктуры, housekeeping, структурой документации и списком исторических документов.

---

## 1. Где истина для каждого типа данных

| Данные | Истина | Копии и производные |
|---|---|---|
| Пользователи, роли, доверие, блокировки, согласия | PostgreSQL `users` | Кеш `me` не хранится |
| Сессии, magic link | PostgreSQL | — |
| Подписки, платежи, выплаты | PostgreSQL; у провайдера — его учёт, сверка по `pspPaymentId` | Вебхуки — `psp_webhook_events` |
| Материалы, версии, ревизии, теги, отборы, подборки | PostgreSQL | Кеш публичных списков (при Redis), SSR-кеш страниц |
| Медиа | Файлы — S3 (оригинал + варианты); метаданные, права, лицензии — PostgreSQL | — |
| Локали интерфейса | `web/i18n/locales/*.json` в репозитории | — |
| SEO-поля | PostgreSQL на версии; canonical и hreflang — вычисляются | — |
| Редакционные решения, модерация, аудит | PostgreSQL `audit_log` (с этапа 2), поля `*ById` | — |
| Продуктовые метрики | PostgreSQL (`article_read_daily`, платежи, подписки); отчёты — SQL в репозитории | Дашборд — этап 5 |
| Ошибки и события приложения | Сборщик ошибок (self-hosted Sentry-совместимый или провайдер РФ `[ДОПУЩЕНИЕ: выбор на этапе 1]`); логи — у хостинга с ретенцией 30 дней | — |
| Архитектурные решения | `docs/decisions/` | — |
| Контракт API | `server/src/graphql/**/*.graphql`; сгенерированные типы — производные | Справочник — генерируется |
| Дизайн-система | `web/app/assets/css/main.css` (токены) + `docs/vision/06-design-system.md` | — |
| Правила разработки | `CLAUDE.md`, `docs/plans/README.md`, `docs/reports/README.md` | — |
| Секреты | Хранилище секретов хостинга; `.env` только локально, в git не попадает [ФАКТ: `git check-ignore`] | — |

## 2. Окружения

| Окружение | База | Хранилище | Почта | Где | Назначение |
|---|---|---|---|---|---|
| dev | Docker PostgreSQL (`docker compose`) или ветка Neon без ПДн | MinIO в compose или бакет dev | Mailpit (compose) или консоль | ноутбук | Разработка, тесты |
| ci | PostgreSQL в контейнере GitHub Actions | фейк-адаптер | фейк | GitHub | Тесты, сборка, проверки |
| preview | Ветка Neon от анонимизированного дампа `[ДОПУЩЕНИЕ]` | бакет dev | консоль | локальный запуск сборки | Проверка миграций и сборки перед релизом |
| prod | Управляемый PostgreSQL в РФ с бэкапами | S3 в РФ | провайдер РФ | провайдер РФ (ADR-0011) | Пользователи |

Конфигурация — переменные окружения; список и назначение — в `server/env.example` и
`web/.env.example` (создать); адрес API — только в серверной части `runtimeConfig` Nuxt
(ADR-0023). Один Docker-образ на API и один на Nuxt; развёртывание по коммиту в `app` через
GitHub Actions с шагом миграций перед выкладкой (не внутри сборки [ФАКТ: `server/package.json:8`]).
Пин версии Node — `.nvmrc` и `engines` (сейчас 24 локально против 20 в CI); одна версия pnpm
вместо трёх [ФАКТ: `00-reality-check.md` §6].

## 3. Сиды

`prisma db seed` — идемпотентный скрипт (`upsert` по слагам): владелец (`owner`), редактор,
модератор, два автора (`trusted` и `probation`), читатель; шесть рубрик, четыре формата,
теги; десять материалов во всех статусах и обоих контурах, один с английской версией, один
отбор на главную, одна жалоба. Тот же сид используется в тестах и e2e. Для бенчмарка поиска —
отдельный генератор 10 000 материалов.

## 4. Миграции с данными и откат

Правила — `03-data-model.md` §2–3: миграции только с именем; `ADD VALUE` отдельной
миграцией; переименования через `RENAME`; деструктивные шаги (M11) — только после скрипта
проверки; `prisma migrate diff` пуст — шаг CI; `migrate deploy` — шаг релиза с автоматическим
дампом перед ним; откат — forward-миграция или восстановление дампа. Репетиция каждой
миграции с данными — на копии прода.

## 5. Резервные копии и восстановление

| Что | Как | Проверка |
|---|---|---|
| База | Ежедневные бэкапы управляемого PostgreSQL (хранение 30 дней) + еженедельный `pg_dump -Fc` в отдельный бакет (хранение 12 недель) | Восстановление на пустой инстанс раз в квартал с проверкой счётчиков — записывается в `docs/reports/` |
| Медиа | Версионирование бакета + еженедельная синхронизация во второй бакет | Выборочная проверка целостности |
| Секреты и конфигурация | Хранилище секретов хостинга; копия в парольном менеджере владельца | — |
| Репозиторий | GitHub + локальные клоны | — |

Первое восстановление проводится до открытой регистрации (DoD этапа 1).

## 6. Тесты (ADR-0031)

| Уровень | Инструмент | Что проверяет | С этапа |
|---|---|---|---|
| Контракт | скрипт валидации операций, `codegen --check`, проверка SDL | все операции валидны; в публичных типах нет ПДн | 0 |
| Резолверы | Vitest + PostgreSQL в контейнере + сид | права, видимость черновиков, статусные переходы, инвалидация кеша, лимиты | 0–1 |
| Пакет `content` | Vitest | схема, рендер (снимки), экспорт round-trip | 1 |
| Миграции | `migrate reset` + `migrate diff` + сид в CI; дамп-репетиция вручную | схема без drift; бэкфиллы на пустой базе | 1 |
| E2E | Playwright + Mailpit + фейк-провайдер | вход, публикация, проверка, отбор, конфликт, офлайн, платёж в песочнице | 0 smoke, далее по этапам |
| Качество | axe, Lighthouse CI | WCAG AA, бюджеты | 3 |
| Нагрузка | скрипт (k6 или autocannon `[ДОПУЩЕНИЕ]`) | p95 поиска, десять соавторов (6) | 3, 6 |

`pnpm test` падает при нуле тестов; покрытие строк не цель.

## 7. CI

Workflow на PR в `app` и на push в `app`:

1. `setup`: Node из `.nvmrc`, pnpm одной версии, кеш.
2. `lint` (`.output` и `.nuxt` в ignores), `format`, `typecheck` веба (`vue-tsc`) и сервера.
3. `codegen --check`, валидация операций.
4. `test`: сервер (с PostgreSQL-сервисом), веб, пакет `content`.
5. `migrate`: `migrate reset` на сервисе, `migrate diff` пуст, сид.
6. `build`: сервер и веб; запуск сервера → `GET /health = 200`; запуск собранного Nuxt →
   `GET / = 200`.
7. `e2e`: Playwright smoke на сиде.
8. с этапа 3: `quality` — axe и Lighthouse CI с бюджетами.

Никаких `|| true` [ФАКТ: сейчас `.github/workflows/pull_request.yml:64-65`]. Цель — до 10 минут
`[ДОПУЩЕНИЕ]`. Деплой — отдельный workflow на push в `app` после зелёного CI: дамп базы →
миграции → выкладка API → выкладка Nuxt → smoke по `/health` и `/`.

## 8. Наблюдаемость

- Логи: структурированный JSON (ADR-0032), уровни, идентификатор запроса из Nuxt в API; без
  ПДн и токенов; ретенция 30 дней у хостинга.
- Health: `GET /health` на API (база, Redis при включении) и на Nuxt; внешняя проверка
  доступности раз в минуту с уведомлением владельцу `[ДОПУЩЕНИЕ: сервис проверки]`.
- Ошибки: сборщик с группировкой и уведомлениями; фронт отправляет ошибки рендера и запросов.
- Метрики: на старте — SQL-отчёты (публикации, прочтения, подписки, платежи) и метрики
  хостинга (CPU, память, соединения); Prometheus-экспорт — при потребности.

## 9. Обработка ошибок

Словарь кодов и маскирование — ADR-0032. Фронт: по коду показывает состояние
(`06-design-system.md` §6); `NOT_FOUND` → 404, `FORBIDDEN` → 403, `RATE_LIMITED` → пояснение и
время, `CONFLICT` → баннер редактора, `INTERNAL_ERROR` → идентификатор запроса для письма в
поддержку.

## 10. Бюджет инфраструктуры

Гипотезы в ₽/мес по этапам `[ДОПУЩЕНИЕ: цены провайдеров РФ на 2026 год, проверить при
выборе]`:

| Этап | Состав | Оценка |
|---|---|---|
| 0 | Neon dev, GitHub Actions, домен, почта dev | 0–500 |
| 1–2 | Управляемый PostgreSQL (минимальный), VM или контейнеры для API и Nuxt, S3 (единицы ГБ), почтовый провайдер (до 10 000 писем), сборщик ошибок self-hosted | 3 000–8 000 |
| 3 | + бэкап-бакет, проверка доступности | 4 000–10 000 |
| 4–5 | + Redis, рост базы и трафика | 6 000–15 000 |
| 6 | + процесс совместной работы | 8 000–20 000 |

Правило контроля: счёт сверяется ежемесячно; рост более чем на 30 % без роста аудитории —
разбор.

## 11. Housekeeping

Один скрипт по расписанию хостинга (или `setInterval` в API при одном инстансе):
`read_events` старше 8 дней; `read_salts` старше 2 дней; сессии старше 30 дней после
истечения; использованные и просроченные magic link; прореживание `autosave`-ревизий старше
30 дней; пометка неиспользуемых медиа (5); отправка уведомлений подписчикам (3); продления и
просрочки подписок (4).

## 12. Структура документации

| Раздел из промта | Где живёт |
|---|---|
| Описание продукта | `docs/vision/01-product.md`; публичный README — с этапа 1 |
| Архитектура | `docs/vision/02-target-architecture.md` |
| Модель данных | `docs/vision/03-data-model.md`; истина — `schema.prisma` |
| Роли и права | `docs/vision/04-roles-and-access.md` |
| API | истина — `server/src/graphql/**/*.graphql`; справочник генерируется из SDL в `docs/api/` (этап 0) |
| Дизайн-система | `docs/vision/06-design-system.md` + `main.css` |
| Правила контента | `docs/vision/05-editor.md` §7–8 (каталог блоков, граница свободы); публичные правила публикации — `/legal/content-rules` |
| Процесс публикации | `docs/vision/05-editor.md` §2, `docs/vision/04-roles-and-access.md` |
| Эксплуатация | этот документ; runbook-и по инцидентам — `docs/guides/` с этапа 1 |
| Журнал решений | `docs/decisions/` |
| Журнал изменений | `docs/reports/` (по планам) + `CHANGELOG.md` по релизам с этапа 1 |
| Дорожная карта | `docs/issues/` |
| Планы и отчёты | `docs/plans/`, `docs/reports/` |

### Исторические документы

Помечены плашкой «Исторический документ» и не переписываются (список из
`00-reality-check.md` §7): `docs/architecture/database_architecture.md`,
`docs/status/api_implementation_status.md`, `docs/status/web_implementation_status.md`,
`docs/status/graphql_queries_structure.md`, `docs/status/README.md`,
`docs/guides/fonts-guide.md`, `docs/queries/README.md` (29 из 85 запросов невалидны), корневой
`altera-project-summary-2026-09-05.md`. Частично актуальные `docs/guides/admin_query_standards.md`
и `docs/guides/article_workflow.md` заменяются новыми документами на этапах 0–1 и тогда
помечаются.

### Правки `CLAUDE.md` после этапа 1

Локаль по умолчанию (RU), цикл статусов на языковой версии, новые команды (сид, codegen,
тесты), ветки, структура `packages/`. До этого — только фактические поправки (причина IPX 500,
прод-сборка под Node 24).

## 13. Процедуры последней инстанции

- Утрата доступа владельца: доступ к базе и скрипт назначения `owner` по e-mail с записью в
  аудит; хранится у владельца вне продукта.
- Компрометация секретов: ротация JWT-секретов (все сессии отзываются), ключей S3 и
  провайдера; запись в `docs/reports/`.
- Падение провайдера: восстановление из дампа и бакета у другого провайдера РФ (часы).

## 14. Что сознательно не делаем

- Kubernetes, микросервисы, очереди сообщений: один API, один Nuxt, один скрипт по расписанию.
- Мультирегиональность, CDN на старте (при необходимости — CDN провайдера перед S3).
- Внешние SaaS-аналитику и APM за рубежом.
- Собственный стенд «staging» с полной копией прода: превью на анонимизированной ветке.
