# T-NNN: <название задачи>

- **Эпик**: E-NN <имя эпика>
- **Статус**: кандидат | зависит: T-NNN | решение владельца: Q-NN | отложена: F-NN | готова | в работе | завершена
- **Параллельность**: нет: меняет продуктовый код | да: не меняет продуктовый код | да: только с <условие>
- **Роль-исполнитель**: <одна роль сквада>
- **Участники стадий**: архитектура — <роль или «нет»>; тестирование — <…>; ревью — <…>; документация — <…>; дизайн — <…>; SEO — <…>; релиз — <…>
- **Навыки**: <перечень через запятую: Prisma-миграции, GraphQL Yoga, Nuxt 4, Vitest, Playwright, …>
- **Authorization**: <задача со статусом `готова` | ссылка на прямое письменное поручение владельца и его границы>
- **Baseline**: <полный SHA до реализации>
- **Проверяемая revision**: <полный SHA; при dirty tree — ссылка на diff и SHA-256 затронутых файлов>

## 1. Результат

Что существует после выполнения — в утвердительной форме, одно-три предложения.

## 2. Источники

- Журнал: §N.M / #NN
- Спецификации: `docs/spec/…`
- ADR: ADR-NNNN (актуальный статус)

## 3. Границы и явно исключённое

Что входит; что не входит и в какой задаче/этапе лежит.

- Scope файлов и поведения: <…>
- Сохраняемый контракт (API, данные, роли, сценарии): <…>

## 4. Зависимости и что разблокирует

- Зависит от: T-NNN, … (или «нет»)
- Разблокирует: T-NNN, …

## 5. Критерии готовности и способ проверки

Список проверяемых утверждений со стабильными ID. Для каждого — способ (команда, тест, ручная
проверка страницы). ID не переименовывается между run, чтобы не потерять историю.

- **AC-1**: <наблюдаемый результат>; проверка: <способ>.

## 6. Доказательство результата

Для каждого AC-ID: `check_id`, actor/run, стадия, полный commit, dirty fingerprint, команда и
cwd, exit code, фактическое число сценариев, значимый вывод/артефакт, `trace_ref`, результат и
ограничения. Ссылки на тесты и независимый вердикт ревью.

Успешный run не завершает стадию автоматически; завершённая стадия не означает принятую задачу.

## 7. Пометки

`[ФАКТ: файл:строка]`, `[ДОПУЩЕНИЕ]` — всё, что не подтверждено кодом или спецификацией.

## 8. Требует решения владельца

нет | Q-NN — что именно и почему без этого задача не готова.

## 9. Handoff остановленной стадии

Заполняется при остановке и не удаляет прежние попытки: цель, authorization, actor/run, стадия,
branch/HEAD, dirty fingerprint и diff, сделанное/остаток, решения и отвергнутые гипотезы,
evidence/`trace_ref`, `check_id → число последовательных неуспехов`, причина остановки,
проверяемое условие возобновления, отсутствие активного дубликата и следующий разрешённый шаг.
