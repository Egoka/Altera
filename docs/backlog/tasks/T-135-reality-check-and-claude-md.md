# T-135: Срез реальности и «Что сломано» в CLAUDE.md по фактическому коду

- **Эпик**: E-18 Документация и ADR
- **Статус**: кандидат
- **Параллельность**: да: не меняет продуктовый код
- **Роль-исполнитель**: хранитель документации
- **Участники стадий**: ревью — независимый ревьюер
- **Навыки**: Документация, сверка с кодом
- **Authorization**: прямое поручение владельца 2026-09-26 «заведи задачи на доработку возвращённых 16»; допуск в Todo — по [правилам пополнения](../../multica/backlog-autofill.md)
- **Baseline**: `5af26a3fd661917cb00e8021409ffe4e6352c8c1`
- **Проверяемая revision**: заполняется исполнителем

## 1. Результат

`docs/vision/00-reality-check.md` содержит только проверенные утверждения с `[ФАКТ: файл:строка]` на указанном срезе, а раздел «Что сломано» в `CLAUDE.md` отражает действительное состояние `app`.

Доработка по итогам независимого ревью 2026-09-26: T-111 (ALTE-92, PR #161).

## 2. Источники

- Спецификация задачи T-111 (AC-1)
- Находки ревью: `docs/reports/2026-09-26-in-review-acceptance-report.md` (строка T-111, «Системные проблемы»)

## 3. Границы и явно исключённое

- Входит:
  - Исправить неверные утверждения среза (число enum, i18n, `useSeoMeta`, ссылка `admin.ts:102`) и дать `[ФАКТ]` каждому утверждению.
  - Обновить «Что сломано» в `CLAUDE.md`: убрать решённое (`/login`, `/auth/verify`, `auth.global.ts`, `error.vue`, `useSeoMeta`, sitemap, лимиты) и оставить действительные пробелы со ссылками на задачи T-123…T-134.
- Не входит:
  - Изменения кода.
- Сохраняемый контракт: Формат среза `00-reality-check.md`.

## 4. Зависимости и что разблокирует

- Зависит от: нет
- Разблокирует: приёмку T-111 (ALTE-92)

## 5. Критерии готовности и способ проверки

- **AC-1**: каждое утверждение среза снабжено `[ФАКТ: файл:строка]`, проверяемым на указанном SHA; проверка: выборочная сверка ревьюером не менее 20 утверждений.
- **AC-2**: ни один пункт «Что сломано» в `CLAUDE.md` не опровергается файлами `app`; проверка: сверка ревьюером.

## 6. Доказательство результата

Проверки по критериям §5 с `check_id`, run, revision и выводом; независимый вердикт ревью с маркером
`ALTERA_REVIEW_V1` на финальном head. Отчёт по ритуалу проекта (`docs/reports/`) и receipt
`docs/reports/tasks/T-135.{json,md}`. После приёмки этой задачи возвращённая задача проходит
повторную приёмку в своей карточке.

## 7. Пометки

- [ФАКТ: `docs/vision/00-reality-check.md:521`] «13 enum» при 21 в `schema.prisma` среза.
- [ФАКТ: `docs/vision/00-reality-check.md:620-622`] утверждения об i18n и `useSeoMeta` опровергаются кодом среза.

## 8. Требует решения владельца

нет
