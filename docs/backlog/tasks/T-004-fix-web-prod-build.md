# T-004: Прод-сборка веба под пиновой версией Node

- **Эпик**: E-01 Инженерная база и честный CI
- **Статус**: завершена (PR #34, merge 83f0d0ac; приёмка ALTE-13)
- **Параллельность**: нет: меняет продуктовый код
- **Роль-исполнитель**: разработчик
- **Участники стадий**: архитектура — нет; тестирование — тестировщик; ревью — независимый ревьюер
- **Навыки**: Nuxt 4, Nitro, Node

## 1. Результат
`node web/.output/server/index.mjs` отвечает 200 на SSR-маршрутах под версией Node из `.nvmrc`; причина ошибки импорта `vue` устранена и описана в отчёте.

## 2. Источники
- Журнал: прямого решения нет; задача техническая (границы — спецификации ниже)
- Спецификации: `docs/vision/00-reality-check.md`
- ADR: не применимо

## 3. Границы и явно исключённое
- Входит: Сборка и запуск прод-бандла веба; зависимость `sharp`/`/_ipx/` не решается здесь.
- Не входит: Отказ от `/_ipx/` (T-064).

## 4. Зависимости и что разблокирует
- Зависит от: T-001
- Разблокирует: T-005

## 5. Критерии готовности и способ проверки
1. `pnpm --filter nuxt-app run build && node web/.output/server/index.mjs` — `/` и `/en` отвечают 200 — проверка: curl с кодами.
   Уточнение по факту реализации: буквальный `--filter web` не существует (возвращает `No projects matched`); каноническое имя пакета по `web/package.json` — `nuxt-app`.
2. CI выполняет smoke-запуск прод-бандла — проверка: лог CI.

## 6. Доказательство результата

- **source_fix_sha**: `ec64b105fc7156aba2cc22b7dcb5608d33699c5a`
- **final_head**: `428acc0fa97a03a38702ed5e13b461ec255f58b7`
- **merge_commit**: `83f0d0ac49353b1f1eeb46016a1065f078fa5711` (PR #34 → `app`)
- **baseline**: `843b7d2c16a294adfc73eb7e1059731d6d11b247` (origin/app на старте)
- **план**: `docs/plans/2026-09-15-t004-web-prod-build.md`
- **отчёт**: `docs/reports/2026-09-15-t004-web-prod-build-report.md`

AC-1 подтверждён (Node 24.12.0, macOS arm64):
- `pnpm --filter nuxt-app run build` — exit 0
- `PORT=4319 pnpm --filter nuxt-app run smoke`: `/` → 200, `/en` → 200, exit 0
- Smoke при занятом порте: exit 1, не принимает ответы чужого сервера

AC-2 подтверждён в GitHub CI (HEAD `428acc0f`):
- job `Сборка и типизация веба` — SUCCESS: https://github.com/Egoka/Altera/actions/runs/34900406483/job/104164745256
- шаг `Smoke check production web`: `/` → 200, `/en` → 200

Независимый ревью — READY, blocking findings нет. Merge — 2026-09-14T21:47:56Z.

Ключевые факты по результату реализации:
- Причина дефекта: `@pinia/nuxt@0.11.2` без прямой peer-зависимости `pinia`; Nitro встраивал CJS-бандл, преобразуя `require("vue")` в несуществующий ESM default import.
- Исправление: добавлена прямая зафиксированная зависимость `pinia@3.0.3` в `web/package.json`.
- Буквальный `--filter web` из исходной формулировки AC не существует; каноническое имя — `nuxt-app`.

## 7. Пометки
[ФАКТ: устранён] прод-сборка отвечала 500 на каждом SSR-маршруте под Node 24 до merge `83f0d0ac` (2026-09-14).

## 8. Требует решения владельца
нет.
