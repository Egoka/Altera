# T-017: Миграция: закладки и признак базового авторства первого запуска

- **Эпик**: E-03 Модель данных v2
- **Статус**: завершена (PR #79, merge 7a900e02; приёмка ALTE-53)
- **Параллельность**: нет: меняет продуктовый код
- **Роль-исполнитель**: разработчик
- **Участники стадий**: архитектура — архитектор (`[ДОПУЩЕНИЕ]` техническое отображение); тестирование — тестировщик; ревью — независимый ревьюер
- **Навыки**: Prisma-миграции

## 1. Результат
`Bookmark(userId, articleId)` приватная; базовое авторство первого запуска отображено в данных так, что вывод роли `author` и проверки плана работают без оплаты и бессрочно (по `plan-free.md` п. 6а — базовая выдача `[ДОПУЩЕНИЕ]`).

## 2. Источники
- Журнал: §24.1, §25.1, §25.10, #58
- Спецификации: `docs/spec/70-plans-and-billing/plan-free.md`, `docs/spec/70-plans-and-billing/role-derivation.md`, `docs/spec/30-account/reader/bookmarks.md`, `docs/spec/10-flows/bookmark-and-follow.md`
- ADR: ADR-0041 (частично заменён журналом Г4), ADR-0035 (частично заменён журналом Г5–Г5b)

## 3. Границы и явно исключённое
- Входит: Закладки; техническое отображение базового авторства; `PlanGrant` как существующая сущность модели.
- Не входит: Подписка на автора (F-10); платные периоды (F-01).

## 4. Зависимости и что разблокирует
- Зависит от: T-015
- Разблокирует: T-007, T-020, T-026, T-033, T-037, T-084

## 5. Критерии готовности и способ проверки
1. Служебная роль не может иметь закладок — проверка: тест ограничения или проверки прав.
2. Пользователь с базовым авторством проходит `ensureActiveAuthor` без записи подписки — проверка: тест после T-026.

## 6. Доказательство результата
Миграция, тесты. Отчёт по ритуалу проекта (`docs/reports/`) с разделом «Как проверено».

Подтверждено 2026-09-17: PR #79 смёржен в `app` 2026-09-16T21:29:27Z, merge commit
`7a900e026ece570fa483017a8edc73fb58618863`; CI на PR зелёный целиком. Multica issue ALTE-53
уже в статусе `done`. `docs/backlog/matrix.md` не был синхронизирован после слияния —
исправлено этим коммитом.

## 7. Пометки
`[ДОПУЩЕНИЕ]` Механизм — базовая выдача (`PlanGrant` без срока); правило владельца — только «бессрочно при первом «Создать статью»».

## 8. Требует решения владельца
нет. `[ДОПУЩЕНИЕ]` Механизм — базовая выдача (`PlanGrant` без срока); правило владельца — только «бессрочно при первом «Создать статью»».
