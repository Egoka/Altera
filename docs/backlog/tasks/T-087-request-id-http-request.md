# T-087: Сквозной `requestId` и событие `http.request` с `userId` и снимком роли

- **Эпик**: E-13 Наблюдаемость
- **Статус**: завершена (PR #60, merge 64c47488; приёмка записана в файле, ALTE-32 в Multica — in_review)
- **Параллельность**: нет: меняет продуктовый код
- **Роль-исполнитель**: разработчик
- **Участники стадий**: тестирование — тестировщик; ревью — независимый ревьюер
- **Навыки**: Nuxt server routes, GraphQL Yoga

## 1. Результат
`requestId` генерируется на входе в Nuxt, передаётся в API и возвращается клиенту; `http.request` содержит маршрут по шаблону, статус, длительность, внутренний `userId` и роль; задания хранят `originRequestId`; клиенту `requestId` показывается только при технических сбоях.

## 2. Источники
- Журнал: §28.1, §28.4
- Спецификации: `docs/spec/80-observability/request-tracing.md`, `docs/spec/80-observability/logging-policy.md`
- ADR: ADR-0032

## 3. Границы и явно исключённое
- Входит: Трассировка.
- Не входит: ничего сверх результата; смежное — в задачах эпика

## 4. Зависимости и что разблокирует
- Зависит от: T-086, T-009
- Разблокирует: T-058

## 5. Критерии готовности и способ проверки
1. Ответ 500 содержит `requestId`, ответ `NOT_FOUND` — нет — проверка: тесты.
2. Запись `http.request` содержит роль — проверка: тест логгера.

## 6. Доказательство результата
Тесты. Отчёт по ритуалу проекта (`docs/reports/`) с разделом «Как проверено».

Подтверждено 2026-09-17: PR #60 (`server/t087-request-id`) смёржен в `app` 2026-09-16T08:59:18Z,
merge commit `64c47488bd847467ed2f44cdb4877b4b10591d78`; CI на PR — все проверки успешны, включая
`Формат, линт и тесты` и `test`. Отчёт `docs/reports/2026-09-15-t087-request-id-report.md`
фиксирует AC-1 и AC-2 пройденными и независимое ревью без критичных замечаний. Живой прод
(`https://altera-m4po.onrender.com`) отдаёт `requestId` в `extensions` при техническом `INTERNAL_ERROR`
и не отдаёт его при штатных ошибках — поведение подтверждено напрямую curl-запросом. Задержка
финализации была вызвана независимым инфраструктурным инцидентом `LOG_HASH_SECRET`
(см. ALTE-32), не относящимся к содержанию этой задачи; инцидент закрыт.

## 7. Пометки
Все утверждения — по спецификациям; кодовых фактов, требующих отдельной проверки, не заявлено.

## 8. Требует решения владельца
нет
