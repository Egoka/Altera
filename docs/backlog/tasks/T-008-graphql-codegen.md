# T-008: GraphQL-кодоген из SDL сервера и `codegen --check` в CI

- **Эпик**: E-02 Соединение веба и API
- **Статус**: завершена
- **Параллельность**: нет: меняет продуктовый код
- **Роль-исполнитель**: разработчик
- **Участники стадий**: архитектура — архитектор (структура пакетов); ревью — независимый ревьюер
- **Навыки**: graphql-codegen, TypeScript, GraphQL Yoga

## 1. Результат
Типы операций фронта генерируются из SDL сервера; ручные типы удалены; `codegen --check` в CI падает при расхождении.

## 2. Источники
- Журнал: прямого решения нет; задача техническая (границы — спецификации ниже)
- Спецификации: `docs/vision/02-target-architecture.md`
- ADR: ADR-0012, ADR-0020

## 3. Границы и явно исключённое
- Входит: Настройка кодогена, генерация, CI-шаг.
- Не входит: Переписывание самих операций (T-010).

## 4. Зависимости и что разблокирует
- Зависит от: нет
- Разблокирует: T-010

## 5. Критерии готовности и способ проверки
1. `pnpm codegen --check` зелёный в CI и красный при изменении SDL без перегенерации — проверка: два прогона.
2. В `web/app` нет ручных TypeScript-типов, дублирующих схему — проверка: grep по удалённым файлам.

## 6. Доказательство результата
Лог CI, diff. Отчёт по ритуалу проекта (`docs/reports/`) с разделом «Как проверено».

## 7. Пометки
[ФАКТ: CLAUDE.md] клиента GraphQL в проекте нет, операции — строки в `web/app/query/**`.

## 8. Требует решения владельца
нет. [ФАКТ: CLAUDE.md] клиента GraphQL в проекте нет, операции — строки в `web/app/query/**`.

## 9. Финализация (2026-09-15)

- **source SHA**: `e64a76141de55cfd23cdb35f77e60506aa0e439b`
- **merge commit**: `741470846a5db8b28e8a77a70160a32b6fda4f1c` (в `app`)
- **CI**: passed — https://github.com/Egoka/Altera/actions/runs/34914997468
- **PR**: https://github.com/Egoka/Altera/pull/39
- **Acceptance evidence**: ALTE-16 comment 01a0a28f-c3d4-7cc4-8692-c6ba962116f5
- **Разблокирует**: T-010 (зависимость T-008 снята)
