# T-005: Линт-правила и гигиена: запрет `console.log`, `.output` вне линта, удаление мёртвого кода

- **Эпик**: E-01 Инженерная база и честный CI
- **Статус**: зависит: T-004
- **Параллельность**: да: только линт-конфигурация и удаление мёртвых файлов; конфликтует с любой активной кодовой цепочкой в `web/`
- **Роль-исполнитель**: разработчик
- **Участники стадий**: ревью — независимый ревьюер
- **Навыки**: ESLint, Nuxt 4

## 1. Результат
ESLint запрещает `console.log` вне логгера, игнорирует `.output`; демо-страницы доступны только в dev; мёртвые компоненты, `process.client` и `@neondatabase/serverless` удалены; `migrate deploy` вынесен из `build`.

## 2. Источники
- Журнал: прямого решения нет; задача техническая (границы — спецификации ниже)
- Спецификации: `docs/vision/00-reality-check.md`, `docs/spec/00-registries/routes.md`
- ADR: ADR-0032

## 3. Границы и явно исключённое
- Входит: Линт, скрипты сборки, удаление мёртвого кода по списку `00-reality-check.md` §10.
- Не входит: Логгер (T-086).

## 4. Зависимости и что разблокирует
- Зависит от: T-004
- Разблокирует: нет прямых зависимых задач

## 5. Критерии готовности и способ проверки
1. `pnpm lint` проходит; `console.log` в `server/src` вызывает ошибку линта — проверка: временная вставка.
2. Маршруты `routes.md` #69 (`/fonts-showcase`, `/components-showcase`, `/test-error`) отвечают 404 в прод-сборке — проверка: curl после T-004.
3. `pnpm --filter server build` не запускает `prisma migrate deploy` — проверка: скрипт `build`.

**Фактическое состояние на 2026-09-09** (коммит 8dac22a, отчёт
`docs/reports/2026-09-09-honest-checks-report.md`):

1. **Частично.** `pnpm lint` проходит (код 0): `.output` и `.data` исключены в корневом
   `eslint.config.mjs` **и** в `web/eslint.config.mjs` — второй конфиг перекрывает первый при
   запуске из каталога пакета. Удалена мёртвая фикстура `mockStats` в
   `web/app/pages/types/index.vue`. Не сделано: запрет `console.log` вне логгера.
2. **Не закрыт** и не проверяем в текущем виде — см. §8.
3. **Не закрыт.** `prisma migrate deploy` по-прежнему в скрипте `build` в `server/package.json`.

Остаток задачи — запрет `console.log`, критерий 2 после решения владельца, критерий 3.
Дублирование двух почти одинаковых конфигов ESLint сохраняется и в границы прошлого захода не входило.

## 6. Доказательство результата
Вывод `pnpm lint`, diff удалённых файлов в отчёте. Отчёт по ритуалу проекта (`docs/reports/`) с разделом «Как проверено».

## 7. Пометки
[ФАКТ: docs/reports/2026-09-09-honest-checks-report.md] часть критерия 1 выполнена.
[РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-09-09] Критерий 2 проверяется «curl после T-004», поэтому объявлена
зависимость от T-004: раздел «Зависимости» изменён с «нет» на «T-004», статус — `зависит: T-004`.
Критерий 2 оставлен в этой задаче. Основание: operating-model §4.4 — критерий обязан быть
проверяемым, а до прод-сборки веба (T-004) он не проверяем.
[ФАКТ: docs/vision/00-reality-check.md §10] список мёртвых компонентов веба.

## 8. Требует решения владельца
нет. [ФАКТ: docs/vision/00-reality-check.md §10] список мёртвых компонентов веба.
