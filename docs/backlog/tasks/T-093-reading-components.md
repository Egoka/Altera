# T-093: Компоненты чтения: `ArticleCard`, атомы метаданных, `ProBadge`, `BookmarkButton`, состояния списков, шапка и футер

- **Эпик**: E-14 Дизайн-система и i18n
- **Статус**: зависит: T-092, T-009
- **Параллельность**: нет: меняет продуктовый код
- **Роль-исполнитель**: разработчик
- **Участники стадий**: дизайн — дизайнер; ревью — независимый ревьюер
- **Навыки**: Vue 3, Nuxt 4

## 1. Результат
Собственные компоненты чтения по `06-design-system.md` §4: карточки трёх размеров, атомы метаданных, бейдж грейда, кнопка закладки, экраны состояний; шапка с меню из рубрик базы и футер с реальными ссылками.

## 2. Источники
- Журнал: прямого решения нет; задача техническая (границы — спецификации ниже)
- Спецификации: `docs/vision/06-design-system.md`, `docs/spec/20-public/home.md`
- ADR: ADR-0021

## 3. Границы и явно исключённое
- Входит: Компоненты чтения.
- Не входит: Компоненты приложения (T-094).

## 4. Зависимости и что разблокирует
- Зависит от: T-092, T-009
- Разблокирует: T-054

## 5. Критерии готовности и способ проверки
1. Снимок-тесты компонентов проходят; меню строится из ответа API — проверка: Playwright.

## 6. Доказательство результата
Снимок-тесты, Playwright. Отчёт по ритуалу проекта (`docs/reports/`) с разделом «Как проверено».

## 7. Пометки
Все утверждения — по спецификациям; кодовых фактов, требующих отдельной проверки, не заявлено.

## 8. Требует решения владельца
нет
