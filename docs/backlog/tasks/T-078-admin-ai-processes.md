# T-078: AI-процессы `/admin/ai`: история и статусы, стоимость только агрегатом, `analyst` на чтение

- **Эпик**: E-12 Админка II
- **Статус**: зависит: T-069, T-048
- **Параллельность**: нет: меняет продуктовый код
- **Роль-исполнитель**: разработчик
- **Участники стадий**: дизайн — дизайнер; тестирование — тестировщик; ревью — независимый ревьюер
- **Навыки**: Nuxt 4, FishtVue

## 1. Результат
Раздел со всеми действиями, генерациями и ответами AI по статусам; стоимость доступна только агрегатом; `analyst` и `admin` читают, никто не запускает и не повторяет; `moderator` видит свою зону.

## 2. Источники
- Журнал: #34, #52, #56, §27.3, §27.4
- Спецификации: `docs/spec/40-admin/ai-processes.md`
- ADR: не применимо

## 3. Границы и явно исключённое
- Входит: Раздел.
- Не входит: Настройки провайдеров (F-08, #36).

## 4. Зависимости и что разблокирует
- Зависит от: T-069, T-048
- Разблокирует: нет прямых зависимых задач

## 5. Критерии готовности и способ проверки
1. Все строки состояний `ai-processes.md` воспроизводимы — проверка: Playwright.
2. В ответе карточки нет стоимости отдельной записи — проверка: контрактный тест.

## 6. Доказательство результата
Playwright, тесты. Отчёт по ритуалу проекта (`docs/reports/`) с разделом «Как проверено».

## 7. Пометки
Все утверждения — по спецификациям; кодовых фактов, требующих отдельной проверки, не заявлено.

## 8. Требует решения владельца
нет
