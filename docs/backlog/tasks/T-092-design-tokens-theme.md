# T-092: Ядро дизайн-системы: токены, одна палитра, типографика, схема отступов, тема

- **Эпик**: E-14 Дизайн-система и i18n
- **Статус**: кандидат
- **Параллельность**: нет: меняет продуктовый код (`web/app/assets/css`, компоненты)
- **Роль-исполнитель**: дизайнер
- **Участники стадий**: разработка — разработчик; ревью — независимый ревьюер
- **Навыки**: Tailwind 4, CSS-токены, Nuxt color-mode

## 1. Результат
Одна палитра и включённая типографика в `main.css`, единая схема отступов, тема через один механизм; демо-компоненты и `useTheme` удалены.

## 2. Источники
- Журнал: прямого решения нет; задача техническая (границы — спецификации ниже)
- Спецификации: `docs/vision/06-design-system.md`, `docs/vision/00-reality-check.md`
- ADR: ADR-0021

## 3. Границы и явно исключённое
- Входит: Токены и базовые стили.
- Не входит: Компоненты (T-093, T-094).

## 4. Зависимости и что разблокирует
- Зависит от: нет
- Разблокирует: T-093, T-094

## 5. Критерии готовности и способ проверки
1. В `main.css` нет закомментированной базовой типографики — проверка: grep.
2. Тема переключается одним механизмом — проверка: Playwright.

## 6. Доказательство результата
Playwright, diff. Отчёт по ритуалу проекта (`docs/reports/`) с разделом «Как проверено».

## 7. Пометки
[ФАКТ: CLAUDE.md] базовая типографика в `main.css` закомментирована.

## 8. Требует решения владельца
нет. [ФАКТ: CLAUDE.md] базовая типографика в `main.css` закомментирована.
