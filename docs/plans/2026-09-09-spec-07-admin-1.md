# План: заход 7 — админка I

- **Дата**: 2026-09-09
- **Базовый коммит**: f5d75f9
- **Отчёт**: docs/reports/2026-09-09-spec-07-admin-1-report.md
- **Статус**: завершён (отчёт — docs/reports/2026-09-09-spec-07-admin-1-report.md)

## Вход

- Реестры: `admin-sections.md` #1–7, #11 (#12 «Жалобы» — отложено), `routes.md` #41–47, #51–52,
  `flows.md` #5, #10, #11, #16 (#9 — отложено), `access-matrix.md`, `events-and-logs.md`.
- Заходы 2 (роли, исключения, эскалация), 4 (наблюдение за рейтингом — только чтение), 5a
  (финансовый контур — заход 8a), 6 (кабинет: переписка, замечания к блокам, проверка профиля).
- Журнал: §1–7, §12–16, §20.19, §24.3, §25.2, §25.4, §25.6–7; `docs/guides/admin_query_standards.md`.

## Выход

`docs/spec/40-admin/`: dashboard (#1), categories (#2), tags (#3), articles (#4), review-queue
(#5), users (#6), admins (#7), statistics (#11) — 8 разделов по `_templates/admin-section.md`
(≥ 120 строк, 12 разделов, 6 состояний). `docs/spec/10-flows/`: moderation (#5), appoint-admin
(#10), archive-account (#11), permanent-delete (#16) — 4 сценария по `_templates/flow.md` (≥ 80
строк, Mermaid). `complaints.md` (#12) и `complaint.md` (#9) не создаются — сценарий жалоб
отложен владельцем; перечисляются в отчёте.

Статус файлов — «на утверждении»; строки реестров не меняются, добавляется пометка о созданных
файлах. ADR-0005 п. 3 и ADR-0042 п. 3 (теги создаёт автор; таксономия у `editor`) противоречат
журналу §12 — строки статуса.

## Правила захода

Общий блок `docs/prompts/phase-2-program.md` целиком; дополнительно: минимальные роли — по
дефолтным наборам журнала §1 без иерархии по включению; «удалить» = обратимый архив, «удалить
навсегда» — только `owner` с вводом имени (журнал #1); чтение ПДн аудируется автоматически с
контекстом (журнал #6, §14.4); `analyst` — read-only без задизейбленных кнопок (кнопок нет);
служебные записи изолированы (§25.2); детальная матрица кнопок и полей — здесь (журнал §7).
Отложенные темы (жалобы, рассылки, механика промокодов, письма) не разворачиваются.

## Проверка

Скрипт: размеры и число разделов; 6 обязательных состояний; существование файлов по реестрам;
Mermaid; коды ошибок и событий — из реестров; словарь; свод `[ВОПРОС ВЛАДЕЛЬЦУ]`.
