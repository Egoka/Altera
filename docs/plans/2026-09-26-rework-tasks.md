# План: задачи на доработку 16 задач, возвращённых ревью (2026-09-26)

- **Поручение**: прямой запрос владельца 2026-09-26 — «заведи задачи на доработку возвращённых 16».
- **Источник находок**: [отчёт ревью in_review](../reports/2026-09-26-in-review-acceptance-report.md);
  каждая находка перепроверена по коду `app` при ревью.
- **Baseline**: `app` `5af26a3fd661917cb00e8021409ffe4e6352c8c1` (после гейта блокеров, журнал §31–§42).

## Сверка с решениями гейта блокеров

- §36 п. 3 утвердил механизм T-024, но обходы по `X-Forwarded-For` и алиасам остаются дефектом
  механизма, а не вопросом порогов (пороги — T-116).
- §37 п. 8 (архивирование служебной записи при снятии роли) уже заведено как T-117 — в задачу по
  T-074 не входит.
- §37 п. 16 утвердил T-059 в сделанном объёме; дефект рассылки уведомлений от этого не зависит.
- §35 п. 3 включил офлайн-страницу в срез; открытый редирект остаётся дефектом.
- e2e `22-admin-admins` стабилизирован PR #216 — в задачу по T-074 не входит.

## Состав

Одна задача — один связный результат; сквозной дефект нескольких задач — одна задача.

| Новая | Возвращённые | Суть |
|---|---|---|
| T-123 | T-022 | вход по ссылке: блокировка через `acceptConsent`, 403 из веб-почты, refresh-cookie |
| T-124 | T-023 | обновление сессии на клиенте, CAS ротации |
| T-125 | T-024 (+T-059) | доверенный адрес клиента и счёт алиасов в лимитах |
| T-126 | T-013 | имя аккаунта не из e-mail |
| T-127 | T-030 | тексты и тупик плана в сводке кабинета |
| T-128 | T-037, T-084 | кэш плана при выдаче и отзыве, базовые выдачи в `/admin/grants` |
| T-129 | T-054, T-055, T-057 | ссылки и меню на `/en` |
| T-130 | T-058 | открытый редирект `/offline`, статусы `error.vue` |
| T-131 | T-059 | уведомления сотрудникам об обращениях |
| T-132 | T-070 | иерархия восстановления рубрик, строки состояний |
| T-133 | T-074 | инвариант владельцев под конкуренцией, ошибки мутаций, e-mail владельцев |
| T-134 | T-080 | ПД в списке `/admin/mail`, секретные шаблоны |
| T-135 | T-111 | срез реальности и «Что сломано» в `CLAUDE.md` |

Порядок: файлы задач → `python3 scripts/backlog/sync_status.py --apply` → проверка `git diff` →
PR в `app` обычным merge-коммитом → отчёт. Карточки Multica создаёт очередь по правилам
пополнения (`docs/multica/backlog-autofill.md`); статусы возвращённых карточек не меняются.
