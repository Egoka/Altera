# T-006: проверка кеш-обёртки Redis/noop на текущем `app`

- **Дата**: 2026-09-21
- **Задача**: T-006 / ALTE-103 (`01a0c0ba-22cf-7724-bb90-3805d34c2535`)
- **Ветка / worktree**: `feat/t006-cache-noop` / `.worktrees/t006-cache-verify`
- **Базовый коммит**: `924c682b56a161aa6a724462fe8d6191d9b19a80` (`origin/app`)
- **Исходное дерево**: clean
- **Отчёт**: `docs/reports/2026-09-21-t006-cache-verification-report.md`
- **Статус**: выполняется

## Исходное положение

Код T-006 уже находится в `app`: коммиты `0cac749`, `67faa50`, `723e40f`, `6e1083e`, `41630d7`
дали контракт `Cache`, `NoopCache`, `RedisCache` с инвалидацией по тегам через Lua и
SHA-256-ключ от полного объекта аргументов; отчёт реализации —
`docs/reports/2026-09-15-t006-cache-wrapper-report.md`. Независимая проверка критериев на
принятой ревизии не выполнялась, а после `41630d7` кеш трогали ещё пять коммитов
(`c8781cd`, `70fd464`, `ea1d616`, `33a222d`, `c87317a`) и задача T-024 добавила второй
ioredis-клиент (`server/src/rate-limits/redis-store.ts`). Поэтому заход — проверка критериев
на текущем `app` и закрепление того критерия, который до сих пор держался только ручным grep.

## Что делаем

1. Проверить три критерия задачи на `924c682` с сохранением команд и вывода.
2. Просмотреть все кешируемые резолверы: ключ содержит все аргументы запроса, черновики и
   архив не попадают в общий кеш, админские выборки кеш не используют.
3. Закрепить критерий «в коде нет обхода keyspace» тестом уровня исходников по образцу
   `server/tests/error-contract.test.ts`: ручной grep не защищает от новых Redis-клиентов.
4. Прогнать `format`, `lint`, `test`, `build:ci` и smoke без `REDIS_URL`.
5. Записать отчёт с разделом «Как проверено» и передать receipt.

## Границы

Продуктовое поведение кеша не меняется: новых резолверов, изменений схемы данных и правок
TTL в заходе нет. Единственное изменение кода — новый тест исходного контракта.
