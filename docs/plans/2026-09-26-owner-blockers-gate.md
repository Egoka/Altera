# План: интерактивный гейт блокеров владельца Q-01…Q-13 (2026-09-26)

- **Поручение**: владелец, сессия 2026-09-26 — провести гейт по открытым вопросам
  `docs/backlog/owner-blockers.md` вместе с ним, по одной карточке.
- **Главное правило**: агент не решает ни одного вопроса сам. Нет ответа — нет решения:
  не пишется «предлагаю считать решённым», не вносится `[ДОПУЩЕНИЕ]` вместо ответа.
- **Ветка**: `docs/owner-blockers-gate` (worktree `.worktrees/owner-blockers-gate`) от `origin/app` 753d7cc.

## Порядок карточек (предлагается владельцу, он может изменить)

Q-02 → Q-06 → Q-03 → Q-01 → Q-09 → Q-04 → Q-08 → Q-05 → Q-07 → Q-11 → Q-12 → Q-13.
Q-10 уже снят (2026-09-14).

## Шаги для каждой карточки

1. Контекст простыми словами: суть, что блокирует, последствия вариантов; источники и факт в коде.
2. Для задач, уже сделанных поверх открытого вопроса (T-024, T-058, T-099, T-083, T-059, T-074,
   T-021, T-062, T-089 и другие найденные) — отдельный вопрос: утвердить, доработать, откатить.
3. Вопросы через AskUserQuestion, по 1–4 за раз; крупные карточки делятся.
4. Уточнение неполных ответов; числовые параметры не додумываются.
5. Повтор формулировки решения и подтверждение владельца, затем следующая карточка.

## Фиксация (после подтверждения)

- Журнал: новые тематические разделы §31… с пометкой «внесено по ответам владельца в гейте
  блокеров 2026-09-26»; сводный список не трогается; отложенное — «Отложено».
- `owner-blockers.md`: «закрыт §N» / «частично закрыт» / «отложен».
- Шапки `docs/backlog/tasks/T-*.md`: статусы связанных задач; при конфликте с сессией
  синхронизации бэклога её правки сохраняются. `matrix.md` пересобирается скриптом.
- Статусы в Multica не трогаются, задачи не назначаются.

## После всех карточек

- Список затрагиваемых файлов спецификаций, реестров и ADR показывается владельцу до правки.
- Синхронизация без новых правил и чисел сверх сказанного владельцем; ADR получают строку
  статуса «частично заменено журналом §…».
- Отчёт `docs/reports/2026-09-26-owner-blockers-gate.md` с разделом «Гейт блокеров закрыт».
- PR в `app` обычным merge-коммитом, без обновления ветки без конфликта, без `--no-verify`;
  после слияния ветка удаляется на origin и локально.

## Прогресс

Обновляется по ходу гейта, чтобы при перерыве можно было продолжить.
- Q-02 — закрыт §31 (2026-09-26).
- Q-06 — частично закрыт §32 (категории утверждены; критерии — черновик агента на утверждение; T-048 fake в работу).
- Q-03 — частично закрыт §33 (проход параллельно; временная матрица ADR-0030; числа — архитектор предлагает, владелец утверждает).
- Q-01 — частично закрыт §34 (GlitchTip/Sentry, YandexGPT; T-021/T-062/T-089 утверждены; вход по паролю в запуск; хостинг/S3/почта/домен отложены).
- Q-09 — закрыт §35.
- Q-04 — частично закрыт §36.
- Q-08 — частично закрыт §37.
- Q-05 — закрыт §38.
- Q-07 — закрыт §39.
- Q-11 — частично закрыт §40.
- Q-12 — закрыт §41.
- Q-13 — закрыт §42.
