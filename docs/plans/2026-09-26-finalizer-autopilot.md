# План: агент-финализатор и автопилот «приёмка в Done» (2026-09-26)

- **Поручение**: владелец выбрал вариант (б) из
  [фазы Done после автослияния](../multica/done-finalization.md): «создавай агента-финализатора
  и автопилот».
- **Базовый `app`**: `835691f45667b685badd51f764af6bc6f2010720` (слияние PR #212).
- **Ограничения**: остальные автопилоты остаются на паузе. Новый автопилот создаётся на паузе:
  идёт ручная приёмка 43 задач, двойные запросы ревью не нужны. Статусы карточек не менять,
  описания оркестратора и ревьюера не трогать.

## Шаги

1. Снять `before`: автопилоты и агенты Multica.
2. Агент «Altera — финализатор»: runtime Claude, модель и `custom_args` как у ревьюера, `PATH` с
   Node 24.12.0, навык `altera-project-rules`, concurrency 1, без MCP (пробу делает ревьюер).
3. Автопилот «Altera — приёмка в Done», `run_only`, project Altera: создать без триггера, сразу
   поставить на паузу, затем добавить расписание.
4. Readback и очищенный снимок `docs/multica/snapshots/2026-09-26-finalizer/`.
5. Обновить документ фазы Done, отчёт, PR в `app` обычным merge-коммитом, удалить ветку.
