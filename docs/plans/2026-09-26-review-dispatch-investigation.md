# План: кто запускает независимое ревью и Done после автослияния (2026-09-26)

- **Поручение**: прямой запрос владельца — выяснить, почему 43 задачи Multica висят в `in_review`,
  на что ушли 13 запусков ревьюера за сутки 2026-09-21 и какой автопилот должен запускать ревью;
  предложить исправление (починить существующий автопилот или создать новый).
- **Базовый `app`**: `753d7cce6f2b1a4a74f110212523da13a9588ab4`.
- **Ограничения**: автопилоты остановлены владельцем — не запускать, не включать, не перенастраивать,
  задачи не назначать, статусы в Multica не менять. Параллельно идут ручная приёмка этих 43 задач,
  стабилизация e2e и синхронизация бэклога — их файлы не трогать. Чтение Multica, логов, истории
  запусков и конфигурации разрешено.

## Шаги

1. **Инвентарь автопилотов** (только чтение): `multica autopilot list/get`, инструкции агентов,
   установленный контракт `.git/autonomy-runtime/current/contracts/`, `runtime_bridge.py`,
   `controller.py`, `deploy_evidence.py`, установленный config (без секретов).
2. **История запусков 14–21.09**: `multica agent tasks` ревьюера, оркестратора, релиз-инженера,
   агента приёмки; ledger контроллера (копия), итоги тиков оркестратора.
3. **Цепочки задач** T-022 (ALTE-57), T-023 (ALTE-97), T-054 (ALTE-91), T-089 (ALTE-115): PR,
   автослияние, запуски и комментарии в карточке, receipt в `app`, `controller.py verify`
   (фаза `done`, только чтение, в отдельном worktree и копии config).
4. **Первопричина** — подтвердить кодом, запусками и ответами контроллера (метод
   `systematic-debugging`), а не догадкой.
5. **Решение**: сравнить (а) правило в существующем автопилоте и (б) отдельный автопилот фазы Done;
   дать рекомендацию с готовым текстом инструкции, триггером, защитой от дублей и зацикливания.
6. **Правки в репозитории отдельным PR** (ветка `fix/done-phase-dispatch`):
   - `controller.py`: зависшая запись `running` фазы `done` не должна блокировать задачу навсегда,
     если карточка не в Done (сверка внешнего результата под `dispatch.lock`), с тестом;
   - документ фазы Done и предлагаемая инструкция автопилота в `docs/multica/` — со статусом
     «предложено, ждёт решения владельца»; установленный runtime и Multica не меняются;
   - отчёт в `docs/reports/`.
7. Проверки: `python3 -m unittest` для `scripts/autonomy`, хуки коммита (`format`, `lint`, `test`).
8. PR в `app` обычным merge-коммитом, без обновления ветки при отсутствии конфликта; удалить ветку
   на origin и локально.

## Что не делается без владельца

- Изменение описаний и триггеров автопилотов, создание нового автопилота или агента.
- Переустановка runtime (`install.py`) — новая версия контроллера начнёт действовать только после неё.
- Живой запуск ревьюера или пробы выкладки для проверки — только по запуску владельца.
