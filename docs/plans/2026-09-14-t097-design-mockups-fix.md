# T-097: Исправление макетов по замечаниям ревью

- **Дата**: 2026-09-14
- **Ветка**: docs/adr-0047-plus
- **Задача**: T-097 | docs/backlog/tasks/T-097-design-mockups-launch.md
- **Authorization**: поручение оркестратора, comment 01a0a117-186a-72e3-8495-bcd4e738e1a6
- **Базовый коммит**: a9b3435
- **Исходное дерево**: dirty (CLAUDE.md, docs/guides/README.md, server/env.example; untracked: docker-compose.yml, docs/guides/local-development.md, docs/plans/2026-09-14-t001-*, docs/reports/2026-09-14-t001-*, t003-*)
- **Отчёт**: docs/reports/2026-09-14-t097-design-mockups-fix-report.md (заполняется по завершении)
- **Статус**: выполняется

## Что исправить

Три отклонения, выявленных независимым ревьюером:

### 1. КРИТИЧЕСКОЕ — Главная §1: удалить «Зону 4: лента рубрик»

Файл: `docs/design/mockups/t097-launch-mockups.md`, строка 58.

Удалить из DESKTOP-диаграммы:
```
│ Зона 4: лента рубрик (по одной секции на рубрику)                   │
├─────────────────────────────────────────────────────────────────────┤
```

Основание: home.md §12 «Закрыто Г3 (журнал §20.1, §20.3): секций по рубрикам нет».
Главная содержит ровно три подборки: топ (lede), «Новое», «Популярное».

Сопутствующее исправление: в таблице состояний §1 строка «Загрузка» ссылается на «зон 2–4».
После удаления зоны 4 изменить на «зон 1–3».

### 2. Сводка админки §9: добавить точку останова tablet (md)

Раздел сейчас имеет только DESKTOP и MOBILE.
Добавить явный блок TABLET (md, 768–1023) по образцу §8 (Очередь проверки):
«одна колонка, те же карточки; ручная публикация и сложные действия — десктоп» —
или зафиксировать, что tablet = mobile, если поведение совпадает.

### 3. Таблицы покрытия: добавить пропущенные строки с пометками

Добавить по одной строке на каждое пропущенное состояние:

| Таблица | Строка для добавления |
|---|---|
| Главная | Ограничение плана (н/п — главная публична для чтения) |
| Лента рубрики | Ограничение плана (н/п — чтение не лимитируется); Paywall (зарезервировано) |
| Кабинет | Не найдено / удалено (н/п — адрес фиксирован); Paywall (зарезервировано) |
| Редактор-обвязка | Paywall (зарезервировано) |
| Сводка админки | Нет прав на часть действий (н/п — действий нет); Лимит / превышение (н/п — не лимитируется) |

Также обновить итоговую таблицу «Итого по покрытию» в соответствии с новыми числами строк.

## Шаги

1. Сохранить этот план (коммит).
2. Исправить `docs/design/mockups/t097-launch-mockups.md` (три пункта).
3. Прогнать `pnpm format:fix && pnpm lint && pnpm test` — все exit 0.
4. Зафиксировать исправленный артефакт коммитом.
5. Написать отчёт.
6. Зафиксировать отчёт.
7. Передать на повторное независимое ревью.
