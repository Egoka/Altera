# Программа второй фазы: поштучные спецификации по заходам

Вторая фаза проектирования Altera даёт не обзоры, а спецификации: каждая страница, кабинет
каждой роли, каждый раздел админки, каждый маршрут пользователя, каждая роль, каждый компонент
рейтинга, каждый план — отдельным файлом в `docs/spec/` по шаблону из `docs/spec/_templates/`.

Первая фаза (2026-09-05) провалила именно это: промт назвал главным результатом роадмап, задал
«один файл на контур» и трижды надавил на сокращение — в итоге карта маршрутов уместилась в 23
строки, админка в 18. Программа ниже устроена так, чтобы это не повторилось: сначала реестры,
потом по файлу на строку, без оценок и без сокращений.

Каждый заход — отдельная сессия Fable по отдельному промту `docs/prompts/fable-spec-NN-<slug>.md`,
собранному из `fable-spec-NN-template.md`. Заход заканчивается отчётом и гейтом владельца там,
где он указан. Ориентир ёмкости захода — 10–16 качественных файлов; больше — делить.

---

## Заходы

| Заход | Вход | Выход (файлы в `docs/spec/`) | Гейт владельца |
|---|---|---|---|
| **0 — перебазирование** (`fable-rebase-2026-09-06.md`) | ответы на вопросы захода | ADR-0034…0043 (+условные), ревизии `docs/vision/`, роадмап ревизии 2, плашки | **Г0**: ADR и порядок этапов утверждены |
| **1 — реестры** (`fable-spec-01-registries.md`) | ADR ревизии 2, `06-design-system.md` §5 ревизии 2 | `00-registries/{routes,pages,admin-sections,access-matrix,events-and-logs,plans,flows,roles}.md`, `60-ranking/README.md` (таблица компонентов) | **Г1**: каждый реестр утверждён построчно; владелец вычёркивает и добавляет строки. Без Г1 заходы 2–9 не начинаются |
| **2 — роли и доступ** | `roles.md`, `access-matrix.md` | `50-access/roles/{guest,reader,author,editor,moderator,analyst,admin,owner}.md` + политики `visibility`, `permission-checks`, `session-lifecycle`, `rate-limits`, `escalation-and-demotion` — 13 | Г2 (лёгкий): матрица подтверждена |
| **3 — публичные страницы** | `routes.md`, `pages.md`, заход 2 | `20-public/`: home, section-feed, tag-feed, article, author, authors-index, sections-index, tags-index, login, verify, pricing, not-found, gone, error, offline — 15 | — |
| **4 — выдача и рейтинг** | ADR-0034/0038/0039/0040, `60-ranking/README.md` | `60-ranking/`: article-score, author-score, views-counting, ai-review, pro-boost, freshness-decay, topic-relevance, home-sections, feed-principles, anti-fraud, explainability — 11; `10-flows/moderation.md` | **Г4**: формула и секции главной утверждены — без них ленты дальше не проектируются |
| **5a — планы и биллинг** | `plans.md`, ADR-0035, заход 2 | `70-plans-and-billing/`: plan-free, plan-standard, plan-pro, subscription-lifecycle, role-derivation, payment-provider, receipts-54fz, refunds, grants-and-promo — 9; `10-flows/pay-and-upgrade.md`, `10-flows/subscription-expires.md` | **Г5**: цены-гипотезы |
| **5b — бизнес-модель и остальные публичные** | заход 5a | `90-business-model/`: overview, unit-economics, pricing-hypotheses, funnel, revenue-streams, costs, metrics, risks, legal-152-54 — 9; `20-public/`: search, about, contact, legal-terms, legal-privacy, legal-content-rules, legal-license, legal-paid-services — 8 | Г5 (продолжение) |
| **6 — кабинет** | `pages.md` (account), `flows.md`, заходы 2, 4, 5a | `30-account/reader/`: profile-edit, bookmarks, sessions, subscription, email-change, delete-account — 6; `30-account/author/`: articles, article-new, article-edit (обвязка редактора, без блоков), article-stats, payment, ai-review-view — 6; `10-flows/`: register-and-login, become-author, write-and-publish — 3 | — |
| **7 — админка I** | `admin-sections.md`, заходы 2, 4 | `40-admin/`: dashboard, categories, tags, articles, review-queue, users, admins, statistics, complaints — 9; `10-flows/`: appoint-admin, complaint — 2 | Г7 (лёгкий) |
| **8a — админка II** | `admin-sections.md`, заходы 4, 5a | `40-admin/`: subscriptions, payments-and-refunds, ai-reviews, ranking-config, audit-log, media-library, legal-texts, system-settings — 8 | — |
| **8b — наблюдаемость** | `events-and-logs.md`, ADR-0032, все предыдущие заходы | `80-observability/`: logging-policy, error-dictionary, log-event-registry, request-tracing, health-and-alerts, error-collector, audit-vs-logs, retention-and-pd — 8; закрытие реестра событий | — |
| **9 — медиа и приёмка** | ADR-0008/0030, все реестры | `85-media-and-binary/`: storage-layout, upload-pipeline, image-variants, avatars, article-covers, exports, receipts-and-documents, retention-and-orphans, backups, access-and-signed-urls — 10; сверка всех реестров с файлами; `docs/spec/OPEN-QUESTIONS.md` — свод всех `[ВОПРОС ВЛАДЕЛЬЦУ]`; статусы | **Г9**: приёмка фазы |

Состав файлов в таблице — ориентир из проектирования программы; **истина — утверждённые
реестры после Г1**. Заход берёт строки реестра, а не список из этой таблицы.

Порядок обусловлен зависимостями: роли → страницы (страница ссылается на матрицу); рейтинг →
кабинет и админка (статистика автора и раздел рейтинга ссылаются на формулу); планы → кабинет
(страница оплаты, состояние «истёкшая подписка»); реестр событий заполняется страницами по ходу
и закрывается в 8b.

---

## Как владелец проходит гейт

- **Г1** — открывает каждый реестр, правит строки прямо в файле (вычёркивает с пометкой
  «отменено: причина», добавляет, переименовывает), меняет статус строк на «утверждён», делает
  коммит. Это единственный гейт, требующий работы построчно; остальные — «да / поправить».
- **Г4, Г5** — читает два-три ключевых файла (`article-score.md`, `home-sections.md`;
  `pricing-hypotheses.md`, `plan-*.md`) и отвечает на свод вопросов из отчёта захода.
- **Г9** — принимает свод `OPEN-QUESTIONS.md` и статусы; после этого фаза 2 закрыта и можно
  разбивать этапы `docs/issues/` на задачи.

---

## Общий блок правил

Вставляется в каждый промт заходов 1–9 дословно, под заголовком «Правила захода».

```markdown
## Правила захода

1. **Запрещено**: оценки в человеко-днях, неделях, «сложности»; разделы «что не делаем»,
   «YAGNI», «сценарий деградации»; код, SQL, миграции, GraphQL SDL (имена запросов и полей в
   таблицах — можно); задачи и чек-листы исполнения; пересказ ADR — только ссылка `ADR-NNNN`.
2. **Одна сущность — один файл.** Страница, раздел, роль, flow, компонент рейтинга, план,
   политика — каждый отдельно. Объединять «похожие» запрещено; общее выносится в политику и
   цитируется ссылкой.
3. **Критерий полноты — реестр.** Заход завершён, когда каждая порученная строка имеет файл со
   статусом «на утверждении» и реестр обновлён. Строка без файла перечисляется в отчёте как
   недоделка с причиной. Новая строка, возникшая по ходу, добавляется в реестр в статусе
   «черновик» и попадает в отчёт.
4. **Минимальный объём и полнота шаблона**: страница и раздел админки — не короче 120 строк,
   все разделы шаблона заполнены; роль — 80; flow — 80 плюс Mermaid-схема; компонент
   рейтинга — 100; политика — 60. Пустой раздел заполняется одним предложением-обоснованием
   («не применимо, потому что …»), а не пропускается. Таблица с одной строкой — сигнал
   недоработки; обязательные строки состояний удалять нельзя.
5. **Пометки**: `[ФАКТ: файл:строка]` — утверждение о коде; `[ДОПУЩЕНИЕ]` — всё непроверенное
   (значения по умолчанию, лимиты, тексты, цены); `[ВОПРОС ВЛАДЕЛЬЦУ]` — только с последствием
   («если да — …, если нет — …»). Файл с числами и без единого `[ДОПУЩЕНИЕ]` — подозрителен.
6. **Стоп-правило**: если для раздела нужно решение владельца, раздел заполняется вариантом по
   умолчанию с `[ДОПУЩЕНИЕ]` и вопросом в последнем разделе файла. Заход не останавливается и
   не задаёт вопросы посреди работы; все вопросы собираются в отчёт одним списком с
   последствиями.
7. **Словарь** — только `docs/vision/01-product.md` ревизии 2. Слова «контур», «витрина»,
   «отбор», «заказ», «правило-арбитр», «поддержка автора» вне исторического контекста
   запрещены.
8. **Не выдумывать состояние кода.** Спецификация описывает целевое поведение; про
   существующее — только со ссылкой на `docs/vision/00-reality-check.md` или `файл:строка`.
9. **Состояния и ошибки обязательны**: у каждой страницы — все строки состояний из шаблона; у
   каждого действия — код ошибки из ADR-0032. Новый код — только через
   `80-observability/error-dictionary.md`, не по месту.
10. **Ритуал проекта**: до первого файла — план в `docs/plans/ГГГГ-ММ-ДД-spec-NN-<slug>.md` с
    базовым коммитом; после — отчёт в `docs/reports/ГГГГ-ММ-ДД-spec-NN-<slug>-report.md` с
    разделом «Как проверено»: скрипт сверки реестра с файлами, проверка ссылок, подсчёт строк
    по файлам, свод `[ВОПРОС ВЛАДЕЛЬЦУ]`. Коммиты — conventional, в `docs/platform-design`,
    без push. Если pre-commit хук мешает (нет тестов), `--no-verify` с указанием причины в
    отчёте.
11. **Ссылки вместо копий**: на ADR — по номеру; на другие спецификации — относительным путём;
    на компоненты — `docs/vision/06-design-system.md` §4; на коды событий —
    `00-registries/events-and-logs.md`.
```

---

## Как собрать промт захода

1. Скопировать `docs/prompts/fable-spec-NN-template.md` в `docs/prompts/fable-spec-NN-<slug>.md`.
2. Заполнить плейсхолдеры: номер и имя захода, контур и папка, список строк реестра (номера и
   имена — из утверждённого реестра), шаблон из `_templates/`, обязательное чтение, гейт.
3. Вставить общий блок правил без изменений.
4. Запустить сессию Fable с этим промтом первым сообщением.
5. После отчёта захода: пройти гейт, если он есть; обновить статусы строк; собрать следующий
   промт.

Промт захода 1 уже собран: `docs/prompts/fable-spec-01-registries.md`.
