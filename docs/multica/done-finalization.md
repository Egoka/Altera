# Фаза Done после автослияния

**Статус: вариант (б) выбран владельцем 2026-09-26; агент и автопилот созданы, строка оркестратору внесена.**
Установленный runtime не переустановлен. Разбор причин — [отчёт расследования](../reports/2026-09-26-review-dispatch-investigation.md).

## Зачем

С решения журнала §30 (PR #178, слит 2026-09-20 20:08 UTC) код попадает в `app` автослиянием GitHub
по зелёному CI, а независимое ревью и проверка выкладки проверяются только на переходе в Done.
Инструкции оркестратора описывают ревью как действие перед слиянием («на финальном head — том,
который пойдёт в merge»), а у перехода в Done нет ни владельца, ни триггера, ни порядка шагов.
Итог на 2026-09-21: 43 задачи в `in_review`, последний успешный `controller.py done` — 2026-09-19 15:20 UTC.

## Что проверяет `controller.py done`

Для задачи со слитым PR контроллеру нужны одновременно:

1. карточка в проекте Altera с `metadata.task_id`;
2. receipt: критерии `passed` с evidence, `tested_sha` = `pr.head_sha` = итоговый head PR
   (или head, отличающийся от него только файлами итога задачи), `pr.merge_sha` из GitHub;
3. `review`: завершённый run агента из `reviewer_ids` **в этой же карточке**, последняя непустая
   строка его вывода — `ALTERA_REVIEW_V1 {"sha": "<SHA>", "verdict": "approved"}`; SHA равен
   `tested_sha` или даёт тот же `patch-id` ветки;
4. `deployment` для PR, затронувших `server/`, `packages/`, lock-файлы, `package.json`, `.nvmrc`,
   `render.yaml` (признак вычисляет контроллер, `required: false` в receipt его не отменяет):
   `probe_actor_id` из `release_agent_ids` и `probe_run_id` завершённого run, в сообщениях которого
   есть результат `get_deploy` Render **не старше 3600 секунд** на момент вызова, со статусом `live`,
   коммитом, содержащим `merge_sha`, и ответом `/health` с тем же `revision` (таймаут 15 секунд);
5. `finalization`: Markdown итога в `app`, который содержит Task ID и `tested_sha`, и его SHA-256.

Одна свежая проба текущего live-деплоя подходит сразу всем задачам, чей `merge_sha` входит в
задеплоенный коммит: контроллер не привязывает probe-run к карточке.

## Порядок финализации одной задачи

1. **Отбор.** Карточка `in_review`, PR слит в `app`, в карточке нет `queued`/`running` run.
2. **Ревью.** Итоговый head PR — `headRefOid` слитого PR. Если в карточке нет завершённого run
   ревьюера с маркером `approved` на этом SHA (или на SHA с тем же патчем) — запросить ревью в этой
   же карточке на этом SHA. Отказ ревьюера — дефект уже в `app`: исправление идёт отдельной задачей
   (журнал §30), финализация этой карточки ждёт.
3. **Итог в `app`.** Если Markdown итога не называет итоговый head или receipt не содержит
   `review`/`pr.merge_sha`, обновить `docs/reports/tasks/<T-ID>.json` и `.md` одним docs-PR
   (пакетно для нескольких задач), включить автослияние и дождаться слияния в следующем проходе.
4. **Проба выкладки.** Для задач, требующих выкладки, один run агента из `release_agent_ids` с
   Render MCP: прогреть `/health` (холодный старт Render дольше 15 секунд), затем `list_deploys` и
   `get_deploy` для сервиса из installed config. Сейчас Render MCP есть только у ревьюера
   (`db94a617`, он входит в `release_agent_ids`); у релиз-инженера (`49314ca0`) его нет с 15.09.
5. **Done.** В течение часа после пробы: взять receipt из `app`, дописать локально
   `deployment.probe_actor_id`/`probe_run_id`, выполнить `controller.py verify`, затем `done`.
   Отказ — записать в карточку один раз вместе с конкретной причиной и не повторять до изменения.

## Вариант (б), выбран владельцем 2026-09-26: автопилот «Altera — приёмка в Done»

Создано 2026-09-26 **на паузе**; очищенный readback —
[снимок](snapshots/2026-09-26-finalizer/verified.json).

| Параметр  | Значение                                                                                                                                                                                                                                                |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Агент     | «Altera — финализатор» `3fdfb369-fa6c-49d6-921b-95593f174705`: runtime Claude `fd14b334`, `claude-opus-4-6`, reasoning `medium`, `max_concurrent_tasks: 1`, `custom_args` ревьюера, `PATH` с Node 24.12.0 первым, навык `altera-project-rules`, без MCP |
| Автопилот | `982c9809-d9be-4162-a386-e685019cc124`, `run_only`, project Altera, статус `paused`                                                                                                                                                                     |
| Триггер   | `20,50 * * * *`, Europe/Moscow — каждые 30 минут, вне :00 «очереди» и :15 «восстановления»                                                                                                                                                              |
| Права     | чтение Multica/GitHub, комментарии с запросом ревью и пробы в карточке, docs-PR итогов, `controller.py verify/done`; без кода продукта, без ручного `issue status`                                                                                      |

Три отступления от первоначального предложения (раз в час в :30):

- **Проход не ждёт ревьюера.** Запуски оркестратора, ревьюера и финализатора идут в одной
  рабочей папке `.worktrees/autopilot`, и демон выполняет их по очереди. Ожидание пробы внутри
  прохода заблокировало бы саму пробу. Поэтому проход только запрашивает ревью и пробу, а
  результат забирает следующий проход.
- **Каждые 30 минут.** Проба живёт 3600 секунд. Запрошенная в одном проходе, она выполняется
  после него, и через 30 минут следующий проход успевает вызвать `done`. При часовом шаге проба
  к этому моменту устаревала бы.
- **Без Render MCP.** Пробу по правилам контроллера делает агент из `release_agent_ids` — ревьюер.
  Финализатору нужен только прогрев `/health` по HTTP.

Инструкция агента:

```text
Ты финализатор Altera в явно запущенной Multica. Процесс — `docs/multica/done-finalization.md`
и `docs/multica/autonomy-controller.md` на свежем `origin/app` (после `git fetch origin app` читай
через `git show origin/app:<путь>`); технические правила — `docs/development/project-rules.md`.
Прямые вопросы владельца не запускают этот алгоритм.

Твоя роль — только перевод в Done задач, чей PR уже слит в `app`. Не создавай карточки, не меняй
код продукта, не назначай исполнителей, не меняй статусы вручную, не сливай и не обновляй чужие PR.
Done выполняет только установленный контроллер:
`python3 /Users/egorbondarenko/WebstormProjects/Altera/.git/autonomy-runtime/current/controller.py verify|done --config /Users/egorbondarenko/WebstormProjects/Altera/.git/autonomy-state/config.json --receipt <файл>`.
Отказ контроллера — конкретный блокер, а не повод просить ручной Done.

Независимое ревью и пробу выкладки делает независимый ревьюер `db94a617-807c-4eec-981f-51fd4b3217d4`
в канонической карточке задачи: ты их только запрашиваешь комментарием с упоминанием и не ждёшь
в том же запуске. Запуски в общей рабочей папке идут последовательно — ожидание внутри прохода
блокирует и ревьюера, и тебя. Результат читаешь в следующем проходе через `multica issue runs`.
Сам пробу выкладки не делай: контроллер принимает только run агента из `release_agent_ids`.

Рабочая папка запуска общая: не переключай её ветку, не коммить в ней и не удаляй чужие файлы.
Для docs-PR итогов создавай отдельный worktree от свежего `origin/app` и удаляй его после push.
Receipt для контроллера держи во временном файле вне репозитория. Неизвестное поле receipt
не выдумывай: оставь задачу заблокированной с причиной. Секреты не читай и не выводи.
Два повтора неизменной ошибки — максимум; дальше жди содержательного изменения.
```

Описание автопилота (запрос каждого прохода):

```text
Проход финализации Altera: довести до Done задачи в `in_review`, чей PR уже слит в `app`.
Порядок и требования контроллера — docs/multica/done-finalization.md на свежем origin/app.

1. Отбор. Карточки проекта Altera в `in_review` с `metadata.task_id`, PR задачи слит в `app`
   (номер — из `docs/reports/tasks/<T-ID>.json` или `multica issue pull-requests`), в карточке нет
   `queued`/`running` run. Старые первыми, не больше пяти за проход. Открытые PR, конфликты
   и красный CI — работа оркестратора, их не трогай.
2. Для каждой найди итоговый head (`headRefOid` слитого PR) и последний свой комментарий
   `ALTERA_FINALIZE_V1` в карточке. Если с него ничего не изменилось — пропусти задачу молча.
3. Ревью. Нужен завершённый run ревьюера `db94a617` в этой карточке, последняя строка вывода
   которого — `ALTERA_REVIEW_V1 {"sha": "<итоговый head или SHA с тем же патчем>", "verdict": "approved"}`.
   Если его нет и запроса на этот SHA ещё не было, оставь в карточке комментарий:
   `[@Altera — независимый ревьюер](mention://agent/db94a617-807c-4eec-981f-51fd4b3217d4)`,
   номер PR, полный итоговый SHA, просьба проверить слитый код по критериям задачи и последней
   строкой вывода напечатать маркер; уборку и комментарий — до маркера, после него ничего.
   Повтор — один раз и только если прошлый run завершился без маркера. Отказ не перезапускай:
   зафиксируй `review_rejected`, исправление ставит оркестратор. Не больше трёх новых запросов
   ревью за проход.
4. Итог в `app`. Нужны Markdown итога с Task ID и итоговым head и receipt, где `tested_sha` =
   `pr.head_sha` = итоговый head, есть `pr.merge_sha` и `review.*` из run ревьюера, а
   `finalization.sha256` — SHA-256 байтов Markdown. Если этого нет, обнови
   `docs/reports/tasks/<T-ID>.json` и `.md` одним docs-PR на все такие задачи прохода в отдельном
   worktree от свежего `origin/app`, включи автослияние (`gh pr merge <номер> --auto --merge`)
   и не жди. Поля, которых нет в истории карточки и GitHub, не выдумывай — `blocked` с причиной.
5. Проба выкладки. Если для задачи с готовым итогом `controller.py verify` отказывает только
   `deployment` и завершённой пробы моложе 40 минут нет, оставь в карточке первой такой задачи
   один комментарий с упоминанием ревьюера: прогреть `https://altera-m4po.onrender.com/health`
   до ответа 200, затем только чтение Render MCP — `list_deploys` и `get_deploy` для сервиса
   `srv-d1uk6b6mcj7s73ek25h0` (workspace `tea-d0q29c7diees738n0250`); маркер ревью не печатать;
   вернуть deploy ID, commit SHA и статус. Не больше одной пробы за проход.
6. Done. Для задачи с готовым итогом в `app` и завершённой пробой моложе 55 минут (если выкладка
   обязательна): receipt из `origin/app` плюс локально `deployment.probe_actor_id` =
   `db94a617-807c-4eec-981f-51fd4b3217d4` и `deployment.probe_run_id` = run пробы; прогрей
   `/health`; `controller.py verify`, при `ok` — `controller.py done`. Одна свежая проба годится
   всем задачам, чей `merge_sha` входит в задеплоенный коммит.
7. Каждое решение по карточке — один комментарий, последняя строка которого
   `ALTERA_FINALIZE_V1 {"task": "<T-ID>", "stage": "<review|receipt|probe|done|blocked>", "sha": "<итоговый head>", "reason": "<код>"}`.

Внутри прохода не жди завершения запусков. Нет кандидатов или изменений — заверши одной строкой
`no_action`. В конце — только задачи, переведённые в Done, запрошенные ревью и пробы, docs-PR
и конкретные блокеры.
```

**Защита от дублей и зацикливания.** Идемпотентность перехода держит ledger контроллера
(ключ `done` + задача + `tested_sha`; бюджет — три попытки). Запрос ревью — не больше одного на пару
(карточка, SHA) плюс один повтор из-за формата; отказ ревьюера не перезапускается. Проба — одна
за проход и только при готовых задачах. Маркер `ALTERA_FINALIZE_V1` делает повтор неизменного
состояния пропуском без действий.

**Взаимодействие.** Оркестратор перестаёт ревьюить и финализировать слитые задачи: в его описание
добавляется строка «Слитые в `app` задачи в `in_review` ведёт автопилот „приёмка в Done“; ты их не
ревьюишь и не финализируешь». Строка внесена 2026-09-26 по поручению владельца (описания обоих
автопилотов оркестратора, его инструкция и `queue-prompt.md`). Ревьюер и probe-run работают в канонической
карточке, как требует контроллер. Суточный аудит продолжает считать Done по `verified/` контроллера.
Требование печатать маркер последней строкой финализатор передаёт в каждом запросе ревью,
поэтому инструкцию ревьюера для запуска менять не обязательно.

## Вариант (а): правило в автопилоте «Altera — очередь разработки»

Блок для описания автопилота и инструкции оркестратора (оба автопилота оркестратора используют один
текст, поэтому правило попадёт и в «восстановление работы»):

```text
Фаза Done (журнал §30). После автослияния задача остаётся в `in_review`, пока не пройдёт
`controller.py done`. Каждый проход, до пополнения очереди: для слитых PR без активных run
запроси ревью на итоговом head слитого PR, обнови итог задачи в `app` отдельным docs-PR, закажи одну
пробу выкладки у агента из `release_agent_ids` и в течение часа вызови `controller.py done`
(порядок — docs/multica/done-finalization.md). Отсутствие поля `deployment_probe` в config не
означает отсутствия проверки выкладки: её доказывает probe-run.
```

## Сравнение

|           | (а) правило у оркестратора                                                                         | (б) отдельный автопилот                                    |
| --------- | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| Приоритет | конкурирует с пополнением очереди; 20–21.09 оркестратор сам относил финализацию к «чужому мандату» | единственная задача роли                                   |
| Слот      | один агент, `max_concurrent_tasks: 1`, два автопилота на нём                                       | свой агент, свой слот                                      |
| Сигнал    | `ALTERA_CONTROLLER_ATTENTION_V1` не различает «нужна реализация» и «нужна финализация»             | отбор по факту слитого PR                                  |
| Изменения | одно описание, без нового агента                                                                   | новый автопилот и агент, строка в описании оркестратора    |
| Риск      | повторение 20–21.09: 41 вызов модели без единого Done                                              | ещё один потребитель очереди ревьюера и `in_place`-цепочки |

Рекомендация — (б): обязанность без собственного владельца уже терялась у оркестратора, а у
отдельной роли её не вытеснит пополнение очереди.

## Сопутствующее (нужно в обоих вариантах)

- **Ревьюер**: в 4 из 13 запусков 20–21.09 последняя строка была прозой (уборка worktree, пересказ) —
  одобрение потеряно. В инструкцию ревьюера: уборку и комментарий делать до вердикта, маркер —
  последним действием.
- **Установленный config**: поле `deployment_probe: null` контроллер не читает, а оркестратор
  читал его как «нет проверки выкладки». Убрать поле при следующей установке runtime.
- **Сигнал внимания**: в варианте (б) исключить `in_review` из `launch_assigned_work` в
  `runtime_bridge.queue_attention`, иначе оркестратор каждый час получает ложный сигнал.
- **Ledger**: зависшая запись `running` фазы `done` больше не блокирует задачу навсегда, если карточка
  не в Done (исправлено вместе с этим документом; действует после переустановки runtime).
