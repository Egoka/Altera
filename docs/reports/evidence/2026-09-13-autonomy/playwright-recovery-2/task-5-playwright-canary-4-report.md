# Playwright canary4: consumed approved recovery

One actual invocation under the user's explicit yes to the prepared snapshot retry.
Attempt 880d525a2c7c47178f9c91303bb0a2b3; exact command and sanitized environment are in the durable invocation claim.
The ten release sources, including the one-space formatting correction, were verified before and unchanged after the run. Image97bd and candidate seccomp e75 retained. No build/model/auth/native/Multica operation.

Outer harness exit1; runner exit1; MCP close code0 with no signal. Control topology accepted=false; acceptance topology not started. Failure count3 to4 and stop; no receipt and no further retry authorization.

Saved protocol id3 returned linked home snapshot containing ALTERA_PLAYWRIGHT_CANARY and Continue. Id4 returned linked form snapshot containing Name textbox f1e4 and Submit button f1e5. Id5 (browser_type) returned a successful empty text response, not a snapshot. The canary replaced page observation with this empty response and failed while looking up Submit. Thus linked-file resolution and Continue progressed; form submission, screenshot and later negative/process checks were not reached. A successful type response does not independently prove the DOM value without a later observation.

Commands15-17 removed exactly the web, runner and sink containers under prefix altera-pw-61749a5e21bf411cb32a7ee685ad7c96. Each exit0/exact name/empty stderr. Command18 showed that exact network with Containers {}; command19 removed it with exit0/exact name/empty stderr. No separate post-removal absence probe was performed. Host listener shutdown is in the harness finally path. No global cleanup claim.

Offline action/observation correction is delegated separately. The approved snapshot reader itself is retained; successful actions must not be assumed to return a complete snapshot. Full browser acceptance remains failed.

Evidence archive: docs/reports/evidence/2026-09-13-autonomy/playwright-recovery-2/manifest.json. Explicit allowlist only; no cache or auth directory copied.
