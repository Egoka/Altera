### Spec Compliance

- ❌ Issues found: original acceptance group 8 and retained-counter requirement are violated. A missing counter in otherwise retained v3 state is silently recreated at zero, reopening a check whose retained stop records two failures (`scripts/agent-loop/gate.py:227`, `scripts/agent-loop/gate.py:615`). One focused disposable probe confirmed this; details below.
- Reviewed exactly base `1dad64f9fa0af196e32dd2f9f5676740c8d13797` → head `e6f868472708e460877c631d90616a061d5698dd`, using the supplied immutable package once. All five assigned paths have corresponding hunks; no product source, owner journal, product ADR, runtime/model/schedule/squad/resource or Task3 changes occur in this package.
- ✅ Original AC1: explicit contract/implementation/verification kinds, implementation input/output, ordered fresh prerequisites and a real Git/subprocess fixture are present (`scripts/agent-loop/gate.py:375`, `scripts/agent-loop/gate.py:453`, `scripts/agent-loop/gate.py:500`, `scripts/agent-loop/test_gate.py:540`).
- ✅ Original AC2: declared first-defect return retains parent/history/counters, supersedes effective downstream stages and requires fresh implementation/test/review (`scripts/agent-loop/gate.py:783`, `scripts/agent-loop/test_gate.py:553`).
- ✅ Original AC3 for intact state: a second review failure across iterations blocks ordinary record/start/return; the state-corruption bypass below prevents unconditional acceptance of counter preservation (`scripts/agent-loop/gate.py:772`, `scripts/agent-loop/gate.py:797`, `scripts/agent-loop/test_gate.py:570`).
- ✅ Original AC4: pinned verification and historical proofs are rechecked at record/report/Stop; a newly passing review cannot bless drifted source (`scripts/agent-loop/gate.py:500`, `scripts/agent-loop/gate.py:564`, `scripts/agent-loop/test_gate.py:587`).
- ✅ Original AC5: artifact binding records actual from/to snapshots and retained proof references without rewriting original evidence SHA; final artifacts must be committed before parent release (`scripts/agent-loop/gate.py:635`, `scripts/agent-loop/gate.py:831`, `scripts/agent-loop/test_gate.py:540`).
- ✅ Original AC6: exact own-artifact paths, ancestry, unchanged dirty fingerprint, frozen contract/proof hashes, owner and unique events are checked. Source/config/test/docs changes, dirty source, archive symlink/rename and unrelated HEAD have negative fixtures (`scripts/agent-loop/gate.py:433`, `scripts/agent-loop/gate.py:635`, `scripts/agent-loop/test_gate.py:597`, `scripts/agent-loop/test_gate.py:756`, `scripts/agent-loop/test_gate.py:804`).
- ✅ Original AC7: parent survives return, intermediate stages and pending final artifacts; competing-parent fixtures cover return and finalization (`scripts/agent-loop/gate.py:591`, `scripts/agent-loop/gate.py:783`, `scripts/agent-loop/gate.py:861`, `scripts/agent-loop/test_gate.py:651`, `scripts/agent-loop/test_gate.py:787`). Existing shared-worktree regression is preserved.
- ❌ Original AC8: immutable stage archives and unsupported-version rejection are implemented, but corrupt-state rejection is incomplete and can erase effective failure history (`scripts/agent-loop/gate.py:212`, `scripts/agent-loop/gate.py:433`, `scripts/agent-loop/test_gate.py:614`, `scripts/agent-loop/test_gate.py:809`).
- ✅ Addendum AC1, structurally: publication → independent docs verification → required release → same-parent final publication/check is supported, with distinct source and metadata relations (`scripts/agent-loop/gate.py:529`, `scripts/agent-loop/gate.py:549`, `scripts/agent-loop/test_gate.py:651`). Real release/environment truth remains external, as explicitly documented.
- ✅ Addendum AC2: frozen exact metadata write-set, protected dirty snapshot, independent exact-relation verdict and failed-release ordering are enforced (`scripts/agent-loop/gate.py:375`, `scripts/agent-loop/gate.py:471`, `scripts/agent-loop/gate.py:549`, `scripts/agent-loop/test_gate.py:671`, `scripts/agent-loop/test_gate.py:682`).
- ✅ Addendum AC3: final evidence/archive commit precedes final finish; declared original-task obligations keep the same parent occupied (`scripts/agent-loop/gate.py:831`, `scripts/agent-loop/gate.py:861`, `scripts/agent-loop/test_gate.py:651`).
- ✅ Addendum AC4 for intact state: recover binds current stop/task/stage/check/count/snapshot, current prerequisites, independent confirmer and explicit cause-removal/liveness/Q/conflict assertions; invalid asserted conditions leave state unchanged (`scripts/agent-loop/gate.py:666`, `scripts/agent-loop/test_gate.py:724`). The corruption issue also undermines the ordinary-start side of this group.
- ✅ Addendum AC5: valid recovery acquires the stopped stage while retaining two failures; only its actual pass clears its counter, retaining stop/native history (`scripts/agent-loop/gate.py:666`, `scripts/agent-loop/gate.py:740`, `scripts/agent-loop/test_gate.py:724`).
- ✅ Addendum AC6, structurally: recovery is consumed once, failure reaches three and immediately stops, hashes prevent consumed remedy reuse, and unexecuted outcomes do not grant another execution (`scripts/agent-loop/gate.py:698`, `scripts/agent-loop/gate.py:740`, `scripts/agent-loop/gate.py:772`, `scripts/agent-loop/test_gate.py:736`, `scripts/agent-loop/test_gate.py:824`). One remedy-reuse fixture limitation is noted below.
- ✅ Addendum AC7: unowned source repair cannot resurrect stale prerequisites; Task3 is explicitly retained as stopped and is untouched by this task (`scripts/agent-loop/gate.py:705`, `scripts/agent-loop/test_gate.py:747`, `docs/development/agent-loop-gate.md:392`).
- ✅ Lifecycle contract prose is updated consistently in all three assigned documents, including separate metadata acceptance, release-dependent keeper finalization, recovery and runtime limitations (`docs/development/agent-loop-gate.md:244`, `docs/development/artifact-contracts.md:117`, `docs/multica/operating-model.md:50`, `docs/multica/operating-model.md:140`, `docs/multica/operating-model.md:198`).
- ⚠️ Cannot verify from this diff: semantic truth of source/docs/release verdicts, genuinely independent human/runtime identities, actual environment remediation and native duplicate exclusion, Task3's external retained state, full-autonomy pilot/activation checklists, and unchanged historical documents outside these five files. Controller/runtime must establish these independently; the implementation does not claim its structural CLI establishes them (`docs/development/agent-loop-gate.md:336`, `docs/development/agent-loop-gate.md:383`, `task-6-report.md`, “Self-review and limits”).

### Strengths

- The extension keeps real observed evidence revisions separate from both strict artifact equivalence and metadata publication. History retains source snapshots, old proof hashes and original reports instead of relabeling tests as executed on a future evidence commit (`scripts/agent-loop/gate.py:433`, `scripts/agent-loop/gate.py:529`, `scripts/agent-loop/gate.py:635`).
- State transitions are organized around explicit stage responsibilities, immutable archives, pinned prerequisites and a single retained parent. Return and recovery have distinct admission rules; recovery does not create a fresh two-attempt budget (`scripts/agent-loop/gate.py:500`, `scripts/agent-loop/gate.py:666`, `scripts/agent-loop/gate.py:783`).
- Prior fixtures are preserved: the test-file package contains only an insertion after the original GateTests class; all original 28 fixtures and helpers remain unchanged (`scripts/agent-loop/test_gate.py:431`). The supplied final output reports 45/45 passing, including both prior prerequisite/ownership regressions (`task6-final2-green.txt:1`, `task6-final2-green.txt:48`). No suite or hook rerun was performed in this review.
- Source/evidence identity was independently checked without Git commands: all five reviewed file SHA-256 values equal both the final precheck and postcommit manifests, and their calculated Git blob IDs match the package's head indexes (`bfde05a`, `0d8c27a`, `21f1c6e`, `95b5016`, `68634e1`). The saved precheck patch hashes to `3298423f4e943e397e8938e51f054aed7c64a02a60a1057d76751ffa524a3708`, exactly its manifest. This supports the report's claim that tested parent-plus-dirty bytes equal committed bytes without claiming execution at the later head (`task6-final2-precheck.json:2`, `task6-postcommit.json:2`).

### Issues

#### Critical (Must Fix)

- None found.

#### Important (Should Fix)

- `scripts/agent-loop/gate.py:227` and `scripts/agent-loop/gate.py:615`: validation checks only counters that remain present; `lifecycle_start` then uses `setdefault(..., {'failures': 0})` for any missing declared check. After a real two-failure stop, removing only `tasks[T].checks.review.review` leaves all stop events/history intact but is accepted by `status`; ordinary `start` silently recreates zero and acquires a third run. This directly violates the required fail-closed handling of corrupt retained state and the prohibition on restoring attempts by losing a counter. Validate counter completeness/consistency against retained check events and stage history before acquisition; initialization is legitimate only for a check with no prior execution. Reject corruption without rewriting the retained bytes. Extend the corrupt-state fixture beyond negative integers/missing history to missing counters and counters inconsistent with the latest retained check event.
  Focused probe observed: `before_failures=2`, `stop_event=check-13`, `history_count=13`; after deleting that one counter, `status_accepted_corrupt_counter=true`, `start_result=acquired`, `after_failures=0`, while `retained_stop_failures=2` and `history_count=13`. The resumed source check itself was not executed.

#### Minor (Nice to Have)

- `scripts/agent-loop/test_gate.py:736`: the remedy-reuse negative passes the old stop event and `failures=2` after the resumed failure has created a new stop at three. It therefore rejects at current-stop binding before exercising consumed-remedy rejection. Add a focused case with the current stop/count/cause and a new event/run but identical remedy bytes, so addendum AC6's remedy-specific refusal is actually demonstrated. The production hash guard exists at `scripts/agent-loop/gate.py:698`.
- `task6-commit.txt:27`: normal hooks retain one skipped server test file and one todo. These are explicitly disclosed and not introduced by this five-file task, so they do not block the lifecycle change, but they cannot be counted as accepted coverage. The separate 45-case gate output is pristine.

### Checks Performed

- Trace-first `get_project_map(summary_only=true)` succeeded; `get_outline(scripts/agent-loop/gate.py)` returned NOT_FOUND because the index covers the original checkout.
- Read the immutable diff once in consecutive bounded chunks. Narrow unchanged helper inspection was necessary for two concrete risks: whether expected RED could satisfy return's failed-event condition, and whether publication's outside-dirty fingerprint globally omitted foreign artifact files. Inspected only `proof`, `snapshot_once`, `snapshot`, `git` and fixture setup/evidence/CLI helpers at hash-verified head bytes; both doubts were resolved without findings. No already reviewed changed function was reread.
- Ran exactly one custom disposable Git/subprocess probe for the missing-counter corruption path, using the existing lifecycle setup helpers. It did not discover or rerun the 45-case suite, hooks, Task3 checks or any product check; it did not modify this checkout/index/HEAD. Only the requested review report was written to this checkout.
- Reviewed supplied final regression, precheck, postcommit and hook evidence; independently rehashed five files and their Git blob identities plus the saved precheck patch. No claim is made about test execution at future head `e6f8684`.

### Assessment

**Task quality: Needs fixes.**

**Reasoning:** The main lifecycle, publication and conditional recovery paths are coherent and backed by preserved regressions, but retained-state validation can silently reopen a check stopped at two failures. Fix that admission bypass and verify it with a focused regression before Task6 acceptance.
