# Empty failed-launch profile directory — final offline correction

Base/HEAD `139954950e91c530772216975d1c85bce4bbd155`. Exactly two owned files changed: canary JavaScript and static Python test. Eight other release pins remain unchanged. No Docker/browser/model/auth/native/Multica/runtime/image/vector/security/package/config operation, receipt, staging/commit/hooks, evidence deletion or rewriting of canary7 result. The three-invocation package is exhausted; actual browser count remains7. No further invocation is authorized.

## Confirmed saved outcome and scope

Canary7's control mode reports successful form completion, process/kernel/namespace checks, screenshotPNG/output eviction, filesystem negatives, redirect sink hits1, missing-browser negative, host/TestNet denials and unchanged inventory before its final assertion. It remained accepted=false because profileResidue contained `playwright_chromiumdev_profile-lJcdZs`. Root separately observed that residue as an empty real0700 directory owned by hostUID501, with the distinct original main profile `playwright_chromiumdev_profile-wtmwAd` absent. This supports the overstrong empty-name-list diagnosis; it does not retroactively execute the new temporal main-close check or complete acceptance mode. Original report hash `10831694d179ab5396dad5e547ddcdce782d9f9c19aca942efd573f55ef735e8` is preserved.

## Narrow behavior change

After main client stop and before creating the intentional missing-executable client, the canary records mainProfileObservation and actual mainProfileResidue names, then requires no profile entries whatsoever. Empty directories are still forbidden in this main-close phase. Known main profile names are retained from the already captured normal browser argv's fixed --user-data-dir path; absence of that identity is refusal. Thus a failed launch cannot excuse a leftover or recreated main-profile name.

After missing-client stop, the final profile observation records real lstat type/identity/mode/UID and emptiness, with identity/time recheck for directories. profileResidue retains the actual names. The shared predicate permits only failed-launch phase entries that are newly present after the clean main-close observation, empty real nonsymlink directories, and not any recorded main/prior name. Errors, nonempty directories, symlinks, files, other types and reused names refuse. failedLaunchEmptyDirectories and reusableState=false are assigned only after this proof succeeds. No directory is removed to satisfy the predicate; actual residue names are never replaced with an empty list.

Fresh-cache construction, normal client shutdown, no-download/missing-browser controls, inventory, screenshots, namespace/security criteria and all preceding helpers remain unchanged. This permits an empty failed-launch directory, not reusable browser state or weakened normal-close cleanup.

## Offline verification

Expected RED runs the old actual profileResidue==[] assertion with the saved residue name and reproduces its rejection. Focused GREEN exercises the shared observer/predicate on real synthetic filesystem entries. Cases cover newly created empty failed-launch directory allowed; same empty directory in main phase denied; preexisting/reused name denied; nonempty/symlink/file denied; and clean main/failed phases allowed. Tests verify actual names survive both success and failure and every created fixture remains in place—no cleanup-by-deletion. No canary7 directory was modified or used as a writable fixture.

Focused1/1 and final full static10/10 passed, with one covering run. Self-review before covering added explicit refusal if the known main profile identity is absent. Exact commands/results/log hashes: `playwright-static/empty-failed-launch-fix/commands-and-results.json`. Adjacent `red.log` exit1 expectedRED; `green.log` exit0; `covering.log` exit0. No unexpected local test failure, unchanged-suite repeat or actual browser run.

## Frozen final review package

`playwright-static/empty-failed-launch-fix/empty-failed-launch-only.diff`: 8321 bytes, SHA256 `3b933b9ff16bed59fc6aef409cd58f143e7fd3f6b632f13aed3b020cea02eb66`. `source-proof.json` records before/after files, eight unchanged pins, preserved actual report and check exits; `.before` copies retain starting source. Source writer released for the single combined scoped final review with canary7 evidence. The new phase predicate has offline proof only; whole browser/static/native acceptance or a production receipt is not claimed.
