# Playwright chroot cause-control preparation

Prepared 2026-09-14, scratch only. No Docker, browser, native/model/auth, install, build or syscall execution occurred. Production seccomp/runtime/canary and Git index/HEAD are unchanged. Browser canary remains stopped at two failures, with no receipt.

## Exact review package

Directory: `/private/tmp/altera-agent-loop-autonomy/.superpowers/sdd/2026-09-13-autonomy-execution/playwright-chroot-preparation`.

- `control.json`: exact approved current profile, SHA256 `ff9cc602bfbd54007f4894c121d9f3013e4e7a2a9b9b7a7d3fcd7c2f0f3b7140`.
- `candidate.json`: SHA256 `e75c64002d22c7893bbc3aef2f8d7c0dde8f673254d0846d9182e89b180024f1`. The only semantic delta is deletion of `/syscalls/23/includes`, whose value was `{"caps":["CAP_SYS_CHROOT"]}`, from the unique `names:["chroot"]` rule. Its ALLOW action and every other rule/top-level value/order are unchanged. `sole-diff-proof.json` records duplicate-free parses and equality after restoring only that field.
- `probe.pl`: SHA256 `64eaccb7266cb476410799090178a76463bd682f3b596d0aa6b2a281abc57b29`; one fixed core-builtins-only Perl helper, no modules, shell, exec, network or dependency acquisition. It is intended for the exact existing image `sha256:97bd451e71779dbd6527814023f16dd952e792ba633132fc7da297aed10b1426`. No compiled helper or new runtime is added.
- `commands.json`: exact owner-reviewable runtime-identity preflight and two-arm create/inspect/start/inspect/remove/absence command vectors. No executor automatically consumes it; root must explicitly authorize execution and check pins/metadata first.
- `source-provenance.json`, `syntax.log`, `syntax-final.log`, `self-test.json`, `synthetic-raw.json`, `tests.log`: static and pure-callback proof. Final synthetic suite4/4 passed; Perl syntax passed. These tests do not fork, unshare, read procfs or call chroot on the host.

## Runtime prerequisite

The existing Dockerfile installs Debian git, and the successful build log retains that cached instruction. [Debian bookworm's official git package](https://packages.debian.org/bookworm/git) declares Perl as a dependency. Python/ctypes installation is not established by Dockerfile or the complete browser dependency install log. Under root's ruling, the helper therefore selects `/usr/bin/perl` only. Actual executable presence, canonical path, ELF identity and digest inside this exact image are still unknown; `runtime_identity_preflight` must establish those facts before either arm. If absent/incompatible, stop without substitution/install.

Both arms run UID/GID1000 with all container capabilities dropped, no-new-privileges, read-only rootfs, no network and fixed resource limits. The sole mount is the same read-only probe file. No source, credentials, config, cache or Docker socket is mounted. Seccomp is consumed by the daemon from the private host file; it is not mounted. Stdout/stderr are captured by the trusted host coordinator into mode0700 evidence directories, so no writable container bind mount is needed. `env -i` clears interpreter startup environment; no host env passthrough.

The parent forks one child, retaining ready/go/result pipes before namespace changes. Child records user namespace inode, actual UID/GID and capability/NoNewPrivs/Seccomp fields, calls literal unshare(CLONE_NEWUSER), then waits. Parent writes only setgroups=deny and the single literal UID/GID1000→1000 mappings. Failure sends a refusal byte; chroot is not attempted. Child verifies a new namespace, mapped UID/GID1000 and CAP_SYS_CHROOT in that namespace, then calls literal chroot(`/proc/self/fdinfo/`). Return and errno are captured immediately; a success normalizes errno0. It writes its bounded JSON through the preopened pipe and exits without exec/shell or filesystem reads after chroot. Parent preserves its own before/after state and reaps only that exact child. Alarm/handshake failure kills and waits for the exact known child; no broad cleanup.

Core syscall numbers51(chroot) and97(unshare) are pinned against the official [Linux v6.18 generic header](https://raw.githubusercontent.com/torvalds/linux/v6.18/include/uapi/asm-generic/unistd.h) and [shared syscall table](https://raw.githubusercontent.com/torvalds/linux/v6.18/scripts/syscall.tbl); the [arm64 header](https://raw.githubusercontent.com/torvalds/linux/v6.18/arch/arm64/include/uapi/asm/unistd.h) selects the generated64-bit table. [sched.h](https://raw.githubusercontent.com/torvalds/linux/v6.18/include/uapi/linux/sched.h) pins CLONE_NEWUSER=0x10000000. The helper refuses non-Linux and a non-ELF64/little-endian/AArch64 interpreter before any raw syscall. This is static ABI provenance, not a read of the current VM kernel.

## Meaningful synthetic checks

The actual helper's `child_flow`, `install_maps` and `reap_child` functions execute with fixed in-memory callbacks. Tests distinguish success0/errno0, -1/EPERM, another errno, unshare failure and mapping failure; setup failures never invoke the chroot callback. Mapping stops after the first failed write. Cleanup kills/waits exactly synthetic PID123. The result interpreter keeps setup failures and unexpected control success separate from confirmed seccomp hypothesis. It cannot turn these fixtures into a real cause result.

## Security delta and owner decision

The candidate admits chroot past seccomp for any path; BPF cannot dereference the pathname to allow only `/proc/self/fdinfo/`. Kernel namespace-scoped capability/path/LSM checks still apply. No initial or host CAP_SYS_CHROOT/CAP_SYS_ADMIN is added, and visible mounts remain unchanged, but exposure of the chroot kernel path is a real tester-container security extension. It must not become a daemon/global policy and must not be applied before owner approval of this exact package.

Expected actual comparison: control -1/EPERM versus candidate0/errno0 supports the cause hypothesis. EPERM in both falsifies the seccomp-only remedy. Unexpected control success or other errno requires diagnosis. No outcome resets the stopped browser-canary counter or accepts Playwright. Parent/child namespace behavior, procfs permissions, actual Perl startup/ELF behavior, return/errno, container inspect/cleanup and final browser compatibility are all unexecuted. The prepared command manifest is not execution authorization.

## Preparation fix1 — runtime identity lifecycle only

Independent review found the anonymous `run --rm` prerequisite could leave an unidentifiable worker after interruption. `commands.json.runtime_identity_preflight` now specifies exact-name pre-absence/create/inspect/start-attach/post-inspect/finally force-remove/post-absence. The fixed name is `altera-chroot-identity-97bd451e7177`; all actual output/command/exit records have private, bounded targets under `evidence/runtime-identity/`. A 15-second outer watchdog, exact created-ID/config verification before start, strict bounded result validation, and unconditional targeted finally cleanup after create success are mandatory. Final absence must be positively verified even after failure. An existing name refuses creation and is never cleaned by this operation.

The original Node built-ins-only readback expression and all image/platform/security/resources inputs are byte-for-byte retained; only lifecycle identity changes. Built-in seccomp, no mounts/network, UID/GID1000, cap-dropALL, no-new-privileges and read-only root remain fixed. The two arms, both profile JSON files and Perl probe are unchanged. `fix1/*.before` preserves original manifest/report/hash snapshot; `fix1/fix-only.diff` and `fix1/static-proof.json` record the correction. Static assertions verified sole manifest section change, exact preserved Node/security argv tail, output coverage and absence of mount/profile override. No Docker or helper rerun occurred; runtime presence/identity and Linux behavior remain unknown. This JSON requires an enforcing trusted coordinator; it is not an executor or authorization. Browser failure count remains 2.
