# Независимая приёмка T-110 / ALTE-4

- **Ревьюер**: независимый локальный reviewer, не участвовавший в реализации
- **Реализация**: `e18aa80`
- **Проверенная ревизия**: `3db90d682ad7c483032ea14de968eb2ff63c8fd1`
- **Вердикт**: **ПРИНЯТО**; блокирующих дефектов реализации не найдено
- **Ограничение вердикта**: локальная приёмка документации. Она не доказывает runtime-изоляцию Multica, permissions среды или автоматизированный rollout.

## Проверенный объём

Прочитаны brief, задача T-110, все пять названных в задаче спецификаций, план и старый отчёт. Для критериев также проверены непосредственно связанные источники: журнал только §25.1 и §25.6, `plan-free.md` п. 6а, `_templates/ranking.md`, реестр `60-ranking/README.md` #13 и заголовки `views-counting.md`/`article-score.md`.

Артефакт `t110-review.diff` побайтно совпадает с `git diff --unified=10 e18aa80^ e18aa80` для пяти целевых спецификаций. Коммит `e18aa80` изменяет только план T-110 и эти пять спецификаций. `git diff --exit-code e18aa80..3db90d6 -- <пять файлов>` завершился с кодом 0: целевые файлы не менялись после реализации. `git diff --check e18aa80^ e18aa80 -- <пять файлов>` завершился с кодом 0.

## Критерии приёмки

### AC-1 — принято

Требование: CTA на главной и в списке авторов направляют читателя в создание материала согласно §25.1.

- `docs/spec/20-public/home.md:100`: читатель идёт на `/me/articles/new`, с прямыми ссылками на `plan-free.md` п. 6а и журнал §25.1; гость остаётся на `/pricing`.
- `docs/spec/20-public/authors-index.md:53-55` и `:83-84`: то же разделение зафиксировано и в структуре экрана, и в таблице действий.
- Источник: `docs/decisions/role-review-working-log-2026-09-08.md:628-630` — первое «Создать статью» автоматически и бессрочно открывает базовые авторские возможности без оплаты и ручного одобрения.
- Граница соответствует плану `docs/plans/2026-09-10-t110-spec-sync.md:14-15,22-39`; EmptyState намеренно исключены строками `75-76`.

### AC-2 — принято

Требование: политика grants/promo согласована с бессрочной базовой выдачей и сохраняет пометку `[ДОПУЩЕНИЕ]`.

- `docs/spec/70-plans-and-billing/grants-and-promo.md:20-25`: общее правило обязательного срока получило ровно одно исключение — автоматическую базовую выдачу при первом создании, `endsAt = NULL`, со ссылками на `plan-free.md` и §25.1; пометка `[ДОПУЩЕНИЕ: техническое отображение]` сохранена.
- Там же `:56-58`: исключение повторено в таблице с `standard`, автоматическим актором и `author.enabled` (#87 `[черновик]`).
- Источник: `docs/spec/70-plans-and-billing/plan-free.md:38-44` задаёт те же `PlanGrant` уровня `standard`, `endsAt = NULL`, событие `author.enabled` и ту же пометку допущения; продуктовый принцип подтверждён журналом `:628-630`.
- Обычный ручной `plan.grant` без срока по-прежнему запрещён (`grants-and-promo.md:73-74`), поэтому исключение не расширяет ручную финансовую операцию.

### AC-3 — принято

Требование: строка матрицы #36 отражает заметки к блокам в ручной ветке по §25.6.

- `docs/spec/00-registries/access-matrix.md:69`: замечание привязано к версии; рецензент оставляет его в ручной ветке, автор отмечает решённым; указаны журнал §25.6 и статус `Г6`.
- Источник: `docs/decisions/role-review-working-log-2026-09-08.md:644-647` прямо закрепляет непубличный диалог, заметки рецензента у блоков, привязку к версии и возможность отметить их решёнными.

### AC-4 — принято

Требование трактуется в точной границе плана: привести к шаблону **шапку** `engagement-tracking.md`, не переписывая владелецкий документ целиком (`docs/plans/2026-09-10-t110-spec-sync.md:18,59-68`).

- `docs/spec/60-ranking/engagement-tracking.md:3-6` содержит четыре стандартных поля `Реестр`, `Статус`, `ADR`, `Этап` в том же порядке, что `docs/spec/_templates/ranking.md:3-6`.
- Реестр #13 и формулировка статуса подтверждены `docs/spec/60-ranking/README.md:34-37`; этап 2 согласован с `docs/spec/60-ranking/views-counting.md:3-7` и `article-score.md:3-8`.
- Diff меняет только строки шапки; §§1-20 не затронуты, как заявлено в старом отчёте `docs/reports/2026-09-10-t110-spec-sync-report.md:41-47`.

### AC-5 — принято

Требование: новые продуктовые правила не вводятся; Q-09 и Q-12 не разрешаются вместо владельца.

- Все содержательные добавления являются переносом уже закреплённых фактов: CTA и бессрочность — журнал §25.1; техническая форма выдачи — помеченное допущение `plan-free.md` п. 6а; заметки — журнал §25.6; ranking-шапка — шаблон, реестр и существующие соседние спецификации.
- `git diff-tree --no-commit-id --name-status -r e18aa80` показывает только план и пять целевых `.md`; продуктового кода и файлов вопросов владельца в коммите нет.
- `docs/backlog/owner-blockers.md:70-76` оставляет Q-09 в статусе `открыт`; `:94-100` оставляет Q-12 в статусе `открыт`.
- В `access-matrix.md:52` правило #19 по-прежнему отменяет `translation.save.published`; T-110 меняет только строку #36. Решение Q-12 не подменено.

## Находки

Блокирующих находок по пяти целевым спецификациям нет.

Неблокирующая неточность старого evidence: `docs/reports/2026-09-10-t110-spec-sync-report.md:71,78` утверждает, что каждая правка ссылается именно на §25.1 или §25.6. Правка шапки `engagement-tracking.md:5` ссылается на журнал §23. Это не нарушает исходный критерий задачи `docs/backlog/tasks/T-110-spec-sync-contradictions.md:26-27`, который требует ссылки на решение журнала без ограничения двумя подразделами, и не влияет на AC-4. Формулировку старого отчёта нельзя использовать как точное доказательство этого более узкого утверждения.

## Достаточность старого evidence и пределы проверки

Старый план и отчёт достаточно точно описывают целевой diff и назначение правок. Командные результаты в `docs/reports/2026-09-10-t110-spec-sync-report.md:56-80` являются историческими краткими пересказами без полных логов, поэтому в этом ревью не считаются свежим доказательством lint/test. По прямому ограничению brief продуктовые тесты для документационной задачи не перезапускались. Свежие факты текущего ревью ограничены чтением источников, проверкой точного diff, неизменности пяти файлов и `git diff --check`.
