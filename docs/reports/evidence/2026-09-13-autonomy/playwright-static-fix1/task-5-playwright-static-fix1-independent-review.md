**R1 — Match canary tool arguments to the pinned MCP schema before spending a recovery attempt — ADDRESSED.** In `scripts/agent-runtime/playwright-mcp-canary.mjs:10`, shared builders now provide `target` for click/type and `scale: "css"` for every screenshot. The actual browser-flow call sites use those builders. `pathDenial` at line 13 requires an error tied to the attempted path and a permission/read-only/outside-allowed-path denial, while excluding schema-validation errors. The relative target now resolves from the output directory toward `/source`. The synthetic contract checks use the previously observed real tool schemas, including required/additional-property/enum checks and distinct denial controls. This establishes request/classifier correctness, not a completed live filesystem denial.

**R2 — Enforce missing-receipt refusal on TOML semantics or a strictly validated canonical form — ADDRESSED.** `scripts/agent-runtime/runtime.py:371` replaces the raw substring check with a bounded, deliberately restricted no-receipt parser. It uses only the approved unchanged `_base_line_without_comment` and `_base_key_path` helpers; escaped/ambiguous keys, inline/nested/array/unknown MCP declarations refuse. Recognized Context7/trace entries are checked against their permitted field/command/argument values, so renaming an arbitrary executable to `trace` does not bypass admission. The original escaped-key/vector case is covered by a meaningful regression. Ordinary scalar/array policy values, known emitted forms and managed-empty policy remain covered; this is not advertised as general TOML support.

**New breakage in the four-file fix: None identified that blocks this scoped static review.** The no-browser `contract` branch runs before the canary/browser branch, and production call sites share its builders. Runtime errors use the fixed suppressed-chain refusal. The fix does not modify the receipt validator, dependency/image/seccomp definitions, mapper implementation or browser security policy.

**Static spec verdict: PASS for R1/R2. Static quality verdict: PASS for this fix.** Original static findings are addressed; no new Critical/Important finding remains in the scoped diff.

## Identity and checks

- Base/HEAD remains `139954950e91c530772216975d1c85bce4bbd155`; fix base is the previously reviewed stopped checkpoint, not a new commit. Read the appended Static fix1 report, prior findings and the already established brief/superseding rulings in scope.
- Read `playwright-static/fix1/fix-only.diff` once: 18,628 bytes, SHA-256 `ce716fbfc00f30fec4a914ed927c3a0780be5a6070b4be16f0d0f7d9eac13670`. It changes exactly runtime, runtime test, canary and canary test. `source-proof.json` records the matching before/after identities.
- Independently checked all four `.before` file hashes against the prior checkpoint pins, reconstructed each after-file in memory from the immutable diff, and verified its hash matches `source-proof.json`. No mutable source read or write was needed for that identity check.
- Read raw `r1-red.log`: failure is the missing required `target` in the actual literal click request, not a missing test/module. Read `r1-green.log`: focused schema/classifier check passed. Read `r2-red.log`: the equivalent Playwright TOML incorrectly failed to raise; `r2-green.log` confirms the corrected refusal check passed.
- Read actual `runtime-covering.log`: **25/25 passed**, including ordinary command equivalence and existing argument/model/mount restrictions. Read `playwright-covering.log`: **4/4 passed**. Logs are under `playwright-static/fix1/`. No retained suite, contract command, browser or Docker check was rerun by this reviewer; no new probe was needed.

## Scope and remaining acceptance

No out-of-scope findings were added. This re-review did not restart the original mapper/runtime/receipt audit or inspect real config, auth, cache, native state or APIs. No source/index/HEAD mutation, model/MCP/browser/container/network operation or subagent occurred.

The real browser canary remains **stopped at consecutive failure count2**, regardless of this passing static verdict. The actual sandbox cause, meaningful browser flow, artifact/denial controls, network/no-download checks and final cleanup acceptance remain unexecuted at an accepted security configuration. The classifier's behavior on future actual errors still requires that controlled evidence. No receipt, security change, canary retry, normal commit or whole-milestone PASS is authorized or claimed here. Root may use this scoped result when preparing the separately authorized recovery; it does not reset the browser counter.
