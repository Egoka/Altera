# Process-title and kernel executable identity — offline correction

Base/HEAD `139954950e91c530772216975d1c85bce4bbd155`. Exactly two owned files changed: canary JavaScript and static Python test. Eight other root-release pins unchanged, including image/vector/seccomp/runtime/mapper/build/package inputs and test_runtime3625. No Docker/browser/model/auth/native/Multica/config, build/install, receipt, staging/commit/hooks. Actual same-check count remains6; root reports one package invocation remains, but no invocation is authorized by this report.

## Saved cause and bounded contract

Actual canary6 browser-processes.json contains all seven browser rows. Main PID32 preserves ordinary NUL-delimited arguments; six children expose one rewritten title string, starting with the exact executable plus a role. RendererPID94's title truncates after --ozone-plat. The former argv[0] equality rejects those rows before the already recorded namespace/capability evidence can be evaluated. Saved rows provide no kernel exe proof; synthetic proofs used here must not be mistaken for actual procfs observations.

Every future browser row now requires `/proc/PID/exe` readlink to equal the fixed inventory-pinned executable path, and a regular target with dev/ino equal to that executable's stat identity. The fixed image executable identity is recorded before and after collection; each kernel exe link/stat is recorded and reread alongside raw cmdline, PID starttime/status/maps/namespace checks. Mode/size/mtime/ctime are retained for identity consistency checks. Existing inventory already checks the fixed path's canonical regular executable and content hash in the pinned RO image. Missing/EACCES/mismatched/deleted executable paths or observed replacements refuse; no read-permission widening or argv fallback exists.

Normal argv must begin with the exact path. A rewritten title is admitted only as one raw field with exact `<fixed executable> --type=<known role>` prefix; recognized roles are zygote, renderer, gpu-process and utility, matching saved rows. Multiple/ambiguous/unknown/unanchored type tokens refuse. Observed --no-sandbox tokens refuse in either representation; legitimate --no-zygote-sandbox remains distinct. Titles are explicitly labelled `rewritten-title`, retained raw, and classified with complete_argv=false. No shell parsing or reconstruction of original full arguments is claimed, especially for truncated renderer titles.

A normal main-browser NUL argv row is now mandatory, together with the existing renderer requirement. Its observed arguments and fixed launch provide the no--no-sandbox evidence; child titles cannot establish absence of unprinted flags. Kernel executable identity, the unchanged namespace/capability/NNP/seccomp thresholds, and fixed launch jointly constrain children. Exe readability may still fail for a nondumpable process on the actual run; such a row will remain in diagnostics and refuse rather than weaken evidence.

## Diagnostics and tests

Raw cmdline, decoded fields, representation hint, kernel exe path/identity and errors are captured before isolation assertions and persisted by the existing inspect wrapper. Failure retains all rows. The existing synthetic proc-shaped collector test now supplies a real temporary file as its synthetic pinned executable and proc exe symlinks, verifies identity/raw bytes, then removes a proc exe link and proves the missing-executable error row still persists.

The new focused test copies exact all-seven actual saved rows. Its synthetic kernel identities are explicitly injected (dev1/ino42). Expected RED sends this complete otherwise-valid fixture through the old validator, reproducing refusal of collapsed child argv. Focused GREEN passes normal main plus collapsed zygote/GPU/utility/truncated-renderer rows, and rejects wrong path prefix, device/inode/kernel path, missing/EACCES exe, duplicate/unknown/unanchored role, forbidden sandbox flag, missing main and race errors. Namespace tests retain their original positive/negative thresholds and receive only synthetic executable identity prerequisites.

Focused1/1 passed; final static suite9/9 passed once. `playwright-static/process-title-fix/commands-and-results.json` records exact argv, exit1 expectedRED, exit0 focusedGREEN, exit0 covering and raw log hashes. Raw `red.log`, `green.log`, `covering.log` are adjacent. No unexpected local test failure or unrelated suite rerun. These tests establish the parsing/identity contract, not actual kernel exe readability or full browser acceptance.

## Frozen review package

`playwright-static/process-title-fix/process-title-only.diff`: 23960 bytes, SHA256 `de1f0d6ad21134cb68ea1d46701e1c13dca06cd21526882c514c3b7498f2beaa`. `source-proof.json` binds both before/after files, eight unchanged pins, exact saved-row equality, unchanged namespace thresholds and prior flow/helpers. Saved source evidence SHA256 `7ddbce2dfeff5d6681316487b2d4bcb1451f7554393787c2fcbaefc05cde1da4`. `.before` files preserve the previous checkpoint. Source writer released for independent scoped review. The remaining full invocation belongs to root only after independent PASS; no further retry or accepted receipt is claimed here.
