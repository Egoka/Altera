# D1 native diagnostic runbook (pending local review)

This is a read-only diagnostic stage, not task/model acceptance. No run is authorized until the immutable D1 source, binary, imports, synthetic boundary proof and independent review pass.

1. Record accepted D1 commit, source/build/binary hashes and exact root/UID pins. Verify byte-for-byte immediately before registration. Do not use a draft binary.
2. Refresh Multica issue/agent/runtime/profile and full active/deferred-run state, workspace c5a721ae-7e4f-41cf-92b9-fd1da0aa3ec6. Agent db94a617-807c-4eec-981f-51fd4b3217d4 must be idle; all three autopilots paused. Preserve original runtime fd14b334-b79c-47ae-aad9-a98340bbb7d4 and exact non-runtime fields. ALTE-11 is 01a09bb3-96bf-7532-8d34-e384d9ac5ed2. If a new owner run is live, do not interrupt it.
3. Capture original checkout current HEAD/branch/status and a private preservation snapshot with no credential traversal. Compare after the diagnostic; concurrent owner changes are recorded without attribution or automatic restoration.
4. Create one clearly labeled private diagnostic profile with Claude protocol and the reviewed D1 executable. Pin the exact executable using runtime profile set-path. Discover its runtime without restarting daemon. Static truthful help/version is not model availability acceptance.
5. Fresh compare, bind only reviewer runtime; preserve model, medium, concurrency, instructions, skills, permission and MCP assignment. Read back exact invariants. Assign ALTE-11 without start while Backlog, then explicitly enqueue one diagnostic after quiescence. Record run ID. Expected result is exit78 with ALTERA_METADATA_CONFIG_ONLY_V1, empty stdout, task_acceptance:not_checked, and no model call. A red native provider card is an expected diagnostic transport result, not successful model execution.
6. Read only the bounded descriptor fields from the new run. Do not archive entire message history or raw daemon config. Unresolved candidate stays unresolved; do not reveal unknown values or silently replace configuration.
7. Drain the exact diagnostic run and check full history/deferred state. Unassign with plain --unassign (not incompatible --no-start). Restore reviewer runtime only after a fresh comparison of preserved fields, read back invariants. Restore issue metadata if changed by diagnostic within authorized original state and no concurrent owner edit.
8. Remove temporary runtime without cascade, unset local path, delete profile. Re-read runtime/profile list, reviewer, issue, active/deferred state and paused autopilots. Preserve descriptor, cleanup evidence and failure history in explicit allowlisted report paths. A cleanup failure blocks any next native action.

Installed CLI invocation: command/subcommand/ID before explicit --workspace-id and --output json. profile list defaults to table. No arbitrary host shell, Claude fallback, credential read, daemon restart or autopilot activation belongs to this stage.
