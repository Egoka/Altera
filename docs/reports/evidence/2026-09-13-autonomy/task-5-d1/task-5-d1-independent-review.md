### Spec Compliance

- ❌ Issues found: argv value scanning can turn prompt text into file-read authority (`scripts/agent-runtime/metadata-config-probe.c:335`); required unreviewed-field metadata is incomplete (`scripts/agent-runtime/metadata-config-probe.c:250`, `scripts/agent-runtime/metadata-config-probe.c:343`). Diagnostic exit 78 and unresolved mapping remain correct, but the first defect violates the exactly-one actual MCP flag/opaque prompt-value input boundary.
- ✅ Milestone A’s structural boundary is present: compiled trusted root/alias/UID pins, descriptor-relative no-follow read, private modes and owner checks, bounded regular-file input, same-descriptor and path identity rechecks, strict UTF-8/JSON parsing, bounded metadata-only output, core-dump refusal and raw-buffer wiping (`scripts/agent-runtime/build_metadata_config_probe.py:16`, `scripts/agent-runtime/metadata-config-probe.c:53`, `scripts/agent-runtime/metadata-config-probe.c:134`, `scripts/agent-runtime/metadata-config-probe.c:282`, `scripts/agent-runtime/metadata-config-probe.c:326`).
- ✅ Public literal catalogs match the approved commands, argv vectors and two Context7 endpoints (`scripts/agent-runtime/metadata-config-probe.c:207`). Root explicitly confirmed that both Trace commands are absolute paths under `/Users/egorbondarenko/.trace/bin/`; neither bare trace-mcp nor PATH lookup is authorized. Candidates are not execution or assignment acceptance.
- ✅ The four-file diff adds only D1/build/tests plus the previously requested policy-versus-image documentation correction (`docs/development/runtime-isolation.md:161`). D0/runtime/guard/product/gate implementations and external native state are unchanged by this diff.
- ⚠️ Wrong-file-owner/device-node fixtures were not executed; the report correctly explains the unprivileged harness limit. The UID-pin test is evidence of a process-owner mismatch only, not a wrong-owner file test (`scripts/agent-runtime/test_metadata_config_probe.py:74`). The respective owner/kind rejection branches are present (`scripts/agent-runtime/metadata-config-probe.c:303`); the conditional feasibility requirement does not justify inventing privileged coverage.
- ⚠️ Same-UID producer races, dynamic loader/stdlib trust, and native integration remain outside this diagnostic’s acceptance. Full Task5/M4/M7, MCP/auth/context/hook/refresh, profile binding and native runs remain pending. Nothing here authorizes a model or command execution.

### Strengths

- Opened file identity is checked before/after the bounded read and compared to descriptor-relative path lookups; root/child/file symlinks and cross-device children are rejected (`scripts/agent-runtime/metadata-config-probe.c:265`, `scripts/agent-runtime/metadata-config-probe.c:299`, `scripts/agent-runtime/metadata-config-probe.c:312`).
- The parser rejects malformed UTF-8, decoded duplicate keys, invalid surrogate escapes, trailing data, excessive tokens and depth; the tests include escaped-key duplicates, opaque secret canaries and recognized public escaped literals (`scripts/agent-runtime/metadata-config-probe.c:71`, `scripts/agent-runtime/metadata-config-probe.c:149`, `scripts/agent-runtime/test_metadata_config_probe.py:193`, `scripts/agent-runtime/test_metadata_config_probe.py:364`).
- Deterministic replacement/truncation fixtures pause the test process after the actual config read. Sandbox tests use a positive control for file write/fork/network/unrelated read and then demonstrate all four OS denials; these are meaningful behavioral checks (`scripts/agent-runtime/test_metadata_config_probe.py:219`, `scripts/agent-runtime/test_metadata_config_probe.py:279`).
- The frozen manifest separates the production pinned binary from fixture builds; canonical and approved-alias proof uses the exact production binary. The report retains three build failures/remedies and accurately states 17 full-suite cases plus one later focused case rather than a fictitious 18-case full run (`d1-review/manifest.json:2`, `d1-review/task-5-d1-pinned-proof.json:4`, `task-5-d1-report.md:1`).

### Issues

#### Critical (Must Fix)

- None found in the scoped D1 diff.

#### Important (Should Fix)

1. **[P2] Do not interpret option values as MCP flags.** `scripts/agent-runtime/metadata-config-probe.c:335` scans every argv token for `--mcp-config` or its equals form, without consuming the opaque values of known flags such as `--append-system-prompt`, `--system-prompt` or `--model`. A focused test against the exact frozen binary passed only two arguments: `--append-system-prompt` and the literal prompt value `--mcp-config=NEW_SYNTHETIC_PRIVATE_PATH`. There was no actual MCP option, but D1 opened that file and reported `read_status: ok`, `path_class: daemon_private_temp`, and `known_flags: ["--mcp-config"]`. This promotes ignored prompt text into authority to read a private daemon-shaped path and produces a false invocation descriptor. Parse the known native option arities so their values remain opaque; refuse ambiguous/unsupported argv shapes rather than searching their values. Preserve exactly-one MCP option handling for both supported forms and add negative tests for MCP-looking prompt/other values plus genuine duplicate options. This issue does not demonstrate arbitrary-path access, secret output or execution; those boundaries remained intact.

#### Minor (Nice to Have)

2. **[P3] Emit the required unreviewed-fields summary, including top-level unknown fields.** `scripts/agent-runtime/metadata-config-probe.c:250` makes a top-level extra field unresolved but never counts it, and the output at lines 343–348 has no `unreviewed_fields` boolean at any level. The same focused fixture included one extra top-level synthetic field: the descriptor correctly stayed unresolved but reported `unknown_servers: 0` and its recognized server’s `unknown_fields: 0`, leaving the reason unrepresented. Add a bounded count and the specified `unreviewed_fields: true` flag for unreviewed top-level/server/header/env structures, without exposing keys or values. This is a descriptor-contract/completeness defect, not unsafe mapping acceptance: the current implementation still refuses exact mapping for the unknown field.

### Checks and Evidence

- Read Milestone A and its binding constraints, complete D1 implementer report, immutable `task-5-d1-review.diff` (base a51b1116e1e2553605ab704bda2efc1cb463b31b; head bbe668c59b3f3dbfe3c2a8bb0157f9e4e6e0fcff), frozen manifest/imports/libraries, and the named local proof logs. Diff was read in bounded sections once. No source navigation/crawling or new Git inspection; reused this review task’s prior trace-first worktree rejection and authorized immutable-diff route.
- Independently SHA-256 checked all nine explicitly listed regular frozen artifacts against `d1-review/manifest.json`; all matched. Frozen binary hash: a30501e42d66c6d33f7929d0d8f2900c96e03d1213d36eefc3cd746751f07785. Source hash: bbde1f7bd2bec71451f4800b3e99c24207ef6428422b649e7fae2b0c3680547f. Existing import list contains no getenv/spawn/exec/fork/socket/connect/Keychain imports; only libSystem is listed. Source open flags are readonly and stderr formatting is the only output path.
- Read `task-5-d1-final-tests.log`: 17/17 passing. Read `task-5-d1-owner-pin.log`: one additional focused test passing. The final test file therefore has evidence from 17+1 cases, not one full 18-case run. No suites rerun.
- Read raw sandbox positive/negative stdout and empty stderr: positive control `{write:1, child:1, network:1, outside_read:1}`; denied control all zero. Fixture binary identity differs from the production pin, as expected; separate `d1-review/task-5-d1-pinned-proof.json` records both canonical/alias invocations against the exact pinned production binary.
- One new focused frozen-binary probe for the named argv ambiguity, review tool output d29de6: Python harness exit 0; D1 exit 78, empty stdout, read_status ok despite no actual MCP flag. Used only a newly created mode0700 daemon-shaped synthetic directory and mode0600 JSON under the approved canonical TMPDIR; it contained one public Context7 endpoint plus one synthetic unknown field. Both fixture paths were deleted afterward. Saved the review harness’s exact printed result in `task-5-d1-review-argv-proof.json`; its descriptor is parsed metadata, not a claim to have archived raw subprocess stderr bytes. No pre-existing daemon config enumeration/read, native/profile/model/MCP/auth operation, network operation, build or suite run occurred.
- Implementation/source/index/HEAD and root-owned evidence were not changed. Wrote only the requested review report and the focused review-result artifact. No subagents dispatched. This is the first D1 review finding set; do not reset any existing build/check history when applying a fix.

### Assessment

**Task quality: Needs fixes.**

**Reasoning:** D1’s filesystem, parser, redaction and synthetic OS-denial evidence are substantial and carefully scoped, but its argv scanner can consume a prompt value as file-read authority. Fix that input-boundary defect before native diagnostic registration; complete the nonblocking descriptor metadata gap without widening capability.
