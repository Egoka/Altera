# Root parameters for the Claude refresh milestone

This supplements, without rewriting, task-5-refresh-implementation-brief.md. Source implementation has not been dispatched. User's repeated instruction to finish the remaining autonomy blockers includes preparing and validating supported automatic Claude credential refresh; initial login was completed by the user. No further generic reconfirmation is required. Actual live exchange still waits for reviewed code, synthetic compatibility, and a separate bounded root operation, because those are concrete technical prerequisites.

Ruling: fixed maximum credential artifact 65536 bytes, JSON nesting depth16, any decoded string16384 UTF-8 bytes, captured child stdout+stderr combined8192 bytes. Exceeding bounds stops the process and emits only a fixed failure enum. Fixed timeouts: exchange60000ms, status30000ms, model120000ms; timeout kills the process group and preserves any pending generation. No runtime override of limits by manifest/prompt/env. Why: enough for the known small CLI credential/status shape while bounding opaque memory/output and failure latency. Cost if too narrow: a legitimate response is rejected and recovery is required; no silently expanded limit or repeated old-token exchange.

Ruling: real candidate model acceptance uses only pinned Claude2.1.263 in the existing immutable image, claude-opus-4-6, effort medium, fixed prompt `Reply exactly ALTERA_CLAUDE_AUTH_OK`, and requires the trimmed result exactly `ALTERA_CLAUDE_AUTH_OK`, exit0, JSON is_error false and primary model metadata matching Opus4.6. Use explicit --print/--output-format json/--model/--effort, --tools with one empty-string value, --strict-mcp-config pointing at a fixed empty mcpServers RO file, and --settings pointing at fixed disableAllHooks:true RO JSON. No source checkout, host/global config, API billing fallback or arbitrary prompt. CLI flags must first be proved by the existing pinned help/public CLI contract or synthetic argv construction; do not call a model in schema discovery. Record token usage and reported list-price estimate as such, not verified billing. Why: same fixed marker and model as the already successful standalone auth proof, now binds the newly accepted generation. Cost: one bounded normal subscription model request, with actual reported usage retained.

Pinned JSON schema remains pending separate public/immutable-CLI-artifact inspection; no real credential content may be read to discover it. The old source file path is a reference only, never input to a diagnostic dump/hash. Bootstrap into a generation store must preserve the existing file and use an opaque trusted copy with private metadata validation; no overwrite or deletion of the user's working login. Ownership and filesystem checks precede any live operation.

The brief's wording about both stopped checks refers to preserving historical failures, not inventing failures for a new refresh check. Task3 typecheck/review remain stopped at2 and are unrelated. Refresh gets a stable declared check identity before its first actual exchange; failed/ambiguous exchange may consume a rotating token and cannot be blindly retried even when the general two-failure limit has not been reached. Existing candidate recovery is not a second exchange.

The ordinary runtime covering suite runs once after the final command-builder change, with focused tests during iteration; no package-wide rerun after every edit. All source refactoring must be necessary to this operation and reviewable with unchanged command boundary evidence.

Pinned schema is now established in task-5-refresh-schema-proof.md from immutable image entrypoint SHA256 7d25d7c8ae6c6e009cc7dae4e817f674179fd31fb7761bcd56fee4c2902b4c03, Claude2.1.263: require own top-level claudeAiOauth object, nonempty string refreshToken, nonempty scopes string array. Each scope must be nonempty and whitespace-free, with no trimming/normalization; join with one space preserves the CLI split contract. A dead-token empty string refuses before exchange. No real credential was read for this determination.

Ruling: preserve optional own claudeAiOauth.clientId through CLAUDE_CODE_OAUTH_CLIENT_ID only in the same fixed child's in-container environment. If present, require a nonempty string, <=256 UTF-8 bytes and no whitespace/control characters; absence leaves the pinned CLI default intact. Do not expose or hash the value; unrelated environment remains excluded. The pinned artifact proves this exact optional input, so silently discarding it would change the OAuth client identity. No caller may select an endpoint/provider or pass other environment settings. Cost if the conservative bound rejects a legitimate custom client: fail before exchange and report unsupported credential shape; do not coerce it to a default or retry.

Supplement during implementation at945397d: add an explicit opaque bootstrap entrypoint in credential_refresh.py within the existing scope, with synthetic tests in test_credential_refresh.py. It initializes only a new store/current pointer under the same exclusive lock, metadata-validates source and all destinations, copies bytes without host JSON parsing/hashing/logging, preserves the original source, refuses existing ambiguous state, and fsyncs before publication. Live bootstrap remains root-only after review. current.json is exactly {"generation":"<32 lowercase hexadecimal characters>"}. This is necessary to make initial live adoption concrete; it is not a general credential-store editor.

Model acceptance evidence correction: the fixed explicit claude-opus-4-6 argv supplies selected-model authority; require exact fixed result, is_error false and modelUsage["claude-opus-4-6"].canonicalModel=="claude-opus-4-6". The already accepted proof includes auxiliary claude-haiku-4-5-20251001 usage, which is permitted and recorded rather than hidden. modelUsage alone is not claimed to prove which model was selected. Preserve allowlisted usage/list-price estimate, not billing claims. Accepted non-secret proof: docs/reports/evidence/2026-09-13-autonomy/claude-persistent-auth/model-auth-proof.json.

Immutable4400B headless branch source window at entrypoint offset198957043 is saved in task-5-refresh-headless-source-window.txt, SHA256 ca0f73f462867fbb08f919de56f191b75979d46a2f4edb2cfb7d92bd4273bf7c, read from the same pinned image with no network/mounts. The refresh-branch catch emits fixed prefix "Login failed: " then opaque error text; browser branch includes "Opening browser to sign in", URL prompt and "Paste code here if prompted > ". Classification must expose only fixed prefix-present booleans/enums, never captured response/error strings. An unrelated early exit does not prove attempted exchange.
