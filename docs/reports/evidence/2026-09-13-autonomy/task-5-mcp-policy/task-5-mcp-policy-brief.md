# Task 5: verified reviewer MCP policy and container canary

Base: d4bf8b898217196b0450d174d0a2a90550a4e66c. Sole implementation writer, execution worktree only. User explicitly requests closing remaining autonomy blockers. This is a bounded prerequisite, not full native acceptance.

## Binding constraints

Сохранить исходное вложение, старые документы и уникальные правила. Не править журнал владельца и продуктовые ADR. Не менять продуктовые статусы без критериев и evidence. Один implementer одновременно; независимый reviewer не создаёт проверяемую реализацию. Две подряд неуспешные попытки одной проверки останавливают эту проверку; счёт не сбрасывается новым run. Продуктовые изменения в server/web/packages имеют одну активную цепочку. Проверки привязаны к точной ревизии и dirty diff. Не читать и не выводить секреты. Сохранять существующие модели, расписания, состав сквада и ресурс in_place. Реальное ограничение runtime нельзя подменять промптом. Автопилоты включать только после применённых и проверенных изменений и выполненных чек-листов. Неподтверждённые возможности и стоимость обозначать неизвестными.

## Evidence and decision

Read docs/reports/evidence/2026-09-13-autonomy/d1-native/native-descriptor-proof.json: actual reviewer MCP is context7 HTTP https://mcp.context7.com/mcp with NO headers/env and inherited trace host-name command with exact [serve] argv, NO env. D1 native run expected78 succeeded, cleanup complete; no model acceptance claimed. Root verified immutable effective_args and provider-proxy after trace rejected execution-worktree paths.

Ruling: preserve the observed trace argv [serve] when mapping to pinned Linux /usr/local/bin/trace-mcp; do not silently replace it by the earlier proposed review preset. Preserve the existing explicit [serve,--preset,review] standalone policy for existing callers. Context7 gains only its exact observed public endpoint. This resolves the design-only native-brief trace preset proposal against actual preservation evidence. Unknown/server/field/header/env/URL variants must fail before a model starts. Endpoint validation is exact, including absence of query/fragment/userinfo/port changes. Empty policy remains valid only where existing tests/contracts explicitly require it; no inference of full roster preservation from this local policy validator.

## Implementation scope

Owned existing files: scripts/agent-runtime/runtime.py, scripts/agent-runtime/provider-proxy.mjs, scripts/agent-runtime/test_runtime.py, scripts/agent-runtime/test_protocol_guard.mjs ONLY if that is the existing proxy-test home (otherwise report actual existing test path before editing), docs/development/runtime-isolation.md. New focused proxy test and no-model MCP canary files under scripts/agent-runtime may be added with exact names recorded before edit. No D1, auth, Dockerfile/package/image, native adapter, gate, product, or Multica mutations. Root owns pending d1-native archive; never stage it.

1. Meaningful RED then minimal implementation: strict known server names, trace stdio exact Linux command with the two explicitly supported argv vectors; context7 exact HTTP type/url only. Reject extra fields, empty/malformed args, altered endpoint, headers/env and unknown names/types. Preserve byte/argv semantics for supported inputs. Tighten malformed JSON handling if needed without unrelated refactoring; no secret content in errors.
2. Add only mcp.context7.com to the provider proxy exact CONNECT hostname set; existing DNS/IP/port/redirect-target refusal and isolated network boundaries remain intact. Reuse existing proxy tests where possible, meaningful exact-host and deceptive/alternate authority negatives.
3. Add a reproducible no-model Docker canary using the existing immutable image and network/RO policy mechanism, no credential mount or host source writes. Perform Context7 MCP initialize, tools/list and one read-only resolve-library-id request for a public library with a synthetic query (no private project content). Verify actual tool result, not just HTTP connectivity. Perform trace map/outline against a synthetic source snapshot using [serve], documenting exact tool IDs and successful results. No model, no API key, no package download, no host config/home/socket. Capture bounded non-secret protocol proof and exit status. Check forbidden CONNECT destinations still reject; reuse verified current OS boundary setup rather than broadening network for tests. Any external auth/rate/error blocker is recorded, not bypassed by endpoint/model change.
4. Run focused suites once on final source and self-review. Record exact commands/output, source/policy/image identities and all actual failures in task-5-mcp-policy-report.md. Two real consecutive failures stop that check; declared expected RED excluded. Prepare immutable scoped diff for root review. Do not rerun unrelated stopped Task3 checks.

## Completion / coordination

One writer; do not spawn reviewers/subagents. Commit only owned files after root has approved exact file list if it differs from this brief, with normal hooks; root does not mutate index/HEAD while you implement. Node /Users/egorbondarenko/.nvm/versions/node/v24.12.0/bin, pnpm10.18.3. Existing image sha256:0baed89d66accc9338e938d6c0a81924014836561890d12005063b0b7bdb409a. Source/guard/proxy are policy mounts, so no image rebuild for them alone. Escalate bounded Docker/network operations normally if sandbox requires it; never run host Claude/Codex or inspect auth.

Return DONE/BLOCKED/NEEDS_CONTEXT with commit, covering results, remaining gaps and report path. Passing this milestone does not authorize binding a native profile or mark full Task5 complete. Native adapter, Codex config discovery, tester Playwright, context/hooks, refresh and trusted collector remain separate.
