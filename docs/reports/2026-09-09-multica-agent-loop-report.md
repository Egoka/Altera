# Отчёт: автономный цикл Multica — доступ к коду, очередь, лёгкий конфиг

- **План**: `docs/plans/2026-09-09-multica-agent-loop.md`
- **Дата работ**: 2026-09-09 — 2026-09-10
- **Ветка**: `docs/platform-design`
- **Базовый коммит**: `82e1abd`
- **Коммиты захода**: `642fcfb`, `e354901`, плюс коммит этого отчёта
- **Статус**: выполнен частично — три пункта перенесены владельцу, один отклонён осознанно

## 1. Что сделано

### Репозиторий

`CLAUDE.md` и `docs/multica/operating-model.md` §7 приведены к разрешению владельца об автономии
от 2026-09-05, подтверждённому 2026-09-09. Push, слияние в `app` и выкладка убраны из списка
действий, требующих решения владельца; условия выпуска сохранены — релиз-инженер, независимое
ревью актуальной ревизии, целевое окружение, состояние миграций, план восстановления.

Права харнесса `.claude/settings.json`: `Bash(git push:*)` перенесён из `deny`. **Эту правку
сделал владелец лично** — мне защита харнесса не позволяет расширять собственные права, и я
на этом остановился, а не искал обход. Запреты на `git reset --hard`, `git clean -fd` и чтение
файлов окружения сохранены.

`docs/multica/autopilot-checklists.md` заполнен фактическими отметками: 14 «да», 4 «нет»,
2 «частично», с указанием, чем каждый пункт подтверждён.

### GitHub

Ветка `docs/platform-design` запушена в origin (69 коммитов, 603 файла), открыт pull request
[#16](https://github.com/Egoka/Altera/pull/16) в `app`. Force-push не использовался.

### Multica

**Описание проекта** переписано целиком. Прежнее было срезом от 2026-09-05 (`server/api`,
`e1b9a97`) и утверждало «no tests»; оно раздавалось как контекст каждому запуску. Новое — по
фактическому состоянию, по-русски, с датой среза, стеком, честными проверками, списком
отсутствующего и порядком работ.

**Навык `altera-project-rules`** создан и подключён всем десяти агентам. Он держит то, что нужно
знать до чтения репозитория: старшинство источников, ритуал плана и отчёта, стадии и evidence,
бюджет попыток, команды проверки, ветки и коммиты, правило секретов.

**Десять промптов ролей переписаны.** Из них убраны: разбор инцидента маршрутизации `ALTE-2`,
привязка к `CLI v0.4.40` (установлена 0.4.42), инвариант «исходная папка на `server/api`»,
«подтверждённая базовая ветка — app», «Express 5» и утверждение, что у `web` и `server` нет
`test`-скриптов. Число попыток приведено к двум по операционной модели §5.

| Роль | Было, символов | Стало |
|---|---|---|
| Оркестратор | 9257 | 3671 |
| Архитектор | 4465 | 2531 |
| Разработчик | 4490 | ~2900 |
| Дизайнер | ~4400 | 2189 |
| Редактор | ~4400 | 2255 |
| Инструкции сквада | 9875 | ~3400 |

**Три автопилота** починены. У очереди убрана ссылка на `metadata ready=true` — таких свойств в
workspace не было, условие было невыполнимым; гейтом остался статус `Todo`. У восстановления
«три попытки» заменены на две. Из всех трёх убран абзац с разрешением, заменён ссылкой на §7.

**Свойство задач** `Задача бэклога` (текст) заведено: 1 из 20.

**Очередь наполнена.** Семь задач заведены как ALTE-4…ALTE-10 в статусе Backlog, каждая по
scoped-схеме: цель, границы и что не трогать, источники, критерии, как проверить, роли. Тело
задачи не пересказывает файл, а ссылается на `docs/backlog/tasks/T-NNN-*.md`.

**ALTE-2 и ALTE-3 закрыты** в `Done` с комментарием: результат `ebb0305` вошёл в историю и
является предком текущего `HEAD`; устаревшие требования постановки названы явно, чтобы не
воспроизводились.

## 2. Как проверено

**Проверки репозитория** — при каждом коммите, живым pre-commit:

```
pnpm format:fix && pnpm lint && pnpm test
```

Вывод: `All matched files use Prettier code style` дважды; `lint` без диагностик;
`server test: Test Files 2 passed (2), Tests 19 passed (19)`,
`web test: Test Files 1 passed (1), Tests 5 passed (5)` — 24 теста.

**Соответствие бэклога**: 112 файлов задач, 112 идентификаторов в `matrix.md`, расхождений нет.

**Вхождение результата ALTE-3 в историю**:

```
git merge-base --is-ancestor ebb0305 HEAD   # код возврата 0
```

**Push и PR**: `git push -u origin docs/platform-design` — `* [new branch]`;
`gh pr create --base app` вернул адрес PR #16.

**Multica** — перечитыванием каждого поля после сохранения:

- Описание проекта: 1986 символов нового текста после перезагрузки страницы.
- Промпты ролей: поиск по `ALTE-2`, `server/api`, `Express 5`, `0.4.40`, `трёх попыток`,
  `базовая ветка — app` даёт ноль совпадений; единственное вхождение слова Express — во фразе
  «Express в проекте нет».
- Инструкции сквада: 4522 символа, `Две подряд неуспешные` присутствует, старых маркеров нет.
- Автопилоты: у очереди `ready=true` отсутствует, у восстановления `трёх неудачных` отсутствует
  и присутствует `две подряд неуспешные`, у всех трёх нет абзаца от 5 сентября.
- Навык: `Used by 10 agents`, все десять ролей перечислены.
- Доска: Backlog 7, Todo 0, In Progress 0, In Review 0, Done 2.

## 3. Отклонения от плана

**Ресурс локальной папки не удалён.** План (шаг B5) предполагал убрать его после подключения
GitHub. Ресурс `/Users/egorbondarenko/WebstormProjects/Altera` по-прежнему отдаёт «Local daemon
isn't reporting an ID yet — the agent can't open this directory» и отрисован приглушённым.
Я его оставил: подтверждения, что GitHub-путь чекаута реально работает, у меня нет — список
`Settings → Repositories` не показал строку репозитория, хотя диалог выбора помечает
`Egoka/Altera` как «Added». Удалять единственный второй указатель на дерево до проверки первого
неразумно. Решение владельца.

**`form_input` дописал вместо замены** в редакторе промпта автопилота «актуальность контекста»:
после первой попытки поле содержало новый и старый текст подряд. Обнаружено перечитыванием после
перезагрузки, исправлено повторной записью; проверено — 1897 символов, старого абзаца нет.
Остальные два автопилота этой ошибки не получили, проверено тем же способом.

**Шесть задач создались без проекта.** Режим «Create another» сбрасывал поле проекта после
первой задачи. Обнаружено по счётчику `Progress 0/3` на доске; проект проставлен вручную каждой
из шести, итог — `Progress 0/9` и Backlog 7.

## 4. Что не сделано и почему

- **Слияние PR #16** — вынесено владельцу. На `origin/app` два коммита (`308206d`, `f5f3ea7`),
  которых нет в ветке; их содержимое я не читал, а сливать 69 своих коммитов поверх
  непрочитанного — ровно тот шаг, ради предотвращения которого существует ревью.
- **Включение автопилотов** — не выполнено. До подтверждённого доступа к коду включать очередь
  бессмысленно: она либо не найдёт дерева, либо возьмётся за задачу и упрётся в ту же стену.
- **Перевод T-110 в `Todo`** — по той же причине. Это и есть первый запуск, и он должен идти при
  работающем доступе к коду и наблюдении.
- **Чистка навыков** (четыре `laravel-*`) и **назначение MCP** (`playwright` тестировщику,
  `context7` архитектору и разработчику) — не выполнены, перенесены.
- **Поле «About you»** в профиле владельца остаётся пустым.

## 5. Ограничения, которые остаются

- Цикл работает, пока включён MacBook-Air-Egor.local: рантаймы Claude, Codex и Cursor — на одной
  машине, облачных воркеров нет.
- Бюджет попыток — текстовое правило; счётчик платформой не проверяется.
- Единственный writer продуктового кода — тоже правило, а не блокировка.
- `pnpm run smoke` подтверждает старт сервера и ответ GraphQL, но не связность с Redis; для
  этого нужен эндпоинт `/health` (эпик E-13).
