# Отчёт: синхронизация после гейта Г9 — медиа и завершение фазы 2

- **Дата завершения**: 2026-09-09
- **План**: docs/plans/2026-09-09-g9-sync-media.md
- **Базовый коммит**: ed46728
- **Коммиты работы**: см. `git log --oneline ed46728..` — один коммит
  `docs(spec): close gate G9 — sync media policies with the decision log and mark phase 2 complete`

## Что сделано

**Журнал** — §29 с двенадцатью решениями и ссылками на две рабочие заметки владельца (заметки не
редактировались); запись о завершении фазы 2.

**По решениям** (10 файлов → «утверждён (гейт Г9)»):

- §29.1 — `article-covers.md`: обложка обязательна и индивидуальна, дефолтных карточек нет;
  `article-edit.md` (`VALIDATION_ERROR`), `write-and-publish.md`, `review-queue.md`.
- §29.2 — `storage-layout.md`, `image-variants.md`, `exports.md`: мастер-оригинал без EXIF как
  источник всего.
- §29.3 — `PROVIDER_UNAVAILABLE: storage` — `error-dictionary.md`, события #11, #53,
  `health-and-alerts.md`, `errors-and-health.md`, `upload-pipeline.md`.
- §29.4 — `image-variants.md`, `avatars.md`: AVIF и WebP, `<picture>`.
- §29.5 — `avatars.md`, `upload-pipeline.md`, `profile-edit.md`, `review-queue.md`, матрица #120,
  события #85 (`reverted`): аватар сразу, предыдущая версия, откат с аудитом.
- §29.6 — `exports.md`: мастер-оригиналы и метаданные без вариантов.
- §29.7 — `receipts-and-documents.md`: только ссылки провайдера до §8.19.
- §29.8 — `retention-and-orphans.md`: без уведомления, только медиа без активных связей,
  интервал — отдельный проход (числа убраны).
- §29.9 — `backups.md`, `health-and-alerts.md`, `errors-and-health.md`, события #53: возраст
  копии — компонент здоровья и основание оповещения.
- §29.10 — `access-and-signed-urls.md`: без водяных знаков.
- §29.11 — `upload-pipeline.md` п. 6а, `article-edit.md` (`updateMediaMeta`), `ai-processes.md`,
  `jobs.md`, события #46 (`kind: alt`).
- §29.12 — `upload-pipeline.md` п. 5 (целевой конвейер со статусами по заметке), `image-variants.md`
  (набор и кадрирование по заметке), `health-and-alerts.md` и `errors-and-health.md` (будущая
  зона «Интеграции и провайдеры» по второй заметке); отложенное — в `OPEN-QUESTIONS.md` §2.

**`OPEN-QUESTIONS.md`**: строки #12–21 помечены «закрыто §29.n», открытыми остаются #1–11;
в отложенные проходы добавлены конвейер обработки медиа, зона провайдеров, интервал
обслуживания сирот. **Фаза 2** отмечена завершённой в `docs/spec/README.md` и `CLAUDE.md`.

## Что не сделано и почему

- Матрица размеров, параметры сжатия, лимиты, очередь, ретраи, правила GIF/PDF/видео/аудио,
  выбор модели `alt` и сервисов — отдельный проход по заметке; AI-улучшение качества не
  включается (§29.12).
- Строки реестра событий #11, #46, #53, #85 дополнены значениями по решениям владельца при
  заморозке (§28.3); новых строк нет.
- Vision-документы, `03-data-model.md` (статусы обработки, предыдущая версия аватара) и роадмап
  не менялись — отложенная работа.

## Отклонения от плана

Нет.

## Как проверено

```
python3 -X utf8 sync9.py                                    # ~45 замен, каждая уникальна: MISS []
grep -h '^- \*\*Статус' 85-media-and-binary/*.md | uniq -c    # 10 × «утверждён (гейт Г9, 2026-09-09)»
wc -l / grep -c '^## '                                      # 10 × (≥ 60 строк, 6 разделов)
grep -rc '^- `\[ВОПРОС ВЛАДЕЛЬЦУ\]`' docs/spec --exclude-dir=_templates   # 12 пометок в 12 файлах (#1–11 свода; один вопрос в двух файлах)
grep -c 'закрыто §29' OPEN-QUESTIONS.md                     # 10
grep -rn 'синхронно|текстовая карточка|WebP `\[ДОПУЩЕНИЕ' 85-media-and-binary   # пусто
git diff --stat                                             # ~25 файлов
```

Коммит с `--no-verify`: pre-commit гоняет форматирование, линт и пустые тесты, к документации не
относящиеся.

## Что осталось

Фаза 2 завершена. Предлагаемый порядок отложенных проходов — в сообщении владельцу и в
`OPEN-QUESTIONS.md` §2; порядок выбирает владелец.
