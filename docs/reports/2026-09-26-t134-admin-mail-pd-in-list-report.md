# Отчёт T-134: письма /admin/mail без ПД в списке и без обхода аудита карточки

- **Дата**: 2026-09-26
- **Задача**: `docs/backlog/tasks/T-134-admin-mail-pd-in-list.md`, blob
  `784ca04d841de918f1339dc16d72e5a2d90dfb61`
- **Карточка**: ALTE-95 (`01a0bc94-460c-730a-9599-6837e45589fe`) — доработка T-080, возвращённой
  независимым ревью 2026-09-26
- **План**: `docs/plans/2026-09-26-t134-admin-mail-pd-in-list.md`
- **Baseline**: `f1ed1885f5be4a364b14ccb442412cfbd13e18b7`
- **Tested SHA**: `44b70286a1d28ca02a65f9c2171f98a6d93be6c6`
- **PR**: [#230](https://github.com/Egoka/Altera/pull/230), merge `9553d2f4c1b32dcd4831ff6f02ae51583b5a6562`
- **Ветка / worktree**: `server/t134-admin-mail-pd` / `.worktrees/t134-admin-mail`

## 1. Что вернуло ревью и что закрыто

| Находка ревью 2026-09-26                                                                                          | Класс  | Что сделано                                                                                                           |
| ----------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------- |
| `listAdminMails` отдаёт полный `recipientEmail` без `admin.read.personal` (журнал §28.7 перекрывает `mail.md` §8) | блок   | Список отдаёт маску `a***e@example.com`; полный адрес — только карточка, чьё открытие пишет `admin.read.personal`     |
| `body` в списке — обход аудита карточки                                                                           | блок   | `body` в списке всегда `null` независимо от роли и шаблона                                                            |
| `SECRET_MAIL_TEMPLATES` без `email_change_code` → повтор письма с заглушкой кода                                  | неблок | Шаблон добавлен в набор секретов; повтор даёт `FORBIDDEN`, `canResend = false`                                        |
| Повтор не атомарен                                                                                                | неблок | Заявка на повтор — условный `UPDATE ... WHERE "resentAt" IS NULL`; из двух параллельных повторов отправку делает один |
| Повтор `support_request_staff` бесполезен                                                                         | неблок | Заведён `REDACTED_COPY_MAIL_TEMPLATES`: письмо с сокращённой копией не повторяется                                    |
| Кнопки по `role == owner`, сервер по `job.retry`                                                                  | неблок | Сервер отдаёт `viewerCanResend` и `canResend`; страницы больше не читают роль                                         |

Спецификация `docs/spec/40-admin/mail.md` синхронизирована с журналом §28.7: §1, §3 и §8 описывают
маску в списке, §5 — предусловия повтора (право `job.retry`, отсутствие прошлого повтора, полная
копия), §12 называет отменённым прежнее правило «полный адрес получателя в списке». Новых
продуктовых правил, чисел и сроков не вводилось.

## 2. Критерии §5

| AC                                                                                      | Результат           | Доказательство                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| AC-1 `adminMails { items { body } }` возвращает `null` для всех строк                   | passed              | `server/tests/admin-mail.test.ts` — «masks the address and hides the body from the list for analyst, admin and owner», «never returns the stored copy in the list, even for a secret-free template»; `web/tests/e2e/22-admin-mail.spec.ts` — таблица списка не содержит текста копии |
| AC-2 открытие списка с полным адресом пишет `admin.read.personal` либо адрес маскирован | passed (маскирован) | `server/tests/admin-mail.test.ts` — маска в списке и `auditLog.create` не вызван; карточка отдаёт полный адрес и пишет аудит; `web/tests/admin-mail-page.nuxt.test.ts` — маска и пояснение в списке; e2e — маска в списке, полный адрес на карточке                                  |
| AC-3 письмо `email_change_code` не повторяется                                          | passed              | `server/tests/admin-mail-secrets.test.ts` — копия без кода, `canResend = false`, мутация даёт `FORBIDDEN`; `admin-mail.test.ts` — `it.each` по `magic_link` и `email_change_code`; e2e — карточка кода без кнопки повтора                                                            |
| AC-4 два параллельных повтора дают одну отправку (PostgreSQL)                           | passed              | `server/tests/mail-resend-database.test.ts` — 2 passed; в CI джоб «Проверить повтор письма T-134» на PostgreSQL 17: `Test Files 1 passed`, `Tests 2 passed` (run 36263253435, job 108462960107)                                                                                      |

RED для AC-4 наблюдался явно: со снятым условием `"resentAt" IS NULL` в `WHERE` тот же тест даёт
две отправки (`expected 2 to be 1`). Условие возвращено, тест снова зелёный.

## 3. Проверки на tested SHA `44b7028`

| Команда                                                                                            | exit | Результат                                                                                                                                             |
| -------------------------------------------------------------------------------------------------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm install --frozen-lockfile`                                                                   | 0    | —                                                                                                                                                     |
| `pnpm format`                                                                                      | 0    | —                                                                                                                                                     |
| `pnpm lint`                                                                                        | 0    | —                                                                                                                                                     |
| `pnpm test`                                                                                        | 0    | server 805 passed / 32 skipped, web 572 passed                                                                                                        |
| `pnpm codegen --check`                                                                             | 0    | дрейфа нет                                                                                                                                            |
| `pnpm --filter server run build:ci`                                                                | 0    | —                                                                                                                                                     |
| `pnpm --filter nuxt-app run typecheck`                                                             | 0    | —                                                                                                                                                     |
| `pnpm --filter nuxt-app run build`                                                                 | 0    | —                                                                                                                                                     |
| `T134_TEST_DATABASE_URL=… pnpm --filter server exec vitest run tests/mail-resend-database.test.ts` | 0    | 2 passed                                                                                                                                              |
| `prisma migrate deploy` на пустой базе                                                             | 0    | миграция применяется поверх всех прежних                                                                                                              |
| `pnpm exec playwright test --project=chromium --grep "admin mail"`                                 | 0    | 21 passed                                                                                                                                             |
| Агрегат CI `test` на tested SHA                                                                    | pass | run 36263253435: «Формат, линт и тесты», «Сборка и типизация веба», «Сборка сервера и дымовая проверка старта», «Браузерная проверка веба» — все pass |

## 4. Границы проверок

- Локальные проверки с базой шли на PostgreSQL 16 из `docker-compose.yml` (порт 25432); в CI — 17.
  Каждый прогон работал в своей базе и удалял её за собой; общие базы и продовые адреса не
  использовались, новые платные окружения не подставлялись.
- Локальный прогон браузерных сценариев ограничен `--grep "admin mail"` (21 сценарий): полный набор
  выполняет джоб «Браузерная проверка веба», он зелёный на tested SHA.
- Отчёт не подтверждает выкладку: probe Render и `/health` того же SHA выполняет отдельный актор.
- `verified` и `accepted_at` в receipt не заполнены: их назначает контроллер.

## 5. Остаток

- Независимое ревью на финальном head с маркером `ALTERA_REVIEW_V1` в этом заходе не проводилось —
  исполнитель свою работу не принимает.
- После приёмки T-134 возвращённая T-080 проходит повторную приёмку в своей карточке ALTE-95.
- Обезличивание копий писем при удалении аккаунта (журнал §28.8) остаётся за `/me/delete` (§35 п. 1)
  и в эту задачу не входило.

Receipt: `docs/reports/tasks/T-134.json`, канонический итог — `docs/reports/tasks/T-134.md`.
