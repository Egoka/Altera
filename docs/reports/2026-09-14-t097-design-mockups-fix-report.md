# T-097: Исправление макетов по замечаниям ревью — Отчёт

- **Дата**: 2026-09-14
- **Ветка**: docs/adr-0047-plus
- **Задача**: T-097
- **Базовая ревизия**: a9b3435
- **Коммит плана**: a40b49c
- **Коммит исправлений**: e5b3758
- **Коммит отчёта**: (текущий)
- **Исполнитель**: Altera — дизайнер (agent 8c80740a)
- **План**: docs/plans/2026-09-14-t097-design-mockups-fix.md

## Что исправлено

### 1. Удалена «Зона 4: лента рубрик» из desktop-диаграммы §1

Строка `│ Зона 4: лента рубрик (по одной секции на рубрику) │` и разделитель удалены из DESKTOP-блока главной страницы.

Основание: `docs/spec/20-public/home.md` §12 — «Закрыто Г3 (журнал §20.1, §20.3): секций по рубрикам нет». Главная содержит ровно три подборки (топ, «Новое», «Популярное»).

Сопутствующее: в строке #1 «Загрузка» таблицы состояний §1 текст изменён с «зон 2–4» на «зон 1–3» (зона 4 больше не существует в диаграмме).

### 2. Добавлена точка останова tablet в §9 (Сводка админки)

Блок `MOBILE (< 768)` заменён на `TABLET (md, 768–1023) + MOBILE (< 768)` с явной фиксацией совпадения поведения: карточки в одну колонку, AdminShell боковое меню — выдвижной drawer.

### 3. Добавлены пропущенные строки в таблицы покрытия

| Таблица | Добавленная строка |
|---|---|
| Главная | Ограничение плана — н/п (план не влияет на чтение) |
| Лента рубрики | Ограничение плана — н/п; Paywall — зарезервировано |
| Кабинет | Не найдено / удалено — н/п; Paywall — зарезервировано |
| Редактор-обвязка | Paywall — зарезервировано |
| Сводка админки | Нет прав на часть действий — н/п; Конфликт — н/п; Лимит / превышение — н/п |

Также унифицированы пометки «н/д» → «н/п», обновлена итоговая таблица «Итого по покрытию» (77 строк вместо 68).

## Как проверено

1. Для каждого из трёх пунктов проверена соответствующая строка спецификации:
   - §1 удаление: home.md §12 «секций по рубрикам нет», §1 «три подборки»
   - §9 tablet: admin/dashboard.md §10 «Мобильный — карточки в одну колонку; ничего не скрывается»
   - Пропущенные строки: сверены с spec §8/§9 каждой страницы по пунктам из задания оркестратора

2. Diff проверен визуально: только целевые изменения, посторонних правок нет.

3. Команды и вывод:
   - `pnpm format:fix` → exit 0
   - `pnpm lint` → exit 0
   - `pnpm test` → exit 0 (server 19 passed, web 56 passed)

4. Коммит e5b3758: format exit 0, lint exit 0, test exit 0.

## Дефекты и ограничения

Нет. Все три пункта из задания оркестратора выполнены.

## Следующий шаг

Артефакт готов к повторному независимому ревью.
