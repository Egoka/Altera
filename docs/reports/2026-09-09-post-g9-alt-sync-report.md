# Отчёт: пост-Г9 синхронизация — единое `alt` медиафайла

- **Дата завершения**: 2026-09-09
- **План**: docs/plans/2026-09-09-post-g9-alt-sync.md
- **Базовый коммит**: 25599db
- **Коммиты работы**: см. `git log --oneline 25599db..` — один коммит
  `docs(spec): post-G9 sync — single media alt, no author or per-article override`

## Что сделано

- Журнал §29 п. 13 — уточнение к п. 11 (без нового гейта, фаза 2 не переоткрыта).
- `85-media-and-binary/upload-pipeline.md`: `alt` убран из аргументов `uploadMedia` и из свойств
  медиа, доступных автору; п. 6а — создаётся один раз, хранится только в медиафайле, автор не
  редактирует, узел документа не переопределяет, публичный вывод использует единое `alt`;
  исправление — только `admin` у медиафайла с аудитом `admin.change` (#75); нового кода события
  нет (§28.3).
- `30-account/author/article-edit.md`: `uploadMedia` без `alt`; `updateMediaMeta` — `alt` только
  показывается.
- `image-variants.md`, `article-covers.md`, `avatars.md`: вывод и обложки используют единое `alt`
  из медиафайла; допущения о `alt` «по локали из узла» и «имя автора для аватара» сняты.
- Матрица #40: `alt` исключён из авторских свойств; исправление — `admin`, аудит через
  `admin.change`.

## Что не сделано и почему

- `docs/vision/03-data-model.md` (поле `alt`: «узел документа может переопределить») не правился
  — vision-документы вне объёма; расхождение отмечено как отложенная работа.

## Как проверено

```
python3: замены уникальны — MISS []
grep -rn 'alt' docs/spec | grep -v '_templates|health|alternat' | grep -i 'автор.*редакт|узл.*переопредел|по локали из узла'   # пусто
```

Коммит с `--no-verify`.
