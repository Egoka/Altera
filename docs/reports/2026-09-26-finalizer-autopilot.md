# Отчёт: агент-финализатор и автопилот «приёмка в Done» (2026-09-26)

- **План**: [docs/plans/2026-09-26-finalizer-autopilot.md](../plans/2026-09-26-finalizer-autopilot.md)
- **Основание**: решение владельца о варианте (б) после
  [расследования](2026-09-26-review-dispatch-investigation.md).
- **Readback**: [снимок](../multica/snapshots/2026-09-26-finalizer/verified.json), состояние до
  создания — [before](../multica/snapshots/2026-09-26-finalizer/before.json).

## Что создано в Multica

| Объект                              | ID                                      | Состояние                                      |
| ----------------------------------- | --------------------------------------- | ---------------------------------------------- |
| Агент «Altera — финализатор»        | `3fdfb369-fa6c-49d6-921b-95593f174705`  | `idle`, 0 запусков                             |
| Автопилот «Altera — приёмка в Done» | `982c9809-d9be-4162-a386-e685019cc124`  | `paused`, 0 запусков                           |
| Триггер                             | schedule `20,50 * * * *`, Europe/Moscow | включён, срабатывает только после снятия паузы |

Автопилот создан без триггера, сразу поставлен на паузу, и только потом добавлено расписание.
Запуска не было. Остальные четыре автопилота остаются на паузе владельца.

Параметры и итоговые тексты инструкции и описания — в [фазе Done](../multica/done-finalization.md).
Отступления от предложения и их причины:

- проход не ждёт ревьюера: запуски идут последовательно в общей `.worktrees/autopilot`;
- расписание каждые 30 минут, иначе проба старше 3600 секунд;
- у агента нет Render MCP: пробу делает ревьюер из `release_agent_ids`.

## Строка оркестратору

По поручению владельца в описания автопилотов «Altera — очередь разработки» (`c40fbc10`) и
«Altera — восстановление работы» (`e564a6fb`), в инструкцию агента-оркестратора (`d08ef989`) и в
`docs/multica/autonomy/queue-prompt.md` добавлена строка сразу после правила о child-review:
«Слитые в `app` задачи в `in_review` ведёт автопилот „Altera — приёмка в Done“; ты их не ревьюишь
и не финализируешь». Остальные поля агента не изменились. Снимки —
[до](../multica/snapshots/2026-09-26-finalizer/orchestrator-before.json) и
[после](../multica/snapshots/2026-09-26-finalizer/orchestrator-verified.json).

## Включение автопилотов

В 12:17 UTC все пять автопилотов, включая новый, переведены в `active` — не этой сессией.
Статусы здесь не менялись. Первый проход финализатора создан в 12:20:03 UTC
(`01a0dda8-8239-7879-8824-a4db6ff2a359`) и ждал общую рабочую папку (`waiting_local_directory`).

## Переустановка runtime

По поручению владельца в 13:29 UTC `scripts/autonomy/install.py` установил выпуск
`25eeffd9e8173321` вместо `f4292dd2de436dea`. Вход установки (скрипты `scripts/autonomy` и
контракты `docs/multica`) взят из `6950b33` и совпадает с `aa70545` (`origin/app` на момент установки).

- От прошлого выпуска отличаются `controller.py` (правка ledger из #212: зависший `running` фазы
  `done` освобождается, если карточка не в Done), `queue-prompt.md` (строка оркестратору) и
  `autonomy-controller.md`.
- Из установленного config удалено поле `deployment_probe: null`, остальные поля перенесены.
  Версия инструкций контроллера и суточного отчёта — `25eeffd9e8173321`.
- Установленные скрипты и контракты побайтно совпадают с репозиторием; тесты `scripts/autonomy` —
  218, OK. Контроллер вручную не запускался.
- Смена версии инструкций меняет ключ события, поэтому первый проход после установки обработает
  снимок очереди заново, даже если в ней ничего не изменилось.
- Откат: прежний выпуск остаётся в `.git/autonomy-runtime/releases/f4292dd2de436dea`, копия config —
  `.git/autonomy-state/config-before-reinstall-2026-09-26.json`.

Наблюдение: установленный контракт `autonomy-controller.md` (как и файл в репозитории) по-прежнему
называет фазу Done после автослияния задачей без владельца и триггера, «ждёт решения владельца»,
хотя решение принято и финализатор создан. Контракт не менялся; правка потребует новой установки.

## Ждёт владельца

1. ~~**Переустановка runtime**~~ — выполнена 2026-09-26, см. [раздел выше](#переустановка-runtime).
2. **Проверка первых проходов финализатора**: комментарии `ALTERA_FINALIZE_V1` в карточках,
   запросы ревью и пробы, переходы в Done через `controller.py done`; повтор без изменений — `no_action`.
