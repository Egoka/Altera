## T-029: аудит чтения ПД — итог

**Task ID**: T-029  
**Tested SHA**: `256954685739374a363768b59fe47b3791d87665`  
**PR**: https://github.com/Egoka/Altera/pull/127  
**Baseline**: `7bec6f1d7049203c3ba7278183eea60becad7dd5`

### Что сделано

- Добавлен резолвер `adminUser(id: ID!): AccountUser` с проверкой права `accounts`.
- После каждого успешного чтения, возвращающего ПД, в таблицу `auditLog` записывается событие `admin.read.personal` с полями `actorId`, `actorRole`, `entityType: "user"`, `entityId`, `context`, `purpose`, `requestId` — сотрудник ничего не вводит.
- Существующий запрос `users` (список) обёрнут аналогично.
- Публичный тип `User` (используется как `Article.author`) не содержит поля `email` — moderator не получает email автора.

### Как проверено

- `server/tests/admin-pd-audit.test.ts` — 8 тестов, покрывают AC-1 и AC-2.
- Полный прогон сервера: 145 passed | 19 skipped (0 failed).
- Format, lint: exit 0.

### Критерии

| AC | Статус | Evidence |
|----|--------|----------|
| AC-1 | passed | `admin-pd-audit.test.ts`: adminUser создаёт auditLog запись |
| AC-2 | passed | `admin-pd-audit.test.ts`: Article.author не содержит email |

**Ожидает**: независимое review.
