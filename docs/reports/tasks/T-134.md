# T-134 — итог задачи

- **Task ID**: T-134
- **Карточка Multica**: ALTE-95 (`01a0bc94-460c-730a-9599-6837e45589fe`)
- **Задача**: T-134: Письма /admin/mail: без ПД в списке и без обхода аудита карточки
- **Спецификация**: `docs/backlog/tasks/T-134-admin-mail-pd-in-list.md` (blob `784ca04d841de918f1339dc16d72e5a2d90dfb61`)
- **Доработка для**: T-080 (ALTE-95), возвращена независимым ревью 2026-09-26
- **PR**: [#230](https://github.com/Egoka/Altera/pull/230), связанные: нет
- **Tested SHA (head PR)**: `44b70286a1d28ca02a65f9c2171f98a6d93be6c6`
- **Base SHA**: `f1ed1885f5be4a364b14ccb442412cfbd13e18b7`
- **Merge SHA в `app`**: `9553d2f4c1b32dcd4831ff6f02ae51583b5a6562` (автослияние GitHub по зелёному CI)
- **CI**: агрегат `test` на tested SHA — pass (run 36263253435)
- **Выкладка**: обязательна: PR меняет `server/` и миграции; probe Render и `/health` того же SHA выполняет отдельный актор

## Критерии

| AC   | Результат | Доказательство                                                                                                                                                                      |
| ---- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-1 | passed    | `server/src/admin/mail.ts` (`presentMail`, вид `list`); `server/tests/admin-mail.test.ts` — список без копии письма для любой роли и шаблона; e2e — таблица списка без текста копии |
| AC-2 | passed    | адрес в списке маскирован (`server/src/admin/personal-data.ts`); `server/tests/admin-mail.test.ts` — маска без записи аудита, карточка с полным адресом и `admin.read.personal`     |
| AC-3 | passed    | `SECRET_MAIL_TEMPLATES` с `email_change_code`; `server/tests/admin-mail-secrets.test.ts` — копия без кода, `canResend = false`, мутация `FORBIDDEN`                                 |
| AC-4 | passed    | условный `UPDATE ... WHERE "resentAt" IS NULL`; `server/tests/mail-resend-database.test.ts` — 2 passed локально и в CI на PostgreSQL 17 (job 108462960107)                          |

## Проверки

`pnpm format` exit 0; `pnpm lint` exit 0; `pnpm test` exit 0 (server 805 passed / 32 skipped,
web 572 passed); `pnpm codegen --check` exit 0; `pnpm --filter server run build:ci` exit 0;
`pnpm --filter nuxt-app run typecheck` exit 0; `pnpm --filter nuxt-app run build` exit 0;
`vitest run tests/mail-resend-database.test.ts` exit 0 (2 passed, PostgreSQL 16 из docker-compose,
своя база на прогон, удалена после прогона); `prisma migrate deploy` на пустой базе exit 0;
`playwright test --project=chromium --grep "admin mail"` exit 0 (21 passed); агрегат CI `test` pass.

Границы: локальная база — PostgreSQL 16, в CI — 17; локальный браузерный прогон ограничен
сценариями раздела писем, полный набор выполнил джоб «Браузерная проверка веба».

## Остаток

Независимое ревью финального head с маркером `ALTERA_REVIEW_V1` в этом заходе не выполнялось.
После приёмки T-134 возвращённая T-080 проходит повторную приёмку в своей карточке. Обезличивание
копий писем при удалении аккаунта (журнал §28.8) остаётся за `/me/delete`.

Отчёт захода: [docs/reports/2026-09-26-t134-admin-mail-pd-in-list-report.md](../2026-09-26-t134-admin-mail-pd-in-list-report.md).
