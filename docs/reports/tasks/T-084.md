# T-084 — Admin grants and subscriptions

- **Issue:** ALTE-82 (`01a0b5ee-a851-7891-b084-89bd16ffc07c`)
- **Source:** `docs/backlog/tasks/T-084-admin-grants-launch.md` @ `bb67f06498463bc3375e7ddff8bea4ad35facad3`
- **Baseline:** `a5f6c64eef712dceef8f36b2aea69f07a331ffd1`
- **Tested SHA:** `179d41917c8dca2319de05136b61f050e412d775`
- **Earlier tested SHA:** `8bed4761986ed167bf65149eac7699403fa0d852` (`c591e28` changed only this receipt); `179d4191` merges `app` 86a1022c into the branch, so the patch changed (see “Checks on the tested SHA”)
- **Branch:** `web/t084-admin-grants`
- **PR:** [#135](https://github.com/Egoka/Altera/pull/135), merged, `merge_sha` `3977168b5082c8e7849945fc17003f3983aeffbc` · [CI](https://github.com/Egoka/Altera/actions/runs/35455157891)
- **Implementer:** `b0f3bc32-dd95-471e-b40e-517aaf83edf0` · run `01a0b9d8-b39c-752a-8e63-ac9aaac60ec4`

## Result

- Added the finance-protected `adminGrants`, `adminGrant`, `grantPlan`, and `revokePlan` GraphQL vertical slice. Audit records use the authenticated `currentUser` actor.
- Connected `/admin/grants` and `/admin/subscriptions` to generated GraphQL operations and removed all mock grants and base first-launch authorship rows.
- Manual grants now require a finite `endsAt`; the indefinite scenario remains an explicit T-110 Playwright fixme.
- Corrected the FishtVue integration to use `data-source`, dynamic slot names, and `click-row`, so persisted rows, statuses, and detail panels render in the browser.

## Verification

- `pnpm format` — passed.
- `pnpm lint` — passed.
- `pnpm test` — server 164 passed / 20 skipped; web 160 passed.
- `pnpm --filter server run build:ci` — passed without migrations.
- `pnpm --filter nuxt-app run typecheck` — passed.
- `pnpm --filter nuxt-app run build` — passed.
- `pnpm --filter nuxt-app exec playwright test tests/e2e/17-admin-grants.spec.ts` — 4 passed / 1 T-110 fixme skipped.

Preflight passed repository and runtime checks. `LOG_HASH_SECRET` was unavailable locally, so no live server/database probe or migration was run.

The numbers above were taken on `8bed4761`.

## Checks on the tested SHA

- `179d4191` resolves the conflict in `web/app/graphql/generated/schema.graphql` by regenerating `web/app/graphql/generated/` with `pnpm codegen` from the merged server schema.
- CI on `179d4191` (workflow Checks, run 35455157891, 2026-09-19): aggregate `test` — success.
- Locally on the same tree: format, lint, `pnpm codegen --check`, `build:ci`, web typecheck — passed; `pnpm test` — server 200 passed / 22 skipped, web 161 passed.
- Patch fingerprint by the controller rule (`git patch-id --stable` from the merge-base with base `86a1022c`, excluding this receipt): `8bed4761` and `c591e28` — `f68fc1b52286…`, `179d4191` — `50c3a915e1d0…`.

## Remaining

Independent native review of the tested SHA `179d4191` and controller-managed development deployment/health verification remain pending. The PR is merged; Done goes only through the controller. `verified` and `accepted_at` are intentionally unset for the controller.
