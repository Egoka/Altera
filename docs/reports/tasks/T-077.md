# T-077 — Background jobs section `/admin/jobs`

- **Issue:** ALTE-96 (`01a0bd72-60b3-7134-8705-cae37e0a45a7`)
- **Source:** `docs/backlog/tasks/T-077-admin-jobs.md` @ `cb8ee9cd763c0f0e7968691e1fcf3dc44fcfddef`
- **Baseline:** `4ea27a63d0fac9ccc9eb38cde522d9b17b58799b`
- **Tested SHA:** `28daa4bf19a1ddcb38390801d4123588ff0cc613`
- **Branch:** `feat/t077-admin-jobs`
- **PR:** [#165](https://github.com/Egoka/Altera/pull/165) · [CI](https://github.com/Egoka/Altera/actions/runs/35494715302)
- **Implementer:** `b0f3bc32-dd95-471e-b40e-517aaf83edf0` · run `01a0bd72-a06c-7df8-b360-878bfbb2063e`
- **Report:** `docs/reports/2026-09-20-t077-admin-jobs-report.md`

## Result

- Added the `job.list` permission (`admin`, `owner`) from access matrix row #95 and extracted the
  pure `hasPermission` predicate from `ensurePermission` without changing error codes, so a role
  without rights never receives retry or cancel controls.
- Added `server/src/admin/jobs.ts` and `server/src/graphql/job/`: `adminJobs`, `adminJob`,
  `jobsSummary`, `retryJob`, `cancelJob`, `retryJobs` on top of the merged T-047 queue, which
  keeps enforcing the retry preconditions and writing the `job.retry` / `job.cancel` audit.
- Added `/admin/jobs` with queue metrics, address-backed filters (`kind`, `status`, `period`,
  `object`, `stuck`, `page`, `sort`), the `?id=` job card, the cancel dialog with a mandatory
  reason and the bulk retry dialog limited to 100 jobs; localized `admin.jobs.*` in `ru` and `en`.

## Verification

- `pnpm format` — passed.
- `pnpm lint` — passed.
- `pnpm test` — server 272 passed / 24 skipped; web 196 passed.
- `pnpm --filter server run build:ci` — passed without migrations.
- `pnpm --filter nuxt-app run typecheck` — passed.
- `pnpm --filter nuxt-app run build` — passed.
- Playwright — not runnable locally (the local Docker daemon answers HTTP 500 and no standalone
  PostgreSQL is installed), executed in CI instead: baseline `4ea27a63` ran 40 tests with
  30 passed / 10 skipped, tested `28daa4bf` ran 50 tests with 40 passed / 10 skipped. The
  difference is exactly the ten new `web/tests/e2e/admin-jobs.spec.ts` scenarios covering every
  state row of `jobs.md` §9.
- Preflight `scripts/autonomy/preflight.py` — ok, seven checks passed.

The first CI attempt ([run 35494536706](https://github.com/Egoka/Altera/actions/runs/35494536706))
failed one scenario because reseeding deleted `audit_logs` rows that the database protects with
an immutability trigger (`P0001`). Reseeding no longer touches the audit and the assertions look
for a record created after the current reseed; the rerun is green.

## Note on the PR head

The report and this receipt are added by a documentation-only commit on top of the tested SHA.
That commit changes no product code, test or configuration, so the tested patch of
`28daa4bf` stays the one verified by CI run 35494715302; the PR head gets its own CI run.

## Remaining

Independent native review of the tested SHA and the controller-managed development deployment
and health verification remain pending. `verified` and `accepted_at` are intentionally unset for
the controller.
