# T-059 (ALTE-111) — «О проекте» и «Письмо в редакцию» с обращениями в поддержку

**Task ID**: T-059
**Issue**: ALTE-111 (`01a0c225-9782-7011-bdee-512631a79291`)
**Source**: `docs/backlog/tasks/T-059-about-and-contact.md`, file SHA `c241a3bf14ff8724d2e0d4380ec3b3b94b083b8b`
**Baseline SHA**: `a959d15160b61bed6579aab87602225c090c0b65`
**Tested SHA**: `9514c01c75f7cc87859d91a0dfcb3ea201db2a27`
**PR**: [#203](https://github.com/Egoka/Altera/pull/203), слит в `app` как `1043e76eae1d9dd04d67b88290c76b35d423f5ca`

## Результат

- **`/contact`** создаёт обращение мутацией `createSupportRequest`. В записи — тема, адрес
  ответа, текст, путь без query и переданный `requestId`; отправитель видит номер `ticketNo`.
  - Сотрудникам `admin`/`owner` уходит письмо через службу писем T-021.
  - Лог `support.request.created` (#80) не содержит ПДн.
  - Лимиты — по `rate-limits.md` §2 п. 4: `contact.ip` в middleware, `contact.user` после
    валидации.
- **`/about`** собран из статических зон 2, 3 и 7, текста владельца
  (`staticText(kind: about)` — механизм версий юридических текстов) и непустых рубрик.
- **Кнопка «битая ссылка»** страницы 404 теперь действительно создаёт обращение.

## Критерии

| Критерий | Статус | Доказательство |
|---|---|---|
| AC-1: обращение сохраняется и содержит `requestId`, если он передан (тест) | passed | `server/tests/support-requests.test.ts`: запись с `requestId` страницы 500 и без него. `web/tests/e2e/59-about-contact.spec.ts` «гость со страницы 500»: на живой базе запись содержит `requestId`, номер на экране равен `ticketNo`. CI run 35569778630 |
| AC-2: все строки состояний обеих спецификаций воспроизводимы (Playwright) | passed | `web/tests/e2e/59-about-contact.spec.ts`, 14 сценариев. `contact.md` §8: ошибки полей, лимит с таймером, сбой сохранения с кодом, «заблокирован», неизвестная тема → «другое». `about.md` §8: загрузка, пусто (другая локаль, «готовится», подсказка `owner`), ошибка данных со статической частью. Браузерный джоб CI: 238 тестов, 229 passed, 9 skipped, 0 failed |

Строка `contact.md` §8 «Загрузка: реквизиты — скелет» не наступает: запрос `contactInfo` не
реализован. Реквизиты и адрес редакции — факты владельца, которых нет в журнале.

## Проверки

- На `6c3a4f3` все команды завершились с exit 0:
  - `pnpm format`, `pnpm lint`;
  - `pnpm test` — server 749 passed + 29 skipped, web 559 passed;
  - `pnpm --filter nuxt-app run typecheck`;
  - `pnpm --filter server run build:ci`;
  - `pnpm --filter nuxt-app run build`.
- `pnpm run codegen` дрейфа не даёт.
- `9514c01` меняет только e2e-файл. На нём прошли format, lint, pre-commit хук, а в CI все
  джобы зелёные: <https://github.com/Egoka/Altera/actions/runs/35569778630>.
- Попытки 1–2 этого прогона упали в несвязанных сценариях на моках — `02-become-author:54` и
  `22-admin-admins:155`. Третья попытка прошла целиком.
- Локально Playwright не запускался: Docker отвечает HTTP 500, локального PostgreSQL нет.

## Независимое ревью

Ревью провели отдельные агенты с чистым контекстом.

- **`79133cd`: approve.** Одно should-fix (ошибка поля «Страница») и мелочи; исправлены в
  `6c3a4f3`.
- **`6c3a4f3`: approve.** Два nit оставлены.
- **Дельта `6c3a4f3..9514c01` (только e2e): approve.**

Нативного ревьюера Multica у этих ревью нет. Поэтому `review.actor_id` и `review.run_id`
в JSON оставлены пустыми.

## Ссылки

- План: `docs/plans/2026-09-21-t059-about-contact.md`
- Отчёт захода: `docs/reports/2026-09-21-t059-about-contact-report.md`
- Receipt: `docs/reports/tasks/T-059.json`

## Остаток

- **Отклонение от спецификации.** Отказ почты уведомления не поднимается как
  `PROVIDER_UNAVAILABLE`: обращение уже сохранено, повтор создал бы дубль. Отказ фиксирует
  `mail.failed`.
- **Не решено владельцем.** Очередь обращений в админке и статус обращения в кабинете (Q-08,
  OPEN-QUESTIONS #2), текст «О проекте» и список «что не работает» (OPEN-QUESTIONS #1),
  реквизиты и адрес для `contactInfo`.
- **Лимит кнопки 404.** Она делит корзину `contact.ip`: порога 5 в час из `not-found.md` §7
  нет в реестре лимитов.
- **Выкладка.** Нужна выкладка миграции `support_requests` на development. Её и Done выполняет
  контроллер.
- **Нестабильные сценарии.** `02-become-author` и `22-admin-admins` стоит разобрать отдельно.
