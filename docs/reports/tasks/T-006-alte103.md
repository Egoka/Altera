# T-006 (ALTE-103) — Повторная проверка кеш-обёртки Redis/noop

**Task ID**: T-006
**Issue**: ALTE-103 (`01a0c0ba-22cf-7724-bb90-3805d34c2535`)
**Baseline SHA**: `924c682b56a161aa6a724462fe8d6191d9b19a80`
**Tested SHA**: `aad42a65dde3de35a114e497041a73eaadc50be8`

## Результат

T-006 принята ранее под ALTE-58 (реализация — PR #38, доказательство — PR #83, ревью
`approved`); исторические receipt `docs/reports/tasks/T-006.json` и `docs/reports/tasks/T-006.md`
сохранены без изменений. Этот заход повторно проверил три критерия на текущем `app`, который ушёл
вперёд на пять коммитов и получил второй ioredis-клиент из T-024, и закрепил AC-2 тестом
исходников вместо ручного grep.

## Критерии

| Критерий | Статус | Доказательство |
|---|---|---|
| AC-1: старт без `REDIS_URL`, запросы отвечают | passed | GraphQL ответил `{"data":{"__typename":"Query"}}`, `/health` → `checks.redis: "disabled"` |
| AC-2: в коде нет вызова `KEYS` | passed | grep по `server/src` пуст; `server/tests/cache-source-contract.test.ts` переносит проверку в набор (RED подтверждён) |
| AC-3: разные поисковые запросы — разные ключи | passed | `tests/cache.test.ts -t 'изолирует два разных поисковых запроса'`, exit 0 |
| AC-4: черновики и архив не кешируются, ключи со всеми аргументами | passed | Разбор всех вызовов `buildCacheKey` / `readThroughPublicCache`; админский поиск кеш не использует |

## Проверки

`pnpm format`, `pnpm lint`, `pnpm test` (server 591 passed + 25 skipped, web 411 passed),
`pnpm --filter server run build:ci` — все exit 0 на `aad42a6`.

## Ссылки

- Отчёт захода: `docs/reports/2026-09-21-t006-cache-verification-report.md`
- План: `docs/plans/2026-09-21-t006-cache-verification.md`
- Исторические документы T-006: `docs/reports/2026-09-15-t006-cache-wrapper-report.md`,
  `docs/reports/evidence/2026-09-16-t006-current-verification.md`,
  `docs/reports/tasks/T-006.json`, `docs/reports/tasks/T-006.md`

## Остаток

Статус в шапке `docs/backlog/tasks/T-006-cache-wrapper-noop.md` остался «кандидат» — вероятная
причина повторной выдачи задачи. Правка шапки и матрицы относится к хранителю документации.
