# T-036 (ALTE-110) — Подписка `/me/subscription` в режиме первого запуска

**Task ID**: T-036
**Issue**: ALTE-110 (`01a0c1f0-b80b-75db-8647-14c48f9906fe`)
**Source**: `docs/backlog/tasks/T-036-subscription-page-launch-mode.md`, file SHA `ac8c9563012432f56903dcc812430678fec3a780`
**Baseline SHA**: `20002d3aa074de2d8c8cfa5f25b283f607be606f`
**Tested SHA**: `3b4ae0cb5b1b3fde30a195259991e72227a20e64`
**PR**: [#199](https://github.com/Egoka/Altera/pull/199), слит в `app` как `a9ac34ab1437aeb94ec99353f646f4ca98d84b65`

## Результат

`/me/subscription` по `subscription.md` для первого запуска (журнал §24.1): план из выдач —
«Базовый план, без оплаты» и «Платные планы появятся позже.» (`[ДОПУЩЕНИЕ]`), действующая
выдача с очередью `#queue`, истёкший план с «Продлить» → `/pricing`, «Платежей нет.»
`#payments`; служебной записи — пояснение §8.17 и ссылка в админку. Гость — вход, ограниченная
сессия — 302 на `/me/archived`, «Ошибка данных» — 500 и `ErrorState` с кодом запроса.
Платёжные мутации (`startCheckout`, `cancelSubscription`, `resumeSubscription`, `requestRefund`,
`confirmPriceChange`) отвечают `FORBIDDEN`, гостю — `UNAUTHENTICATED`. `/me/subscription/checkout`
и `/result` отвечают 404; `/me/billing`, `/me/plan` — 301; `Cache-Control: private, no-store`.

## Критерии

| Критерий | Статус | Доказательство |
|---|---|---|
| AC-1: `/me/subscription/checkout` → 404 (curl) | passed | `curl` по собранному `nuxt build` на `02f8c53`: checkout (с параметрами и без) и `/result` — 404; `web/tests/e2e/36-account-subscription.spec.ts` — 404 гостю и вошедшему в CI run 35564210372 |
| AC-2: строки состояний `subscription.md` первого запуска воспроизводимы (Playwright) | passed | тот же файл: «Нет доступа», «Заблокирован» (302), базовый план, действующая выдача с очередью, «Ограничение плана», служебная запись, «Загрузка», «Ошибка данных», `FORBIDDEN` платёжных мутаций, 301 старых адресов; браузерный джоб 206 passed / 9 skipped из 215 против 195 / 9 из 204 на baseline — все 11 новых сценариев прошли |

## Проверки

`pnpm format`, `pnpm lint`, `pnpm test` (server 713 passed + 28 skipped, web 512 passed) — exit 0 на
`3b4ae0c`; `pnpm --filter server run build:ci`, `pnpm --filter nuxt-app run typecheck`,
`pnpm --filter nuxt-app run build` — exit 0 на `02f8c53` (`3b4ae0c` меняет только утилиту и тест
страницы); `pnpm codegen` без дрейфа. CI на `3b4ae0c` зелёный:
<https://github.com/Egoka/Altera/actions/runs/35564210372>. Playwright локально не запускался:
демон Docker не отвечает, Postgres на машине нет.

## Ссылки

- План: `docs/plans/2026-09-21-t036-subscription-launch.md`
- Отчёт захода: `docs/reports/2026-09-21-t036-subscription-launch-report.md`
- Receipt: `docs/reports/tasks/T-036.json`

## Остаток

Независимое native-ревью и запись Done остаются за контроллером. Экрана `/me/archived` нет.
Платность (F-01), страницы оферты платных услуг и политики возвратов, двухколоночная раскладка
§9 `[ДОПУЩЕНИЕ]` в задачу не входят. Миграций нет, выкладка не требуется.
