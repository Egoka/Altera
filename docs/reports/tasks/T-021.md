# T-021 (ALTE-55) — Модуль почты с транспортами smtp/console/fake и письмо со ссылкой для входа

**Task ID**: T-021
**Issue**: ALTE-55 (`01a0ab1b-bb56-72f6-93b8-c5f9ee63824a`)
**Source**: `docs/backlog/tasks/T-021-mail-module-transports.md` @ `bb67f06498463bc3375e7ddff8bea4ad35facad3`
**Baseline SHA**: `adbdbcb37a0e0b631d55c77ae96cd8354cbbb051`
**Tested SHA**: `5418feb8cc906bca6dc2ca7da51cb6b825512d3c`
**PR**: [#149](https://github.com/Egoka/Altera/pull/149), слит в `app` как `b1c13fd2166d589ae43b7b2f8fdde8278d5ea579`

## Результат

Модуль почты: интерфейс `MailTransport`, транспорты `smtp` (nodemailer), `console`, `fake`,
`unconfigured`; `createMailConfigFromEnv` и `createMailService`. Сервис создаёт `MailMessage`
с событием `queued`, вызывает транспорт, записывает `sent`/`failed`. Логи содержат `mailId`,
`template`, `status`, `provider`, `requestId` без адреса и содержимого. Мутация `requestMagicLink`
строит ссылку из `MAGIC_LINK_BASE_URL` и отправляет письмо `magic_link` на языке запроса.
Ошибка транспорта — `PROVIDER_UNAVAILABLE`. В production без `MAIL_TRANSPORT` — `unconfigured`.
CI: `mail-history-database.test.ts` в `server-smoke`, Mailpit в `web-smoke`.

## Критерии

| Критерий | Статус | Доказательство |
|---|---|---|
| AC-1: письмо входа в истории и в приёмнике | passed | `web/tests/e2e/21-magic-link-mail.spec.ts`: мутация → письмо в Mailpit, `MailMessage` `sent`, события `queued`+`sent`, `sanitizedBody` без токена |
| AC-2: `mail.failed` + `PROVIDER_UNAVAILABLE` при недоступном транспорте | passed | `tests/mail-service.test.ts`, `tests/magic-link-mail.test.ts`, `tests/mail-history-database.test.ts` |

## Проверки

`pnpm format`, `pnpm lint`, `pnpm codegen --check`, `pnpm test` (server 170 passed, 22 skipped;
web 155 passed), `pnpm --filter server run build:ci`, `pnpm --filter nuxt-app run typecheck` — exit 0.
CI: <https://github.com/Egoka/Altera/actions/runs/35452754674>.

## Ссылки

- План: `docs/plans/2026-09-19-t021-mail-module.md`
- Отчёт захода: `docs/reports/2026-09-19-t021-mail-module-report.md`
- Receipt: `docs/reports/tasks/T-021.json`

## Остаток

Реальный провайдер и домен отправителя — Q-01. `adminMails` — T-080.
Независимое ревью и Done — за контроллером.
