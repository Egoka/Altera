# Страница: <название>

- **Реестр**: `00-registries/pages.md` #NN · `00-registries/routes.md` #NN
- **Статус**: черновик | на утверждении | утверждён | заменён
- **ADR**: ADR-XXXX, ADR-YYYY (только ссылки, без пересказа)
- **Этап**: N (из `docs/issues/00-roadmap.md`)

## 1. Назначение

Одно-два предложения: зачем страница существует и какое главное действие на ней совершают.
Если страница — лента, назвать принцип выдачи ссылкой на `60-ranking/*`.

## 2. Аудитория и роли

| Роль | Видит | Может | Не может |
|---|---|---|---|
| Гость | | | |
| Читатель (free) | | | |
| Автор (standard / pro) | | | |
| Администраторы (по ролям, если отличаются) | | | |

## 3. Точки входа и маршруты

- Маршруты ru / en, параметры пути, query-параметры, их валидация.
- Редиректы: 301 по истории слагов, 404 / 410 / 403 и когда какой.
- Откуда сюда приходят (навигация, письма, поиск, соцсети); куда уходят.

## 4. Данные и запросы к API

| Запрос / мутация | Аргументы | Поля | Права | Кеш | Ошибки (коды ADR-0032) |
|---|---|---|---|---|---|

Условие видимости данных для каждой роли (что именно скрывается от гостя и читателя).

## 5. Структура экрана по зонам

Нумерованные зоны сверху вниз. Для каждой: назначение, содержимое, источник данных, порядок
сортировки (для лент — ссылка на файл в `60-ranking/`), что происходит при отсутствии данных.

## 6. Компоненты

| Зона | Компонент (`docs/vision/06-design-system.md` §4 или новый) | Варианты | Состояния компонента |
|---|---|---|---|

Новый компонент помечается «новый» и получает одно предложение о том, почему существующего
недостаточно.

## 7. Действия и их права

| Действие | Роль | Предусловие | Результат | Мутация | Событие / лог (код из `00-registries/events-and-logs.md`) | Подтверждение |
|---|---|---|---|---|---|---|

## 8. Состояния страницы

| Состояние | Когда | Что показано | Код ошибки | HTTP |
|---|---|---|---|---|
| Загрузка | | | — | 200 |
| Пусто | | | — | 200 |
| Ошибка данных | | | | |
| Нет доступа | | | `FORBIDDEN` | 403 |
| Не найдено / удалено | | | `NOT_FOUND` | 404 / 410 |
| Ограничение плана (истёкшая подписка, лимит) | | | | |
| Заблокирован | | | | |
| Paywall — **зарезервировано**: чтение бесплатно, состояние описывается одним предложением «как включится позже» | | | | |

Все строки обязательны; неприменимая строка получает обоснование, а не удаляется.

## 9. Мобильный и десктоп

Раскладка зон на трёх breakpoints (`docs/vision/06-design-system.md` §3); что скрывается или
сворачивается; поведение навигации и липких элементов; порядок зон на телефоне.

## 10. SEO / meta / hreflang

`title`, `description`, `canonical`, `hreflang` для ru/en, Open Graph, JSON-LD (тип), `index`
/ `noindex`, участие в `sitemap.xml` и RSS. Для служебных страниц — `noindex` с обоснованием.

## 11. Аналитические события

| Событие (код из `00-registries/events-and-logs.md`) | Триггер | Поля (без ПДн) | Участвует в рейтинге |
|---|---|---|---|

## 12. Открытые вопросы и допущения

- `[ДОПУЩЕНИЕ]` … (все допущения файла собраны здесь).
- `[ВОПРОС ВЛАДЕЛЬЦУ]` … — если да: …; если нет: ….
