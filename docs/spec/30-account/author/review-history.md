# Страница: Проверка и история решений

- **Реестр**: `00-registries/pages.md` #38 · `00-registries/routes.md` #37
- **Статус**: утверждён (гейт Г6, 2026-09-09)
- **ADR**: ADR-0039 (в части, не противоречащей журналу: балла нет — #41), ADR-0045; журнал §6,
  §16, #12, #14, #16, #18, #40–42, §24.3; `50-access/roles/moderator.md`; `10-flows/write-and-publish.md`
  (#3), `moderation.md` (#5)
- **Этап**: 1

## 1. Назначение

История проверки одной языковой версии: вердикт автоматической проверки с текстом причин
(журнал §16.4, §24.3), сообщения ревьюера — запрос доработки с рекомендациями, снятие с
публикации, ручная публикация, окончательный отказ с комментарием (журнал §6.5–6, #16), в
хронологии. Главное действие — понять, что исправить, и вернуться в редактор. Оспаривания
решения AI нет (журнал #40): единственный путь — правка и ручная ветка.

## 2. Аудитория и роли

| Роль | Видит | Может | Не может |
|---|---|---|---|
| Гость | — (302 на `/login?next=…`) | — | — |
| Читатель (free) | бывший автор — историю своих версий | читать | подавать повторно (`PLAN_LIMIT`) |
| Автор (standard / pro) | вердикт и причины по категориям с текстом (§24.3); сообщения рецензента по роли, без имени сотрудника (§25.9); непубличную двустороннюю переписку внутри конкретного решения (§25.6); даты; текущий статус | перейти в редактор; ответить рецензенту внутри решения; отметить прочитанным | видеть имя сотрудника; видеть пороги и внутренние признаки проверки (§24.3) |
| Администраторы (по ролям, если отличаются) | `editor` — по редакционным; `moderator`/`owner` — ту же историю в `/admin/review` (не здесь) | — | — |

Условие видимости: `article.aiCheck.read` (#8) — свои; `review.message` (#102) — сообщения
автору; `review.reply.author` (#121 `[черновик]`) — ответы автора; переписка непублична (§25.6);
служебные заметки рецензента, не адресованные автору, не показываются `[ДОПУЩЕНИЕ]`.

## 3. Точки входа и маршруты

- `/me/articles/{id}/review`, `/en/me/articles/{id}/review` — `id` языковой версии; `#decision-{n}`
  — якорь решения из письма.
- Редиректы: без сессии — 302 на `/login?next=…`; ограниченная сессия — `/me/archived`; чужая
  версия — 404.
- Приходят: письма о решениях (журнал §16.5), «Мои материалы» («есть причины»), редактор
  (панель «Проверка» → «вся история»), сводка. Уходят: редактор, `/legal/content-rules#…`
  (категория причины), `/me/articles`.

## 4. Данные и запросы к API

| Запрос / мутация | Аргументы | Поля | Права | Кеш | Ошибки (коды ADR-0032) |
|---|---|---|---|---|---|
| `translation.aiCheck(id)` | версия | `decision: publish \| reject \| pending \| none`, `checkedAt`, `reasons[] { category, anchor, text }` (текст формирует проверка — §24.3), `attempt` (первая / после «перередактировать») | #8 свои | нет | `UNAUTHENTICATED`, `NOT_FOUND` |
| `translation.reviewHistory(id)` | версия | `items[] { id, kind: rework_request \| unpublish \| manual_publish \| final_reject \| message \| ai_decision \| submitted \| withdrawn \| published_auto, text, recommendations, byRole, createdAt, readAt, thread[] { byRole \| author, text, createdAt } }` — переписка внутри решения (§25.6) | #8, #102 свои | нет | — |
| `markReviewRead(translationId)` | версия | `readAt` | свои | — | — |
| `replyToDecision(decisionId, text)` | решение из истории, текст | запись `author_reply` в ветке решения (§25.6) | #121 `[черновик]` свои | — | `VALIDATION_ERROR`, `CONFLICT` (решение закрыто окончательным отказом `[ДОПУЩЕНИЕ]`), `RATE_LIMITED` (общий лимит мутаций кабинета) |

Категории причин — те же, что в правилах публикации (`legal-content-rules.md` §4, §24.3); текст
причины — сформированный проверкой, без порогов и признаков; для окончательного отказа ревьюер
может добавить комментарий (§24.3) — он и есть `text` записи `final_reject`. Все записи хранятся
с автором решения и временем (журнал §6.7); автору показывается роль.

## 5. Структура экрана по зонам

1. **Шапка сайта** и меню кабинета.
2. **Карточка версии**: заголовок, язык, статус, «открыть в редакторе» (или «только чтение» для
   `rejected`, истёкшего плана).
3. **Текущее решение**: крупно — «проверяется» / «опубликовано автоматически» / «не прошла
   проверку: N причин» / «на доработке: рекомендации» / «снята с публикации» / «отклонена
   редакцией» с комментарием; кнопка следующего шага («исправить и отправить», «отозвать»).
4. **Причины отказа проверки** (когда есть): список по категориям — название категории со ссылкой
   на правила, текст причины; пояснение «после правки материал посмотрит ревьюер, повторной
   автоматической проверки не будет» (журнал #14).
5. **История**: хронология всех записей `reviewHistory` с ролью и датой; рекомендации рецензента —
   раскрываемые; непрочитанные — отмечены; у каждого решения — ветка переписки с полем ответа
   (§25.6).
6. **Что дальше**: короткие правила — окно «перередактировать» (журнал #9), нет оспаривания AI
   (журнал #40), окончательный отказ — чтение и копирование (журнал §4.5).
7. **Футер**.

## 6. Компоненты

| Зона | Компонент (`docs/vision/06-design-system.md` §4 или новый) | Варианты | Состояния компонента |
|---|---|---|---|
| 1, 7 | `AppHeader`, `AppFooter` | кабинет | — |
| 2 | `ArticleRow` (`dashboard.md`), `StatusBadge` | — | — |
| 3 | `DecisionCard` — новый: крупное текущее решение с действием; аналогов нет | pending / publish / reject / rework / unpublish / final | — |
| 4 | `ReasonList` — новый: причины по категориям с ссылками на правила | — | пусто — скрыт |
| 5 | `Timeline` — новый: хронология решений; переиспользуется в админке | — | непрочитанное |
| 5 | `DecisionThread` — новый: непубличная переписка внутри решения с полем ответа (§25.6) | автор / рецензент | закрыта |

## 7. Действия и их права

| Действие | Роль | Предусловие | Результат | Мутация | Событие / лог (код из `00-registries/events-and-logs.md`) | Подтверждение |
|---|---|---|---|---|---|---|
| Открыть редактор | автор, `editor` | версия не `rejected`; план активен (иначе чтение) | `/me/articles/{id}/edit` | — | — | нет |
| Исправить и отправить | автор | после отказа AI или запроса доработки | редактор → `submitTranslation` → `review` (журнал #14) или снова AI после «перередактировать» (журнал §6.4) | — (в редакторе) | `translation.submit` (#45) | в редакторе |
| Отозвать | автор | `ai_check` / `review` | `draft` | `withdrawTranslation` | `translation.withdraw` (#45) | нет |
| Отметить прочитанным | автор | есть непрочитанные | счётчик в списке и сводке обнуляется | `markReviewRead` | — | нет |
| Ответить рецензенту | автор | решение открыто (доработка, снятие) | сообщение в ветке решения; рецензент видит в `/admin/review` (§25.6) | `replyToDecision` | `review.reply` (#86 `[черновик]`) | нет |
| Открыть категорию в правилах | все | — | `/legal/content-rules#…` | — | — | нет |

## 8. Состояния страницы

| Состояние | Когда | Что показано | Код ошибки | HTTP |
|---|---|---|---|---|
| Загрузка | получение истории | скелет | — | 200 |
| Пусто | версия ещё не подавалась (`routes.md` #37 «П») | «проверок ещё не было» и правила зоны 6 | — | 200 |
| Ошибка данных | история не получена | `ErrorState` | `INTERNAL_ERROR` | 500 |
| Нет доступа | нет сессии | 302 на `/login?next=…` | `UNAUTHENTICATED` | 302 |
| Не найдено / удалено | чужая или удалённая навсегда версия | `not-found.md` | `NOT_FOUND` | 404 |
| Ограничение плана (истёкшая подписка, лимит) | план истёк | история доступна на чтение (§8.20); кнопки подачи — `PLAN_LIMIT` с `/pricing` | `PLAN_LIMIT` | 200 |
| Заблокирован | ограниченная сессия | 302 на `/me/archived` | `FORBIDDEN` | 302 |
| Paywall — **зарезервировано**: чтение бесплатно, состояние описывается одним предложением «как включится позже» | никогда | не зависит от paywall | — | 200 |

## 9. Мобильный и десктоп

Одна колонка `max-w-3xl`; на телефоне карточка решения — сверху, кнопка действия липкая внизу;
история — сворачиваемые записи.

## 10. SEO / meta / hreflang

`title` — «Проверка: {Заголовок} — {Издание}»; `noindex, nofollow`; без `canonical`, `hreflang`;
не в sitemap.

## 11. Аналитические события

| Событие (код из `00-registries/events-and-logs.md`) | Триггер | Поля (без ПДн) | Участвует в рейтинге |
|---|---|---|---|
| `ai.decision` (#71) | решение проверки (пишет система) | `translationId`, `verdict`, `reasons` категории | нет (журнал §16.3) |
| `review.message` (#70) | сообщение ревьюера (пишет ревьюер) | `translationId`, `kind` | нет |
| `translation.reject.final` (#69) | окончательный отказ | `translationId` | нет |
| `http.request` (#41) | просмотр | `route`, `status` | нет |

## 12. Открытые вопросы и допущения

- `[ДОПУЩЕНИЕ]` Ревьюер показан ролью, без имени; переписка односторонняя (автор отвечает
  правкой или ответом); служебные заметки не показываются; якоря `#decision-{n}`; переписка
  закрывается окончательным отказом.
- Отложено (§25.13): письма о решениях — проход почты; (жалобы) — не касается.
- Закрыто Г1–Г5b (журнал #14, #40–42, §16.4, §24.3): нет оспаривания AI; причины — текст
  проверки по категориям; комментарий ревьюера при окончательном отказе; пороги не показываются.
- Закрыто Г6 (§25.6, §25.9): непубличная двусторонняя переписка внутри конкретного решения;
  рецензент обозначен ролью.
