# Страница: Лента рубрики

- **Реестр**: `00-registries/pages.md` #2 · `00-registries/routes.md` #2
- **Статус**: утверждён (гейт Г3, 2026-09-08)
- **ADR**: ADR-0002, ADR-0004, ADR-0005, ADR-0019, ADR-0034, ADR-0036
- **Этап**: 1 (по новизне), 2 (по рейтингу после Г4)

## 1. Назначение

Лента рубрики — список опубликованных версий локали внутри одной рубрики. До утверждения
модели рейтинга на Г4 лента сортируется **только по новизне**; переключатель или отдельная
выдача «по рейтингу» на первом этапе не показываются (журнал §20.4). После Г4 порядок задаёт
`60-ranking/feed-principles.md` с учётом соответствия теме (`topic-relevance.md`). Фильтры по
формату и тегу. Главное действие — открыть материал. Рубрика — адресообразующая сущность
(ADR-0005): её слаг — первый сегмент пути; пустая рубрика не публична (журнал §20.9).

## 2. Аудитория и роли

| Роль | Видит | Может | Не может |
|---|---|---|---|
| Гость | шапку рубрики, список карточек по новизне, фильтры, пагинацию; словесное объяснение позиции — после Г4 | открыть материал, фильтровать, листать | ставить закладки; видеть числа рейтинга; переключать сортировку до Г4 |
| Читатель (free) | то же с активными закладками | закладка с карточки | видеть числа рейтинга |
| Автор (standard / pro) | то же | всё, что читатель | видеть свои неопубликованные материалы в ленте — только через кабинет |
| Администраторы (по ролям, если отличаются) | то же, что гость; закладок нет (журнал #58); `admin`/`owner` видят ссылку «редактировать рубрику» → `/admin/sections` `[ДОПУЩЕНИЕ]` | открыть материал | видеть архивированные материалы рубрики — только в админке |

Видимость: только `published` версии локали (`50-access/visibility.md` п. 1); языки не
смешиваются (журнал §20.5); архивированная рубрика для всех — 404 (`admin-sections.md` #2);
рубрика без опубликованных материалов в локали — 404 до первой публикации (журнал §20.9)
`[ДОПУЩЕНИЕ: 404, а не пустая страница — рубрика «не публична»]`.

## 3. Точки входа и маршруты

- `/{рубрика}` и `/en/{section}` — слаги рубрики по локали (ADR-0004); query: `?format=<слаг>`,
  `?tag=<слаг>`, `?page=N` (N ≥ 1, целое; иначе 404 `[ДОПУЩЕНИЕ]`); параметр `?sort=` появится
  после Г4, если модель рейтинга его предусмотрит (журнал §20.4).
- 301 по истории слагов рубрики (ADR-0004); неизвестный слаг — 404 (страница #21); слаг
  совпадает с зарезервированными путями (`authors`, `tags`, `sections`, `search`, `pricing`,
  `login`, `me`, `admin`, `legal`, `about`, `contact`, `auth`) — рубрика с таким слагом не
  создаётся (`admin-sections.md` #2).
- Приходят: шапка (`SectionNav`), кикер рубрики на карточках главной, страница материала
  (кикер рубрики), список рубрик, поиск, поисковые системы. Уходят: материал, автор, тег,
  другая рубрика.

## 4. Данные и запросы к API

| Запрос / мутация | Аргументы | Поля | Права | Кеш | Ошибки (коды ADR-0032) |
|---|---|---|---|---|---|
| `section(slug, locale)` | `slug`, `locale` | `slug`, `name`, `description`, `cover`, `articleCount`, `formats[]`, `topTags[]`, `slugHistory` (для 301) | все | общий | `NOT_FOUND` |
| `feed(scope: section, slug, locale, format, tag, page)` | как в query | `items[]` карточек (как `home.md` §4), `pageInfo { page, totalPages, hasNext }`, `caption` («по дате публикации») | все | общий по набору параметров; инвалидация при публикации/снятии в рубрике | `NOT_FOUND`, `VALIDATION_ERROR` (`page`) |
| `addBookmark` / `removeBookmark` | `articleId` | `bookmarked` | `reader`, `author` | нет | `UNAUTHENTICATED`, `FORBIDDEN`, `NOT_FOUND` |

Ответ ленты не зависит от сессии (ADR-0019); отметки «в закладках» подмешивает клиент из `me`.

## 5. Структура экрана по зонам

1. **Шапка сайта** (`AppHeader`) с активной рубрикой в `SectionNav`.
2. **Шапка рубрики** (`SectionHeader`): название, описание (пусто — только название), число
   материалов, обложка при наличии.
3. **Панель ленты**: подпись «по дате публикации» (журнал §20.4; переключателя нет), фильтр
   формата (`formats[]` рубрики), фильтр тега (`topTags[]` + ввод слага `[ДОПУЩЕНИЕ]`), кнопка
   «сбросить».
4. **Список**: карточки `large` первые три `[ДОПУЩЕНИЕ]`, далее `small`; сетка по breakpoints;
   `RankExplainer` под карточкой — после Г4. Пусто по фильтру — «ничего не найдено, сбросить
   фильтры»; пустой рубрики не бывает — она не публична (журнал §20.9).
5. **Пагинация** (`Pagination`): по страницам, без бесконечной прокрутки; размер страницы —
   параметр `feed-principles.md` `[ДОПУЩЕНИЕ: 24]`; порядок стабилен между страницами
   (`publishedAt`, затем `id`).
6. **Другие рубрики**: горизонтальный ряд остальных непустых рубрик со счётчиками.
7. **Футер**.

## 6. Компоненты

| Зона | Компонент (`docs/vision/06-design-system.md` §4 или новый) | Варианты | Состояния компонента |
|---|---|---|---|
| 1, 7 | `AppHeader`, `SectionNav`, `AppFooter` | — | нет рубрик |
| 2 | `SectionHeader` | с обложкой / без | описание пустое |
| 3 | `FeedControls` — новый: подпись принципа и фильтры лент (переключатель сортировки — после Г4); существующий `SearchBox` решает другую задачу | рубрика / тег / автор | фильтр без значений скрыт |
| 4 | `ArticleCard` | `large`, `small` | без изображения; бейдж pro; «перевод»; закладка |
| 4 | `RankExplainer` (2) | — | скрыт до Г4 |
| 4 | `EmptyState` | фильтр пуст | — |
| 5 | `Pagination` | — | конец списка; одна страница — скрыта |
| все | `LoadingSkeleton`, `ErrorState` | — | — |

## 7. Действия и их права

| Действие | Роль | Предусловие | Результат | Мутация | Событие / лог (код из `00-registries/events-and-logs.md`) | Подтверждение |
|---|---|---|---|---|---|---|
| Фильтр формата / тега | все | — | новый URL, страница сбрасывается на 1 | — | `http.request` | нет |
| Листать | все | `page ≤ totalPages` | новый URL `?page=` | — | `http.request` | нет |
| Закладка | `reader`, `author` | сессия | иконка меняется | `addBookmark` / `removeBookmark` | `bookmark.add` / `bookmark.remove` (#56) | нет |
| Открыть материал | все | — | переход | — | — | нет |

## 8. Состояния страницы

| Состояние | Когда | Что показано | Код ошибки | HTTP |
|---|---|---|---|---|
| Загрузка | клиентская навигация между страницами и фильтрами | скелеты карточек, панель активна | — | 200 |
| Пусто | фильтр без результатов (рубрика без материалов не публична — 404) | `EmptyState` «ничего не найдено», зона 6 | — | 200 |
| Ошибка данных | `feed` не ответил | `ErrorState` с `requestId`; шапка рубрики, если получена | `INTERNAL_ERROR` | 500 |
| Нет доступа | не применимо: лента публична | — | `FORBIDDEN` | 403 — не возникает |
| Не найдено / удалено | неизвестный слаг; архивированная рубрика; рубрика без публикаций в локали (журнал §20.9) | страница #21 | `NOT_FOUND` | 404 (410 не используется: рубрика архивируется только пустой) |
| Ограничение плана (истёкшая подписка, лимит) | не применимо к чтению | — | `PLAN_LIMIT` | — |
| Заблокирован | архивированный аккаунт видит ленту как гость | обычная лента | — | 200 |
| Paywall — **зарезервировано**: чтение бесплатно, состояние описывается одним предложением «как включится позже» | никогда | при включении карточки получат метку доступа | — | 200 |

Некорректный `page` — `VALIDATION_ERROR` от API превращается в 404 страницы `[ДОПУЩЕНИЕ]`.

## 9. Мобильный и десктоп

- `lg`: список в 3 колонки для `small`, `large` — во всю ширину контейнера; панель фильтров в
  одну строку.
- `md`: 2 колонки; фильтры переносятся на вторую строку.
- `< md`: одна колонка; панель сворачивается в кнопку «Фильтры» с раскрывашкой; зона 6 —
  горизонтальная прокрутка.

## 10. SEO / meta / hreflang

`title` — «{Рубрика} — {Издание}»; `description` — описание рубрики или первое предложение
шаблона; `canonical` — адрес без `format`, `tag`, с `page` при `page > 1`; `hreflang` на
сестринский слаг рубрики только при наличии публикаций в обеих локалях; отфильтрованные
варианты — `noindex, follow`
`[ДОПУЩЕНИЕ]`; Open Graph — обложка рубрики или дефолт; JSON-LD `CollectionPage` +
`BreadcrumbList`; в `sitemap.xml`; RSS рубрики по дате (3, `feeds-and-sitemap.md`).

## 11. Аналитические события

| Событие (код из `00-registries/events-and-logs.md`) | Триггер | Поля (без ПДн) | Участвует в рейтинге |
|---|---|---|---|
| `http.request` (#41) | запрос страницы, включая фильтры и пагинацию | `route`, `status`, `durationMs` | нет |
| `bookmark.add` / `bookmark.remove` (#56) | закладка | `articleId` | нет |
| `page.error` (#64) | ошибка рендера | `route`, `code`, `requestId` | нет |

Просмотры ленты в рейтинг не входят: прочтение засчитывает только страница материала.

## 12. Открытые вопросы и допущения

- `[ДОПУЩЕНИЕ]` Размер страницы 24, первые три карточки `large`; `page` вне диапазона — 404;
  рубрика без публикаций в локали — 404.
- `[ДОПУЩЕНИЕ]` Фильтр по тегу принимает только слаг из `topTags` или ввод; фильтры делают адрес
  `noindex, follow`.
- `[ДОПУЩЕНИЕ]` Ссылка «редактировать рубрику» для `admin`/`owner` в шапке рубрики.
- Закрыто Г3 (журнал §20.4): до Г4 — только по новизне, без переключателя; возможность
  сортировки по рейтингу проектируется в `feed-principles.md`.
